# Claude-kit v0.1 fixes: completion report

- **Dispatches:** `prompts/preserved/2026-09-23-02.md` (the owner's ruling, items 1-6, D1-D8), `-03.md` (continuation, R1-R6), `-04.md` (item 4b, backup, detached checkout, E1-E5), `-07.md` (this final dispatch, with the owner's rulings S1-S3 and R1-R9).
- **Record:** `RECORD.md` intents 2026-09-23-03 (closed `superseded` by -05), -04, -06 and -07; dispatch-notes -08 and -09 for the live exercise; terminals -10 (closes -04), -11 (closes -06) and -12 (closes -07).
- **Branch:** `kit-v0.1-fixes`, cut from `origin/main` `9390407` (the PR #1 merge).
- **PR:** `kit-v0.1-fixes` -> `main`, opened after the commit that carries this report.
- **Not done: no merge, no tag.** Both are the owner's steps.

## What landed

| Commit | What |
|---|---|
| `0a17cdb` | Record: saved dispatch `2026-09-23-02.md`, vacuous sweep, intent 2026-09-23-03 |
| `2834f9d` | Item 3: drop the `checkers_dir` key; the checkers sit at the root |
| `09fb92d` | Item 1: approval fails closed; new required key `approval_exempt_types` |
| `162bd98` | Item 2: agents map to roles; role_guard restricts by role (new required key `agent_roles`) |
| `6b55855` | Record: continuation, saved dispatch `2026-09-23-03.md`, intent -04, -03 closed `superseded` |
| `271faad` | Item 6: the pulse role may use `SubagentHandback` |
| `3ce90e3` | Item 4: released files cite the kit by release version; denylist pattern `(?i)claude-kit[^\n]{0,40}RECORD\.md` |
| `92b26d3` | Record: saved dispatch `2026-09-23-04.md`, intent -06 |
| `0e7f5f9` | Item 4b: released files cite the kit by version, never unversioned; denylist pattern `(?i)\bClaude-kit addition\b` |
| `3952128` | Record: the live exercise; saved dispatch `2026-09-23-07.md`, the exercise's `-05.md` and `-06.md`; intent -07, notes -08 and -09 |

The final commit adds this report and the three terminal entries. The code has been final since `0e7f5f9`; nothing after it touches the hooks, `kit.json` or the tests.

Test counts:

| | v0.1 (`9390407`) | now |
|---|---|---|
| Hook tests | 68 | 83 |
| Install and release tests | 16 | 18 |
| Checker self-tests | 23 and 5 | 23 and 5 |
| Release check denylist patterns | 16 | 18 |

## Behaviour diff

`python3 workshop/behaviour_diff.py --source ~/Zynergy/Forager`, run 2026-09-24 on this branch at `3952128` (the code is the same as `0e7f5f9`). Full output:

```
source tests: b2435efffc (harness.py, test_device_guard.py, test_dispatch_guard.py, test_history_guard.py, test_role_guard.py)
kit hooks and checkers: working tree of /home/zynergy-labs/Zynergy/Claude-kit-fixes
config keys derived from the source: agent_roles, android_package, approval_exempt_types, dispatchable_agents, guard_env_prefix, protected_branches, required_sections, type_targets
FAIL: test_allowlisted_tools_pass (test_role_guard.PlannerTools.test_allowlisted_tools_pass) [Grep]
AssertionError: 'deny' is not None : Grep: deny role_guard: the planner role may not use Grep. The planner allowlist is Agent, AskUserQuestion, Bash, Read, Skill, TodoWrite, ToolSearch, WebFetch, WebSearch; any other tool is denied until the operator adds it by name.
FAIL: test_allowlisted_tools_pass (test_role_guard.PlannerTools.test_allowlisted_tools_pass) [Glob]
AssertionError: 'deny' is not None : Glob: deny role_guard: the planner role may not use Glob. The planner allowlist is Agent, AskUserQuestion, Bash, Read, Skill, TodoWrite, ToolSearch, WebFetch, WebSearch; any other tool is denied until the operator adds it by name.
Ran 49 tests in 7.641s
FAILED (failures=2)

BEHAVIOUR DIFF PASS: 49 source tests ran; the only failures are the expected [('test_allowlisted_tools_pass', 'Glob'), ('test_allowlisted_tools_pass', 'Grep')]
```

49 source tests ran. The only failures are the two Grep and Glob subtests the owner accepted for v0.1.

## Failing-first and sabotage evidence, per item

The first coder (under intent -03) wrote the evidence for items 3, 1 and 2 to `/tmp/kitfix_evidence`, and it survived only as summaries in those commit messages. Under intent -04 it was re-run in scratch copies, with output in `/tmp/kitfix_c04_evidence/`. Both directories are backed up: `~/forager-backups/2026-09-23-04/` holds `kitfix_evidence/`, and `2026-09-24-01/` holds `kitfix_c04_evidence/`. The item 4b ("c06") evidence is in `0e7f5f9`'s commit message; its saved files are in `/tmp/kitfix_saved/c06_item4b/`.

**Item 3 (`2834f9d`).**
- Failing first, against v0.1's hooks and harness (`item3_rerun.txt`): `test_checkers_dir_is_an_unknown_key` failed with `AssertionError: None != 'deny'` on each of role_guard.py, dispatch_guard.py, device_guard.py and history_guard.py (`FAILED (failures=4)`). With the fix: OK.
- Sabotage (`checkers_dir` put back in CONFIG_DEFAULTS): the same four failures.

**Item 1 (`09fb92d`).**
- Failing first, against v0.1's hooks and harness (`item1_rerun.txt`): `test_type_not_exempt_asks` failed with `'allow' != 'ask'`, and `test_approval_exemptions_come_from_config` with `'deny' != 'ask'`.
- Against the parent's (`2834f9d`) hooks with the new harness, v0.1's validator rejects the new key: `unknown key(s) approval_exempt_types`.
- Sabotage 1 (the hook uses a literal build/device split): `'allow' != 'ask'` on both dispatch tests.
- Sabotage 2 (the validator ignores stray exempt types, `item1b_rerun.txt`): `test_exempt_type_must_be_a_configured_type` failed with `None != 'deny'` on all four hooks.

**Item 2 (`162bd98`).**
- Failing first, against v0.1's hooks and harness (`item2_rerun.txt`): `test_an_agent_with_no_role_is_denied_everything` failed with `None != 'deny'` for Read, Bash and Write.
- Against the parent's hooks with the new harness, the role tests fail on role_guard's own wording. Example: `"role_guard: agent 'general-purpose' has no role in agent_roles" not found in '... unknown key(s) agent_roles'`.
- Four sabotages, each failing the tests:
  - An unknown agent passes: `None != 'deny'` x3.
  - The role is taken from the agent name: 5 failures, for example `'role_guard: the pulse role may not use Write' not found in "role_guard: agent 'reader' has role 'reader', ..."`.
  - An unknown role passes: 2 failures.
  - The validator skips roleless agents: `None != 'deny'` on all four hooks.

**Item 6 (`271faad`).**
- Failing first, the new tests against the parent's (`162bd98`) and v0.1's (`9390407`) role_guard (`item6_failfirst.txt`): `test_pulse_may_hand_back` failed with `'deny' is not None : role_guard: the pulse role may not use SubagentHandback. ...`. `test_planner_may_not_hand_back` passes on both; it guards against widening and is not a failing-first test.
- Sabotage 1 (pulse loses the tool) fails `test_pulse_may_hand_back` the same way. Sabotage 2 (planner gains the tool) fails `test_planner_may_not_hand_back` with `None != 'deny'`.

**Item 4 (`3ce90e3`).**
- Failing first: the new test with no pattern failed with `AssertionError: 0 != 1 : PASS: ... 16 denylist pattern(s), no match.` With the pattern and the old wording, release_check.py failed on exactly `.claude/hooks/tests/test_role_guard.py:41`, `check_record.py:97` and `check_prompts.py:172`.
- Sabotage 1 (pattern removed) fails the new test with `0 != 1`. Sabotage 2 (old citation back at check_record.py:97) fails the release check on that line, and `test_release_set_passes` with `1 != 0`.

**Item 4b (`0e7f5f9`, from its commit message).**
- Failing first: the new test with no pattern failed with `0 != 1`. With the pattern and the old wording, release_check.py failed on exactly `check_record.py:953` and `check_prompts.py:244`. After rewording it passes with 18 patterns.
- Sabotage 1 (pattern removed) fails the new test with `0 != 1`. Sabotage 2 (old wording back at check_record.py:953) fails `test_release_set_passes` and `test_planted_string_fails_naming_file_and_line`.

## Live exercise (item 5)

The owner ran it on 2026-09-23 by hand, in the session "Kit live exercise" in `~/Zynergy/Claude-kit`, detached at `0e7f5f9`. Session log: `~/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit/62f3f7fc-29e7-4c89-8cf4-7279ef048198.jsonl` (117 lines). Pulse transcript: `.../62f3f7fc-29e7-4c89-8cf4-7279ef048198/subagents/agent-a07a4dfcb571cac42.jsonl` (21 lines). In lines 49 and 80 the hook's JSON output is stored as an escaped string inside the log's JSON record. The quotes below are unescaped; everything else is verbatim.

1. **Pulse to `pulse` (runs, reports).**
   - Log line 49 (2026-09-23T23:05:56.714Z): `"permissionDecision": "allow", "permissionDecisionReason": "dispatch_guard: Type 'pulse' dispatch to pulse preserved at prompts/preserved/2026-09-23-05.md (HEAD 0e7f5f938a). Type 'pulse' is in approval_exempt_types, so it runs without approval.`
   - Transcript line 18 (23:06:07.056Z) is the `SubagentHandback` tool call. Line 19 (23:06:08.529Z) is `{"success":true,"message":"Report delivered to your caller."}`.
   - Recorded by dispatch-note 2026-09-23-08.
2. **Build to `coder` (approval asked, declined).**
   - Log line 80 (23:13:35.604Z): `"permissionDecision": "ask", "permissionDecisionReason": "dispatch_guard: Type 'build' dispatch to coder preserved at prompts/preserved/2026-09-23-06.md (HEAD 0e7f5f938a). Operator approval required: Type 'build' is not in approval_exempt_types.`
   - Line 81 (23:14:18.302Z) is the tool result, containing `Denied by user`, with `"toolDenialKind":"user-rejected"`.
   - Recorded by dispatch-note 2026-09-23-09.
3. **`Type: pulse` to `coder` (blocked mismatch).** Log line 92 (23:16:18.899Z) is the tool result: `PreToolUse:Agent hook error: dispatch_guard: Type 'pulse' may only be dispatched to 'pulse', not 'coder'.`, with `"is_error":true` and `"toolDenialKind":"permission-rule"`.
4. **To `general-purpose` (blocked unknown agent).** Log line 102 (23:17:36.619Z) is the tool result: `PreToolUse:Agent hook error: dispatch_guard: subagent type 'general-purpose' may not be dispatched. Only coder, pulse may; built-in agent types are blocked.`, with `"toolDenialKind":"permission-rule"`.

For cases 3 and 4 the log holds no `permissionDecision` of `deny`; the log contains no `"deny"` string at all. That these denials came from `dispatch_guard` is an inference from two things: the error text names it, and that text matches `dispatch_guard.py:102-105` (unknown agent) and `:120-122` (Type/target mismatch) at `0e7f5f9`.

The reasons on lines 49 and 80 also carry the hook's `check_prompts.py exit 1` report, which lists `2026-09-23-05.md` (and on line 80, `-06.md`) as unclaimed. That was expected at the time: those files were not yet recorded. The report did not block the dispatch. Both files are now claimed.

## What was not tested

- **role_guard's deny for an unknown agent** is proven by unit tests only (`test_an_agent_with_no_role_is_denied_everything` and `test_an_agent_with_an_unknown_role_is_denied_everything`). Live, dispatch_guard blocks the dispatch before any such agent can run a tool. The live "unknown agent" was a built-in, `general-purpose`, as D7 set out.
- **The declined build's coder** never ran, so role_guard's coder role was not exercised live.
- **The owner's second "Kit live exercise" session** (PID 110771, on v0.2) is not part of this evidence. Its log was not read.

## Restore of ~/Zynergy/Claude-kit (H5)

- **Before switching** (2026-09-24T05:51Z), the only `claude` processes with cwd `~/Zynergy/Claude-kit` were 110686 (`claude rc`, the bridge that hosts the planner) and its child 110712. Both were running during the switch. PIDs 100263, 101054 and 110771 were gone (`/proc/<pid>` absent). No signal was sent to any process.
- **The switch:** `git -C ~/Zynergy/Claude-kit checkout kit-v0.1` succeeded without force. After it, the checkout was on `kit-v0.1` at `a544ca9d504782a17b62dc77f82895ec9c0056ba`.
- **What remains untracked there:** `.claude/worktrees/`, plus `prompts/preserved/2026-09-23-05.md` and `-06.md`. Both were left untouched; their hashes are unchanged.
- **Checker at kit-v0.1:** `check_prompts.py` there reports `FAIL: 2 binding violation(s)`, both files unclaimed. That is expected: that checkout's `RECORD.md` is v0.1's, and they are recorded on this branch.

## Backups

- `~/forager-backups/2026-09-23-03/`: the v0.1 build's `/tmp` scratch.
- `2026-09-23-04/`: ~/Zynergy/Claude-kit before the detached checkout, plus `/tmp/kitfix_evidence` and `/tmp/kitfix_saved`.
- `2026-09-24-01/` (930 files; MANIFEST.sha256 `d0bbe177…a183`, verified on both sides at 2026-09-24T05:49:39Z) holds:
  - the exercise session log and its subagents;
  - the lost planner session's log and its subagents;
  - task output `btz9v3e6i.output`;
  - the new planner's `subagents/`;
  - every `/tmp/kitfix_*` path not in 2026-09-23-04, plus a fresh `kitfix_saved`;
  - the untracked hook-saved dispatches of harness worktrees 01Ftd, 01JDb and 01Vss.

Each has one INDEX.md line.

## Flags raised along the way

These are reported as the dispatch states them, except where marked "verified".

- **The pulse's line-197 guess:** a pulse guessed from line 197 of a log. The planner refuted it from lines 190-197.
- **history_guard blocking heredoc commit messages:** it blocked commit messages written as heredocs. Coders used `-m` arguments instead.
- **Push failure:** one push failed with `publickey`.
- **Lost planner session:** it died after sending `2026-09-24-02.md` to coder. Verified in the planner log `8c77054d-3517-59bc-9ad1-dde101a01572.jsonl`, which holds:
  - line 505: the owner's "I'll take your recommendations";
  - line 508: the Agent call, at 00:56:52Z;
  - no tool result after it;
  - line 512: task `btz9v3e6i` `killed`, at 01:17:08Z.
  That the session's exit took its coder with it is inferred, not verified.
- **Six stopped re-sends and rulings R1-R9:** after the loss, six re-sends from the new planner stopped with nothing committed. They are 01Vss `2026-09-24-03.md` to `-08.md`, cited by path and hash in intent -07. The owner answered them with rulings R1-R9.
- **Rejected plan to edit a committed entry:** the planner planned to edit a committed record entry. The 24-05 coder caught it, and the plan was rejected. The record stays append-only.
- **The 01Ftd file:** the planner missed the untracked 01Ftd `2026-09-23-02.md`. The 24-08 coder caught it. It is backed up and cited under R9.
- **New in this run:** the dispatch cited `dispatch_guard.py:103-106,121-123`. At `0e7f5f9` the two returns are at lines 102-105 and 120-122. This report cites the actual lines.

## Deferred to v0.2, not built

1. The four items of the owner's ruling: history_guard over-blocking on unparseable push commands; a dispatch-note outcome `stopped`; upgrades removing dropped files; a kit bypass table.
2. R6 (a)-(c) of intent 2026-09-23-04:
   - (a) per-checkout numbering of saved dispatches. It is extended by the hook naming files by UTC date, and by per-worktree numbering colliding across harness worktrees. For example, the fix store's `2026-09-23-07.md` and 01JDb's `2026-09-23-07.md` are different dispatches.
   - (b) no entry kind for continuing an intent.
   - (c) whether the planner gains SendMessage.
3. A session-start check that warns when a session's checkout or hooks differ from what is expected.
4. A live-exercise runner.
5. A session launcher with a time limit, a check for the expected session, and cleanup on failure.
6. A tool for recording hook-saved dispatches, including those scattered across harness worktrees.
7. A way to send a prompt to a named agent unchanged.
8. Whether the planner may use TaskStop.
9. The session log records no `deny` decision for blocked Agent calls, only `toolDenialKind`.
10. How a record entry orders a dispatch's own copy against its notes.
11. Resuming a planner session's work after it disconnects.
12. Which timestamp (birth or mtime) "created after" means for backups.
13. A written naming rule for store copies of dispatches saved outside the store.
14. A way for the planner to reach a running coder with a changed ruling.

## Decisions I made (final coder, intent -07)

- **Terminal IDs:** 2026-09-23-10, -11 and -12, continuing R4's series. R4 named only -07 to -09.
- **Notes carry an `Observed` field** for the "what happened" text that S1 requires. The checker does not forbid it, and there was no earlier note to follow.
- **Backup layout:** `claude-projects/`, `claude-1000/` and top-level `kitfix_*`. For the R2 set I compared file by file against 2026-09-23-04's manifest. All 26 files already backed up were unchanged, and `/tmp/kitfix_evidence` had no new file.
- **Guard line numbers:** I cited `dispatch_guard.py`'s actual lines rather than the dispatch's.
- **Exercise file copies:** I copied `-05.md` and `-06.md` into the store together with this dispatch's copy, before appending intent -07. H3 placed the copies after the intent.

## Flags outside scope

- The store's name `2026-09-23-07.md` collides with 01JDb's `2026-09-23-07.md`, which has different content. R4 accepted this, and intent -07 records it.
- ~/Zynergy/Claude-kit on `kit-v0.1` has two untracked files that its own checker flags. They stay until the owner decides.
