HEAD: b54482d5f1e83408d42728ea401aff44777db17e
Target subagent: coder
Type: build
Preserved: 2026-09-24T21:39:04Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Claude-kit v0.2 T2: the store-name rule, stated in coder.md and enforced by check_prompts.py

## Role
You are the coder. You carry out this dispatch and make no design decisions; a gap is a stop-and-ask. Record your work as `coder.md` and the checkers require. Run `check_record.py` and `check_prompts.py` before every commit that touches `RECORD.md`.

## Base and state
- `slayer8366/Claude-kit`: PR #5 (kit-v0.2-t1) merged at 2026-09-24T21:22:25Z; `origin/main` = `b54482d5f1e83408d42728ea401aff44777db17e`. No tags. `docs/specs/2026-09-24-kit-v0.2-tasks.md` line 9 defines T2 (depends on T1).
- On `origin/main`: RECORD.md's last entry is `2026-09-24-07` (terminal, closes intent `2026-09-24-06`); no intent is open. The store holds 11 files, `prompts/preserved/2026-09-23-01.md` to `-07.md` and `2026-09-24-01.md` to `-04.md`, all claimed.
- `~/Zynergy/Claude-kit-fixes`: on `kit-v0.2-t1` at `8d4ba43` (merged), clean. Work here on a new branch `kit-v0.2-t2` from `origin/main`.
- `~/Zynergy/Claude-kit` (main checkout): on `main` at `b54482d`. Not touched.
- The shared counter `~/Zynergy/Claude-kit/.git/claude-kit/dispatch-seq` held `2026-09-24 05` after the planner's pulse (below). The T1 hook created it.
- The planner's harness worktree `~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01PuSBQqnBC6j4SPFCgt8yv2` (call it W; HEAD `b54482d`, T1 hook) holds the untracked hook-saved files this dispatch records:
  - P, the planner's pulse: `prompts/preserved/2026-09-24-05.md`, with header HEAD `b54482d5f1e83408d42728ea401aff44777db17e`, target pulse, type pulse, Preserved `2026-09-24T21:32:57Z`. It is the first dispatch saved by the T1 hook. An earlier attempt at the same pulse was blocked for missing sections, before anything was saved, so it left no file.
  - D, this dispatch. The planner expects it at `prompts/preserved/2026-09-24-06.md` (target coder, type build), or at `2026-09-25-01.md` if it was saved after midnight UTC. Identify it by header and content. Its store copy keeps the name the hook gave it, whichever that is.
- The other harness worktrees hold untracked files saved by pre-T1 hooks: 01Ftd 1, 01JDb 8, 01Vss 11, 01TVux 2 (the 01TVux two are already in the store as `2026-09-24-03.md` and `-04.md`). They are left to T3: do not copy or record them.
Check all of this first; if anything differs, stop.

## Scope boundary
Code: `check_prompts.py` (the new naming check, its module docstring, and new render-check cases) and `.claude/agents/coder.md` (the rule, worded exactly as below). Record: store copies of P and D, one dispatch-note, one intent, one terminal. Nothing else changes: no hook, kit.json, check_record.py, test, install or release tooling, CI, or README change. The task list is not amended. Harness worktrees may be read and copied from, never changed. No merge, no tag. Delete nothing.

## Closed decisions
Owner rulings. They are ruled on; do not re-ask them.
- In the previous planner log `/home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-01TVuxEhXQ9gEiCqR5vw4nGZ/a4155372-2845-559f-b1d8-b1e1db6614c8.jsonl`: the planner's options message is line 228 (2026-09-24T21:23:28Z; T2 option A: "The store copy keeps the name the hook gave it ... Dispatches saved by a pre-T1 hook get the store's next free name ..., and the intent records where the file came from"). The owner's answer is line 231 (21:30:18Z): "I'll take your recommendations".
- In this planner's session log `/home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-01PuSBQqnBC6j4SPFCgt8yv2/a5d14103-ed93-50a1-a091-899109c2144d.jsonl`, the owner answered two questions that amend option A's enforcement. Find the AskUserQuestion result there and quote it with line numbers in the intent:
  1. "Date rule for all": the ancestor check on 802a17f is dropped. CI checks out only one commit, and adopters' headers name their own commits, so it cannot run in either place. The date rule below applies to every checked file, whichever hook saved it.
  2. "Cutoff by name": files named before `2026-09-24-05` are exempt, compared as (date, number), not as text.

What to build:
1. In `check_prompts.py`, add a naming check to `check_binding`, reported as errors like the others. For each file under `prompts/preserved/` (not `recovered/`):
   - The name must match `YYYY-MM-DD-NN.md`: `^(\d{4}-\d{2}-\d{2})-(\d{2,})\.md$`. If it does not, that is an error naming the file.
   - If (date, int(NN)) < ("2026-09-24", 5), the file is exempt. Put the cutoff in one module-level constant, with a comment saying it exempts the files in the store before the rule began.
   - Otherwise, take the header's `Preserved:` line, which is a line before `--- verbatim prompt follows ---` starting with `Preserved: `. If the header has none, that is an error. If its first 10 characters (the UTC date) differ from the name's date, that is an error naming the file, both dates and the rule.
   - `counts` and the existing checks are unchanged.
2. Module docstring: add a paragraph stating the rule and the cutoff. Cite it as "Claude-kit v0.2 addition". No RECORD.md path in this file: it is released, and the denylist forbids that.
3. Render-check cases, added after p5. Each fixture file is written with a hook-style header (HEAD, Target subagent, Type, Preserved, delimiter) and claimed by a valid dispatch-note, so the naming rule is the only thing under test. Update `total`.
   - p6, the wrongly named copy: `preserved/2026-09-25-01.md` whose header says `Preserved: 2026-09-24T10:00:00Z` must be reported.
   - p7: `preserved/2026-09-25-01.md` with `Preserved: 2026-09-25T10:00:00Z` passes. `preserved/2026-09-23-07.md` with `Preserved: 2026-09-24T05:44:30Z` (pre-cutoff, mismatched, like the real one) passes.
   - p8: `preserved/2026-09-25-02.md` with no `Preserved:` line in its header must be reported.
   p1-p5 use `2026-01-01-05.md`, which is before the cutoff, so their headerless fixtures stay valid.
4. `.claude/agents/coder.md`, under "The record": add this item after item 4, exactly as written:
   "5. **Store names.** A dispatch's copy in `prompts/preserved/` keeps the name the dispatch hook gave it. A dispatch saved by a hook that numbered within its own worktree (before the shared dispatch counter) takes the store's next free number within the date of its header's `Preserved:` line, and the intent records where it came from: the original path, size and sha256. If the hook's name is already taken in the store by a different file, stop and ask. `check_prompts.py` checks that each name's date equals its header's `Preserved:` date."

Record names: P's store copy keeps `2026-09-24-05.md`, recorded by dispatch-note `2026-09-24-08` (Type pulse, Outcome answered). For its Report, cite this planner's session log above, meaning the pulse's Agent call and its hand-back; find their line numbers. D's store copy keeps its hook name, recorded by intent `2026-09-24-09` and terminal `2026-09-24-10`. Use these IDs even if the UTC date has changed by the time you write them. If check_record.py rejects them, stop. Make each copy byte for byte, header included, and confirm with `cmp` and sha256. If a hook name is already taken in this store by a different file, stop and ask; do not rename.

## Prediction
Planner prediction, stated and not withheld:
- Tests-only commit, with p6-p8 added and no naming check yet: `check_prompts.py --render-check` reports 2 of 8 failing, p6 and p8, each because the file was accepted; p7 passes. Hook tests stay at 86 OK; install and release tests stay at 18 OK. If test_install runs the vendored `check_prompts.py --render-check` in an adopter, it fails too at that commit; say whether it does.
- After the fix: 8/8. On the real store, check_prompts.py passes with 13 files. Only P and D are checked; the other 11 are exempt. check_record.py render check stays at 23/23. release_check.py passes with 18 release files and 18 patterns.
- Revert check: restore `check_binding` from origin/main against the committed render-check. p6 and p8 fail again with the same messages.
Write your own mechanism prediction in the intent.

## Finish line and abort conditions
Finish: the sweep, the intent, the tests-only commit, the fix and the terminal are all pushed on `kit-v0.2-t2`, and a PR `kit-v0.2-t2` → `main` is open with CI green on its final commit. Then STOP and report with the PR URL, the before and after counts, the failing-first output and the revert-check output.
Abort on:
- any Base-and-state mismatch;
- a needed change outside the scope boundary;
- a new case failing for a reason other than the predicted one;
- two failed fixes on one symptom;
- a store-name collision;
- an owner message in this planner's session log after the AskUserQuestion answer that bears on this dispatch.

Steps, one commit each, each pushed:
1. Verify. Branch `kit-v0.2-t2` from `origin/main`.
2. Sweep: P's store copy and dispatch-note `2026-09-24-08`.
3. D's store copy and intent `2026-09-24-09`.
4. Tests only (p6-p8); show them failing as predicted.
5. The fix and the coder.md rule; checks pass; then the revert check (do not commit the revert).
6. Terminal `2026-09-24-10`; all checks; PR; CI green.
7. STOP and report.

## Checks
Every commit must pass these, in `~/Zynergy/Claude-kit-fixes`; at step 4, p6 and p8 are expected to fail:
- `python3 -m unittest discover -s .claude/hooks/tests -v`
- `python3 check_record.py --render-check`
- `python3 check_prompts.py --render-check`
- `python3 check_record.py` and `python3 check_prompts.py`
- `python3 -m unittest discover -s tests -v`
- `python3 release_check.py`
Write commit messages and record entries to files under `/tmp/kitv02_t2_*` first, with the Write tool, and run commit and push as separate commands. history_guard over-blocks commands that combine a push with text it cannot parse; that is known and is T12. Record any block as a deviation.

## Out of scope
- T3-T16, and T12 in particular.
- The untracked files in other harness worktrees.
- The main checkout.
- Amending the task list.
- Merging, tags, Forager.

## Device items
None gate this dispatch. After the owner merges, they pull `main` in `~/Zynergy/Claude-kit`.