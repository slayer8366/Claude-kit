HEAD: d59280fceb136d04d2165d3851bdd2466a04ace6
Target subagent: coder
Type: build
Preserved: 2026-09-25T04:28:31Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for a second small follow-up to Claude-kit v0.2 T13, on the same branch before it merges. The lock-path check added in 807bbbf tests paths as spelled. The previous coder found, by reading the code, that `./RECORD.md` and `prompts//x` pass it and still resolve to owned files. A path under a symlinked directory can also land outside the adopter. Make the check test where a path lands, not how it is spelled. Follow `.claude/agents/coder.md` at your base.

# Base and state
Premises to verify:
- **Branches.** origin/kit-v0.2-t13 is at dc0ad41 (terminal 2026-09-25-21). PR #14 is open and unmerged. origin/main is 3a5a13d. Nothing is tagged. Work on `kit-v0.2-t13` in ~/Zynergy/Claude-kit-fixes.
- **Record.** The last entry is 2026-09-25-21; no intent is open.
- **Planner worktree W.** This dispatch is expected in its untracked store as `2026-09-25-14.md`. If the hook gave another name, that name governs. No sweep is expected.
- **Code at dc0ad41:**
  - `lock_path_problem` is called in `read_lock`.
  - `ADOPTER_OWNED` is at install.py:56.
  - `adopter_owned` is applied to the key as written.
  - Drive letters are matched with `re.match(r"[A-Za-z]:", path)`.
- **Planner log.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl.

# Scope boundary
Files:
- **`install.py`:** `lock_path_problem`, or how it is called, and its docstring sentence.
- **`tests/test_install.py`:** test u10.

Record:
- this dispatch's copy and intent 2026-09-25-22
- the tests-only commit, the fix, and terminal 2026-09-25-23

Nothing else.

# Closed decisions
- The owner's "Fix before merge" answers the planner's option 1: "Fix it on `kit-v0.2-t13` before merging #14 (recommended). This is the check the last dispatch was meant to be."
- The rule, from the planner's message before that: "normalise the path before the owned-path check; require the resolved path, symlinks followed, to stay inside the adopter's folder".

Cite both by line and timestamp.

The planner's rules, for each key `p` of the old lock's `files` map:
1. Keep the existing checks for an absolute path, a drive-letter start, and a `..` part.
2. Let `n = posixpath.normpath(p)`. Reject if `n` is `.` or empty, or if `n` is adopter-owned by the existing test (an owned path, or under `prompts/`).
3. Let `r = (target / p).resolve()`. `resolve()` is non-strict by default on 3.8. Reject unless `r` lies inside `target.resolve()`. Use `relative_to` in a try block, not `is_relative_to`, which is 3.9+. Reject too if `r`'s path relative to `target.resolve()` is adopter-owned.
4. Each reason names the key and says why ("owned after normalising", "resolves outside the target", …). The stop behaviour is unchanged: nothing written or removed, the lock and each path named, exit 1.

# Prediction
- **Tests-only commit.** u10, with subTests. Each path is in the old lock with a matching sha256 and dropped by the new tag. Each subTest builds its own kit and adopter under `self.root`.
  - (a) `./RECORD.md`
  - (b) `prompts//x.md`, a file under the adopter's `prompts/`
  - (c) `ext/outside.txt`, where the adopter's `ext` is a symlink to a sibling directory of the adopter inside the test's temporary root

  Before the fix, each fails "0 != 1": the upgrade succeeds and removes the file (for (c), the file outside the adopter). Assert the exit code first, then that the file still exists with its bytes unchanged. tests/: 39 run, 3 failure records. Hook tests: 106.
- **After the fix:** 39 OK, and u9 still passes. 106 OK. Render checks 30/30 and 10/10, both checkers PASS, release_check PASS with 19 files.
- **Revert check:** install.py from dc0ad41 gives the same 3 failures.

**Safety:** every file and symlink a subTest creates lies under the test's temporary root, and each target is asserted to be under that root before the upgrade runs.

Your mechanism prediction goes in the intent.

# Finish line and abort conditions
Finish line: the copy and intent, the tests-only commit, the fix and the terminal, all pushed on kit-v0.2-t13. PR #14 then shows CI green on the new final commit. Don't open a new PR. No merge, no tag.

Abort and report on any of these:
- any Base-and-state mismatch other than the dispatch's name or a line drift of the kind the last coder recorded
- a failure for any reason other than the predicted one, including if symlinks can't be created in the test environment
- two failed fixes on one symptom
- a needed change outside scope

Owner messages after this dispatch's Agent call: an abort only if one tells you to do or not do something in this scope, or changes a decision above. Record every other message in the terminal. A planner message follows coder.md item 6.

# Checks
- Failing-first run and revert check.
- Counts before and after. Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
- Run tests in their own Bash call.

# Out of scope
- Paths in the new release (the kit's own release.json).
- check_kit.py, `docs/standing-rulings.md`, T17, T5.
- Merge, tag, deleting anything outside the test temp directories.

# Device items
None.