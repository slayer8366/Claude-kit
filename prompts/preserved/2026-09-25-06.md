HEAD: cda554b0a77bca36469193987ee5925188ebc2c5
Target subagent: coder
Type: build
Preserved: 2026-09-25T01:26:21Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit v0.2 task T9, continuing D5. D5 is the first T9 dispatch, saved untracked in the planner worktree as `prompts/preserved/2026-09-25-05.md` (9695 bytes, sha256 60f69cdd8f6e519fe721c7329554af22ae9101b976799e2dff1e8b18a4b5e3da). Its coder stopped before any change, correctly, on the owner's line 330. It did create an empty local branch `kit-v0.2-t9` from origin/main bbdbbfd in ~/Zynergy/Claude-kit-fixes, not pushed.

Read D5 in full. Its Base and state, Scope boundary, Closed decisions, Prediction, Finish line, Checks and Out of scope apply, except as amended below. Follow `.claude/agents/coder.md`.

# Base and state
Premises to verify. D5's coder verified them at about 01:25Z.
- **Branches.** origin/main is bbdbbfd. Nothing is tagged. Local `kit-v0.2-t9` is at bbdbbfd with no commits and a clean tree; its upstream is set to origin/main, so push with `-u origin kit-v0.2-t9`.
- **Record.** The last entry is 2026-09-25-06. No intent is open.
- **P1 and P2 are proved** (D5 coder's hand-back):
  - `transcript_path` is a common hook input field (Claude Code hooks docs). SendMessage's target is in `tool_input["to"]` (planner log line 141).
  - The agent ID is in the top-level `toolUseResult.agentId` of the JSONL line holding the Agent `tool_result`, whose `tool_use_id` matches the Agent `tool_use`. Sync example: line 200 → 205, `a0112ed1f48579005`. Async example: line 268 → 272, `a1e0888e2666dcce1`.
  - Re-check both examples with one read each. Stop if either has changed.
- **Planner worktree.** D5 is not in the store and stays uncopied: cite it by path, size, sha256 and Preserved line, the R5 practice used for D2. This dispatch, D6, is expected as `2026-09-25-06.md` (counter `2026-09-25 06`). If the hook gave another name, that name governs.
- **Planner log.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl. D5's Agent call is line 314.

# Scope boundary
As in D5, with these changes:
- **Record:**
  - D6's store copy and an intent claiming D6 (expected 2026-09-25-07). The intent cites D5 as stopped before any change, and gives the stop's reason.
  - A terminal (expected -08).
  - D5 opened no intent, so it gets no terminal.
- **Tests:** eight new tests: D5's t1-t7, plus (t8) a missing or empty `to` is denied. For t5's "unreadable" case, use a directory path, and a subTest with `transcript_path` absent from the payload.

# Closed decisions
Everything in D5, with the approval ruling corrected by the owner after D5's Agent call:
- **Line 324** (01:23:48.052Z): "There is an approval step for a message, but it's already established before the message is sent. So it does exist, but not in the order you first proposed."
- **Line 330** (01:24:51.926Z): "Almost. The message is approved before being sent, upon the operator constructing it. Or the planner offers first. Never on a whim."

So the hook still asks nothing when the planner sends a message to an allowed target (return None). The approval is the operator's, given before the message exists: either the operator writes or requests it, or the planner offers it and the operator agrees. The hook cannot check this. The docstring states it.

Docstring text. Add this to role_guard.py's module docstring after the planner bullet, verbatim; wrapping to the file's width is allowed:

"SendMessage (planner only): the target must be an agent this session started, that is, an ID that `toolUseResult.agentId` gives on the transcript line holding the result of one of the session's own Agent calls (matched by tool_use_id). Any other target is denied, and so is every target when the transcript cannot be read. The hook asks nothing: each message is approved by the operator before it is sent, because the operator writes or requests it, or the planner offers it and the operator agrees. No message is sent on the planner's own initiative. The hook cannot check that approval; coder.md item 6 makes a coder stop on a message that widens its scope without quoting an owner ruling. The transcript is written asynchronously, so a message sent just after an Agent result can be denied until the result is on disk; send it again."

Planner rulings on D5 coder's proposed choices:
- **Accepted:**
  - Take the ID from `toolUseResult.agentId`, and only when that line holds exactly one `tool_result` block.
  - Return None for an allowed target.
  - Apply the rule wherever the role is planner, which is the existing code path.
  - The target's type is not used.
  - t8, and the t5 subTests as above.
- **Also:** read the transcript as UTF-8, skip lines that fail to parse as JSON, and compare `to.strip()` for exact equality.

coder.md item 6's wording is unchanged from D5.

Owner messages. After this dispatch's Agent call, an owner message is an abort only if it tells you to do or not do something in T9's scope, or changes a decision above. Record any other message in the terminal by line and timestamp, and continue. If unsure, stop. The owner's lines after 330, up to this dispatch's Agent call, are ruled on here; cite them in the intent. They agree with the text above.

# Prediction
- **Tests-only commit:**
  - t1 and t7 fail with the tool-not-allowed deny.
  - t2-t5 and t8 fail on the reason assertion, because today's reason is "may not use SendMessage" and doesn't name the target rule.
  - t6 passes.
  - Hook tests: 91 + 8 = 99 run.
  - test_install's vendored-hook-tests case fails with the same failures, possibly cut off at 3000 characters.
- **After the fix:**
  - 99 hook tests OK
  - 29 tests/ OK
  - render checks 23/23 and 8/8
  - both checkers PASS
  - release_check.py PASS with 19 files
  - find_dispatches.py: `refused 2` (D2 and D5), stop 0, exit 0
- **Revert check:** the same failures with the same messages.

Your mechanism prediction goes in the intent.

# Finish line and abort conditions
Finish line, pushed on kit-v0.2-t9:
1. D6's store copy and the intent
2. the tests-only commit
3. the fix (role_guard with its docstring, coder.md item 6)
4. the terminal

Then PR kit-v0.2-t9 -> main open, with CI green on its final commit. No merge, no tag.

Aborts: D5's, with the owner-message clause as above.

# Checks
D5's Checks 1-4. For Check 4's live run, use target `a1e0888e2666dcce1` (allowed), then `main` (denied), against the real planner log.

# Out of scope
As in D5. Also: copying D5 or D2, and D5 coder's scratch files in /tmp.

# Device items
None.