HEAD: d59280fceb136d04d2165d3851bdd2466a04ace6
Target subagent: coder
Type: build
Preserved: 2026-09-25T02:10:21Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit v0.2 task T11 (`docs/specs/2026-09-24-kit-v0.2-tasks.md` line 18): role_guard allows the planner TaskStop, with tests. It follows the same pattern as T9's SendMessage. Follow `.claude/agents/coder.md`.

# Base and state
Premises to verify:
- **main.** origin/main is 501c7c37e4e7d0e2fa237a09ce05226fb677e63a (PR #10 merge, 02:09:12Z). Nothing is tagged. The record's last entry is 2026-09-25-10; no intent is open. Create `kit-v0.2-t11` from origin/main in ~/Zynergy/Claude-kit-fixes.
- **Planner worktree W.** ~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB is at d59280f (T9 hooks). Its untracked store files:
  - D2 `2026-09-25-02.md` and D5 `-05.md`: cited, not copied (R5).
  - `-07.md`: now on main.
  - This dispatch, expected as `2026-09-25-08.md` (counter `2026-09-25 08`). If the hook gave another name, that name governs.

  `python3 find_dispatches.py` from your branch should list no `stop` items.
- **Planner log.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl.
- **Code.**
  - role_guard.py on main: `PLANNER_TOOLS` (lines 52-54) lacks TaskStop.
  - `own_agent_ids(transcript_path)` and `check_send_message(payload)` hold T9's rule; `SEND_RULE` is its deny text.
  - TaskStop's input schema, as the planner's tool list shows it: `task_id` (string), and `shell_id` ("Deprecated: use task_id instead").
- **(P1), to prove from the planner log before any code; stop if false.** A background agent's task ID equals its agent ID. Show one `<task-notification>` line whose `<task-id>` equals the `toolUseResult.agentId` of the matching Agent result, for example the worktree-update coder: the Agent call at line 384 and its result at line 386. Also show where TaskStop's `tool_input` fields appear, if any TaskStop call exists in the log; if none, say so and rely on the schema above.

# Scope boundary
Files:
- **`.claude/hooks/role_guard.py`:**
  - TaskStop added to the planner allowlist
  - a target check for the planner's TaskStop
  - T9's check refactored only as far as needed to share `own_agent_ids`
  - the docstring paragraph below
- **`.claude/hooks/tests/test_role_guard.py`:** the new tests.

Record:
- this dispatch's store copy
- intent 2026-09-25-11
- the tests-only commit, the fix, and terminal 2026-09-25-12

No change to coder.md, README (it doesn't list the tools), other hooks, guardlib, kit.json, checkers, install or CI. T9's behaviour and its tests must stay unchanged.

# Closed decisions
Owner rulings in the planner log:
- **Targets.** The T9 AskUserQuestion, whose question read "T9: which targets may the planner SendMessage? (The same answer would carry to T11's TaskStop.)", was answered "Only its own agents (Recommended)".
- **Approval.** Lines 324 and 330, on messages: "The message is approved before being sent, upon the operator constructing it. Or the planner offers first. Never on a whim." The planner applies the same rule to TaskStop, and said so to the owner just before this dispatch. That is the planner's extension, stated openly.
- **This task.** The owner's "Merged" (PR #10) followed the planner's proposal to do T11 next.

Cite these by line and timestamp.

The planner's rules:
1. A planner TaskStop is allowed only when `tool_input["task_id"]`, stripped, is in `own_agent_ids(transcript_path)`, compared exactly. Deny with a reason naming the rule and the target when:
   - `shell_id` is present and non-empty, whatever `task_id` holds (the planner has no background shells)
   - `task_id` is missing or blank
   - the transcript is missing or unreadable
   - the target is not one of the IDs

   The deny text starts "role_guard: the planner may TaskStop only an agent this session started". The pulse still may not use TaskStop. The coder stays unrestricted.
2. **Docstring.** Add after T9's SendMessage paragraph, verbatim (rewrapping is allowed): "TaskStop (planner only): the same target rule as SendMessage, applied to task_id; shell_id is always denied, since the planner starts no background shells. A stop is approved by the operator before it is made, as a message is: the operator asks for it, or the planner offers it and the operator agrees. A stopped agent writes no terminal of its own; its open intent is closed by the next dispatch's record, citing the stop by planner-log line."

# Prediction
- **Tests-only commit.** New tests:
  - (s1) own agent ID allowed
  - (s2) unknown ID denied
  - (s3) an ID only in a Read result's text denied
  - (s4) missing, empty and blank `task_id` denied (subTests)
  - (s5) `shell_id` set, with and without a valid `task_id`, denied (subTests)
  - (s6) missing and unreadable transcript denied (subTests)
  - (s7) a pulse's TaskStop denied

  s1 fails with the tool-not-allowed deny. s2-s6 fail on the reason assertion. s7 passes. Hook tests: 99 + 7 = 106 run. test_install's vendored-tests case fails at that commit, showing the same failures, possibly cut off.
- **After the fix:**
  - 106 hook tests OK, including T9's unchanged tests
  - 29 tests/ OK
  - render checks 23/23 and 8/8, both checkers PASS
  - release_check PASS with 19 files
- **Revert check:** role_guard.py from origin/main against the committed tests gives the same failures.

Your mechanism prediction goes in the intent. It includes P1 as proved.

# Finish line and abort conditions
Finish line, pushed on kit-v0.2-t11:
1. the store copy and the intent
2. the tests-only commit
3. the fix
4. the terminal

Then PR kit-v0.2-t11 -> main open, with CI green on its final commit. No merge, no tag.

Abort and report on any of these:
- any Base-and-state mismatch other than the dispatch's name
- P1 false
- a needed change outside scope
- a new test failing first for another reason
- any T9 test changing result
- two failed fixes on one symptom
- a denylist hit

Owner messages after this dispatch's Agent call: an abort only if one tells you to do or not do something in T11's scope, or changes a decision above. That includes a different approval rule for stops. Record every other message in the terminal by line and timestamp, and continue. If unsure, stop.

A message from the planner (SendMessage) follows coder.md item 6.

# Checks
1. Failing-first run and revert check, as predicted.
2. All counts before and after. Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
3. Run tests in their own Bash call, never with a commit or push.
4. **Live check, read-only:** run the new role_guard as a script with a TaskStop payload against the real planner log.
   - `task_id` = the finished worktree-update coder's ID from P1: expect None.
   - `task_id` = `main`: expect a deny.
   - `shell_id` = `x`: expect a deny.
   - Stop nothing.

# Out of scope
T10, T4, the T3 backlog, history_guard items, test_install's stderr tail, updating any harness worktree, merge, tag, deleting anything.

# Device items
None.