HEAD: 1599a53975b8f980035c594d52f7d125de768370
Target subagent: coder
Type: build
Preserved: 2026-09-25T17:15:53Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are a coder updating the main checkout. Move its three untracked store copies aside, then fast-forward it to origin/main. Follow `.claude/agents/coder.md` in ~/Zynergy/Claude-kit, except for the recording. That is handed to the next dispatch's sweep, as with 2026-09-25-28 (see Scope boundary).

# Base and state
Premises to verify (read-only) first:
- C = ~/Zynergy/Claude-kit, on main at 1599a53975b8f980035c594d52f7d125de768370, with no tracked changes.
- `git ls-remote origin main` = db742d2bd6f0926d908ad5919f9fc28806a68547, the PR #25 merge.
- C's untracked files:
  - `.claude/worktrees/`: leave it alone
  - `prompts/preserved/2026-09-25-27.md`, `-28.md` and `-29.md`: the three to move
  - this dispatch's copy, saved by the hook (expected `-30.md`; the hook's name governs). It is not on origin/main, so it stays where it is.
- K1: for each of the three, `sha256sum` of C's file equals `git -C C show origin/main:prompts/preserved/<name> | sha256sum`, after `git -C C fetch origin`. The T14-16 coder gave these sha256s:
  - -27: 5a3538b2fe339ffe20f9385085d7f018bd824085cb8effe0f85b6d38e69e09b9
  - -28: d1bbaa48a3433bc4adc359f7b73ae40b410914086a75abc348a1b7b8548c59d9
  - -29: see its store copy on origin/main

# Scope boundary
1. `git -C C fetch origin`, then K1.
2. K2: `mv -n` the three into a new ~/forager-backups/<next free UTC-date folder>/prompts/preserved/, using the layout of backup 2026-09-25-02. Add a MANIFEST.sha256 over them that passes `sha256sum -c`, and append one INDEX.md line naming the folder, C and the three files.
3. K3: `git -C C status --porcelain --untracked-files=all` must then show only `.claude/worktrees/…` and this dispatch's copy.
4. `git -C ~/Zynergy/Claude-kit pull --ff-only origin main`, as one Bash call. Report the output verbatim.
5. After: C's HEAD equals db742d2. The three files are tracked (`git ls-files --error-unmatch`) at the K1 hashes. Report `git -C C diff 1599a53 db742d2 --stat -- .claude/`, and whether `.claude/settings.json` changed.

Write nothing else. No RECORD.md entry, no commit, no push, no branch, no merge, and nothing deleted. The next dispatch sweeps this dispatch's copy, so report its name, size and sha256.

# Closed decisions
- The owner, this session, verbatim: "Have a coder move the three untracked copies in ~/Zynergy/Claude-kit aside and pull main up to db742d2".
- Precedent for moving store copies aside: 2026-09-25-28/-29 (K1-K5, backup 2026-09-25-02).

# Merge
not authorised

# Prediction
- K1: all three match origin/main.
- K2: the manifest shows 3 OK, and INDEX.md grows by one line.
- K3: only `.claude/worktrees/` and this dispatch's copy remain untracked.
- The pull prints "Updating 1599a53..db742d2" and "Fast-forward", exits 0, and no hook denies it.
- The `.claude/` diff is empty or small, since #25 added BYPASSES.md, test_bypasses.py and docstring lines. settings.json is unchanged.

# Finish line and abort conditions
**Finish line:** K1-K3 done, the pull fast-forwarded C to db742d2, and the after-checks reported with the store copy's details.

Abort and report on any of these:
- C not on main, or with tracked changes
- origin/main anything other than db742d2
- any of the three differing from origin/main (don't move it)
- a manifest mismatch
- untracked files beyond those listed
- the pull refusing (verbatim, no workaround)
- a hook denying something (quote it)
- an owner message after this Agent call that tells you to do or not do something in this scope

# Checks
- K1 hashes on both sides.
- `sha256sum -c` output.
- The INDEX line.
- Status before and after.
- The pull output.
- HEAD after.
- The `.claude/` diff stat.
- The store copy's name, size and sha256.

# Out of scope
- The record, PRs and tags.
- `.claude/worktrees/` and every worktree.
- Other checkouts.
- Deleting anything.

# Device items
None.