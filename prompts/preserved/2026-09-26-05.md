HEAD: 9d077470c57249c450e6e3de59651bd727e05f37
Target subagent: pulse
Type: pulse
Preserved: 2026-09-26T02:44:35Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** pulse

# Role
You are the pulse for Claude-kit. Read-only: Read, Grep, Glob, and Bash limited to git and gh reads. Change nothing, run no tests, write no files. Answer with file:line evidence; where you cannot verify, say "unverified".

# Base and state
- The kit: origin/pre-main 9d077470c57249c450e6e3de59651bd727e05f37 in the clone at /home/zynergy-labs/Zynergy/Claude-kit (your cwd is a worktree of it, at 9d07747 or later). The kit's release set is `release.json`; its installer is `install.py`; its README's "Install and drift" section states the first-install rules.
- The adopter: Forager, clone /home/zynergy-labs/Zynergy/Forager, remote origin git@github.com:slayer8366/Forager.git, origin/main af12a69 (2026-09-23T00:29:44-07:00). Its main checkout is on branch `claude/new-session-vto65i` at e0b74af; it has many worktrees under `.claude/worktrees/`. It tracks the pre-kit hooks under `.claude/hooks/` (last changed 0eba963, 2026-09-22), `.claude/settings.json`, `.claude/agents/`, `RECORD.md`, `check_record.py`, `check_prompts.py`, `prompts/preserved/2026-09-22-01.md` to `2026-09-23-20.md`, `CLAUDE.md`. No `.claude/kit.json`. Its `.gitignore` ignores `.claude/*` and re-includes only `settings.json`, `agents/` and `hooks/`.
- Purpose: the owner wants Forager on the kit's next release. The questions below are the premises of that install.

# Rules
- Read-only. No test runs, no writes, no `git fetch`, no checkout.
- Cite `path:line` at a named commit (use `git -C /home/zynergy-labs/Zynergy/Forager show origin/main:<path>` or `git grep` against `origin/main` for Forager; the worktree files for the kit). Quote short excerpts where wording matters.
- Do not print long files. Do not open anything under ~/.claude/projects.
- Under 300 lines.

# Questions
1. **Forager's hardcoded config.** In Forager's `.claude/hooks/*.py` at origin/main: the Android package name device_guard protects (constant name, value, line); the protected branch list history_guard uses; the dispatchable agents and type-to-target map in dispatch_guard; any backup directory; any other value the kit's `templates/kit.json` keys would need (list the template's keys from the kit worktree and give Forager's value for each, or "none").
2. **Hook drift.** For each of Forager's hook files and `harness.py`, whether it is byte-identical to any commit of the kit's copy (compare sha256 of `git show origin/main:<path>` in Forager against `git log --format=%H -- <path>` blobs in the kit, or say "not compared"). Which kit RECORD.md entry describes the extraction (2026-09-23-01) and at which Forager commit it was taken.
3. **settings.json.** Show the diff between Forager's `.claude/settings.json` at origin/main and the kit's at 9d07747 (a `diff` you can express with `git diff --no-index` is a write of nothing; if role_guard refuses it, describe the difference by reading both).
4. **The installer's first-install path.** In the kit's `install.py`: the lines that stop when `.claude/settings.json` exists and differs; the lines that write `.claude/kit.json` from the template only when absent; where `.claude/kit.lock` is written; whether any other existing adopter file (RECORD.md, CLAUDE.md, prompts/, `.gitignore`) is ever written or refused. Also whether install.py needs the tag reachable in the kit clone it runs from (the line that reads files from the tag).
5. **.gitignore effect.** Read Forager's `.gitignore` at origin/main: confirm that `.claude/kit.json` and `.claude/kit.lock` would be ignored (cite the `.claude/*` line and the re-includes). Any other kit-written path that Forager ignores (the root scripts, `__pycache__`)?
6. **Root collisions.** List Forager's root-level tracked files at origin/main. Which of the kit's vendored root files (`check_record.py`, `check_prompts.py`, `check_kit.py`, `find_dispatches.py`, `update_worktree.py`, `session_agents.py`, `launch_session.py`, `run_exercise.py`) already exist there, and for the two checkers, whether Forager's copy differs from the kit's current one (sha256 compare).
7. **Record compatibility.** In Forager's `RECORD.md` at origin/main: the number of entries (count of `**Kind:**` lines), the set of `**Kind:**` values, the set of terminal `**Outcome:**` values, and the set of dispatch-note `**Outcome:**` values. The kit's check_record.py at 9d07747 accepts terminal outcomes `completed`, `partial`, `superseded`, `abandoned` and note outcomes `answered`, `declined`, `exercise`, `stopped`, `merged`; name any Forager value outside these. Whether Forager's RECORD.md history has a merge commit touching it (`git log --merges -- RECORD.md`), which the kit's history walk refuses.
8. **Store names.** Forager's `prompts/preserved/` file names versus their `Preserved:` header dates (the kit's check_prompts rule exempts names before 2026-09-24-05): any file whose name date differs from its header date; any header without the hook's `Preserved:` line.
9. **CLAUDE.md.** Lines in Forager's CLAUDE.md at origin/main that describe the hooks, the record, dispatches, merges, or branch names, which an install would make stale or contradict (cite line and excerpt).
10. **Forager's GitHub state.** `gh api repos/slayer8366/Forager/branches/main --jq '{protected: .protected}'`; `gh api repos/slayer8366/Forager --jq '{private: .private, default_branch: .default_branch}'`; open PRs touching `.claude/` (`gh pr list --repo slayer8366/Forager --state open --json number,title,headRefName,files` if the `files` field is available; else the titles).
11. **Live Forager sessions.** `git -C /home/zynergy-labs/Zynergy/Forager worktree list` and, for the locked worktree `bridge-cse_013QR4ELV3wyYrUUyVCEmDwt`, `git -C <that path> log -1 --format=%h%x20%cI%x20%s` and `git -C <that path> status --short` (counts only). Which Forager checkout, if any, is on `main`.
12. **The kit's own install test.** In the kit's `tests/test_install.py`: the test that installs into a throwaway adopter with an existing differing settings.json (name, lines) and the one for a first install writing the template kit.json (name, lines), so a real install can be predicted from them.