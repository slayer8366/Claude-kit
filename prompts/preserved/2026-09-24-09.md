HEAD: cda554b0a77bca36469193987ee5925188ebc2c5
Target subagent: coder
Type: build
Preserved: 2026-09-24T22:39:52Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit v0.2 task T3 (`docs/specs/2026-09-24-kit-v0.2-tasks.md` line 10): a read-only recording tool that lists every untracked hook-saved dispatch across all worktrees of the clone, with size and sha256, and emits copy commands and citation lines, tested on a fixture with three worktrees. You build the tool and its tests only. The backlog of hook-saved files is recorded in a later dispatch, not this one. Follow `.claude/agents/coder.md`, including items 1-5 under "The record".

# Base and state
Premises to verify before relying on them:
- origin/main is cda554b0a77bca36469193987ee5925188ebc2c5 (PR #7 merge). Nothing is tagged. Work in ~/Zynergy/Claude-kit-fixes, which is on kit-v0.2-t12 at 6d0a251 and clean. Create branch `kit-v0.2-t3` from origin/main cda554b.
- RECORD.md's last entry on main is 2026-09-24-12 (terminal, closes -11). No intent is open. The store holds 14 files, 2026-09-23-01 to -07 and 2026-09-24-01 to -07, all claimed.
- The shared counter ~/Zynergy/Claude-kit/.git/claude-kit/dispatch-seq read `2026-09-24 08` after the planner's pulse. This dispatch should take 09.
- The planner's worktree is ~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB (HEAD cda554b, T1+T2+T12 hooks). Its untracked `prompts/preserved/` should hold:
  - P = `2026-09-24-08.md`: the planner's pulse. Header "Preserved: 2026-09-24T22:33:29Z", HEAD cda554b.
  - D = this dispatch, expected as `2026-09-24-09.md`. If the hook gave it a different name, that name governs; report it. Identify D by its header and by its text after the delimiter, which must equal the prompt of this Agent call in the planner log.
- Planner log: /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl. Line 25 (22:31:06Z) is the owner's handover. Line 86 is the planner's AskUserQuestion; line 87 (22:33:06.675Z) is its answer. The pulse's Agent call is at about line 93.
- The pulse (P) reported the untracked files in the other worktrees, all under `prompts/preserved/`. The pulse could not hash them; it tested byte identity with `git diff --no-index`.
  - Already in the store (byte-identical to a file on main):
    - 01JDb `2026-09-23-03`, `-04` (same names)
    - 01PuSB `2026-09-24-05`, `-06`, `-07` (same names)
    - 01TVux `2026-09-24-02`, `-03` (= store `-03`, `-04`)
    - 01Vss `2026-09-24-09`, `-10`, `-11` (= store `2026-09-23-07`, `2026-09-24-01`, `-02`)
  - Not in the store:
    - 01Ftd `2026-09-23-02` (Preserved 22:34:08Z)
    - 01JDb `2026-09-23-02`, `-05`, `-06`, `-07` (Preserved 13:26:08Z, 22:39:08Z, 22:49:56Z, 23:21:01Z)
    - 01JDb `2026-09-24-01`, `-02`
    - 01Vss `2026-09-24-01` to `-08`
  - Header HEADs:
    - 93904076 and 45f053b8: their dispatch_guard.py has no `dispatch-seq`, so pre-T1.
    - b54482d5 and cda554b0: their dispatch_guard.py has `dispatch-seq`, so T1+.
  - The main checkout, Claude-kit-fixes, 012Hz, 018Kt and 01EBb have no untracked store files.
- Counts at cda554b, to re-run before any change:
  - 91 hook tests OK
  - render checks 23/23 (check_record) and 8/8 (check_prompts)
  - both checkers PASS
  - 18 install and release tests OK
  - release_check.py PASS: 18 release files (17 vendored + 1 template), 3902 lines, 18 patterns
- Your session runs the planner worktree's hooks (cda554b), so T12's history_guard is live.

# Scope boundary
Files:
- New `find_dispatches.py` at the repository root. Standard library only, Python 3.8+.
- New `tests/test_find_dispatches.py`.
- `release.json`: add `find_dispatches.py` to `vendored`.
- `README.md`: a row for `find_dispatches.py` in the Layout table (released: yes), and a short section on running it.

Record:
- The sweep: store copy of P under its hook name `2026-09-24-08.md`, plus a dispatch-note (Type pulse, Outcome answered, Report: planner log lines about 93 and the pulse's hand-back).
- D's store copy under its hook name, and the intent.
- One terminal.
- Entry IDs: the UTC date of the entry's timestamp, then the next number in the record for that date. Before 00:00Z that means 2026-09-24-13, -14, -15; after it, 2026-09-25-01, -02, -03. State which IDs you used.

# Closed decisions
Owner rulings (planner log line 87, answering line 86's options, quoted verbatim in the log):
1. Root, released: the tool sits at the repo root and is vendored to adopters.
2. sha256 vs store: a file counts as recorded if its sha256 equals the sha256 of a file in the store of the checkout the tool runs in. The tool scans every worktree that `git worktree list` gives.
3. Hook at header HEAD: a file is T1+ if `git show <header HEAD>:.claude/hooks/dispatch_guard.py` contains `dispatch-seq`. It is pre-T1 if that file exists at that HEAD without it. A HEAD or file that can't be resolved is a stop-and-ask item.
4. Later, read-only tool: the tool writes nothing, never touches the counter or its lock, and never proposes a name dated today. T3 does not record the backlog.

Earlier rulings still apply: coder.md item 5 (T2) and history_guard's parsing rule (T12).

Planner decisions for this dispatch. These were not put to the owner, and the planner reports them:
- **"Today".** For ruling 4, "today" is the current UTC date, the date whose numbers the hook draws from the counter. The line stored in the counter is not used, because after UTC midnight it still holds yesterday's date, which the hook no longer counts from. The tool does not read the counter at all.
- **Name and tests location.** The tool is `find_dispatches.py`. Its tests go in `tests/`, which CI already runs and which is not released, like test_install.
- **Behaviour**, run from a checkout's root as `python3 find_dispatches.py`:
  - The store is the files under the running checkout's `prompts/preserved/` that `git ls-files` lists (tracked).
  - Candidates are, in every other worktree from `git worktree list --porcelain`, the files under `prompts/preserved/` listed by `git -C <wt> ls-files --others --exclude-standard`.
    - The running checkout's own untracked store files are not scanned. Print one warning line with their count.
    - Use only commands that don't write the index or refs, with `GIT_OPTIONAL_LOCKS=0` set.
  - For each candidate, report: worktree path, file name, size, sha256, and the header's HEAD, Preserved timestamp, target and type. The header is the lines before `--- verbatim prompt follows ---`.
  - A candidate with no delimiter, or no `HEAD:` or `Preserved:` line, is a stop-and-ask item: "not hook-saved".
  - Identical candidates (same sha256) are one item that lists every source.
  - **In store:** sha256 equals a store file's. Name that file; emit no command.
  - **T1+:** the proposed name is the hook's name.
    - Stop-and-ask if that name is in the store with different content.
    - Stop-and-ask if the name's date differs from the Preserved date.
  - **Pre-T1:** the proposed name is `<Preserved date>-NN`. NN is one above the highest number for that date among the store's names and the names already proposed in this run. Pre-T1 files of one date are numbered in Preserved-timestamp order, then by path. Names are compared as integers; gaps are not filled.
  - **Refused:** a proposed name dated today (UTC) gets no command and the note "dated today; rerun after UTC midnight". Assign T1+ names before pre-T1 numbers, so a refused T1+ name still counts as taken.
  - **Unknown:** header HEAD not resolvable, or dispatch_guard.py absent at it. Stop-and-ask.
  - **Output:**
    1. One line per item with its status: `in-store`, `record`, `refused`, `stop`.
    2. A "Copy commands" section: `cp -- <src> prompts/preserved/<name>` for each `record` item, quoted with `shlex.quote`.
    3. A "Citations" section: one line per `record` item, carrying original path, size, sha256, Preserved line, HEAD, target, type and proposed store name, in the style of RECORD.md's `Dispatch source` fields.
  - **Exit status:** 1 if any `stop` item exists, else 0. `refused` items alone exit 0.

# Prediction
Planner predictions, stated here and not withheld:
- **Tests-only commit** (tests/test_find_dispatches.py, no tool): every new test fails because `find_dispatches.py` does not exist. The subprocess reports "can't open file" and exits 2, so the returncode assertions fail. Nothing fails for any other reason.
  - Hook tests stay 91 OK.
  - Install and release tests: 18 OK plus the new failures. test_install is unaffected, because it runs only `.claude/hooks/tests` in the adopter (test_install.py:166) and release.json is not yet changed.
- **After the fix:** all new tests pass.
  - test_install's count assertion (test_install.py:86) follows release.json, so it passes with 18 vendored files.
  - release_check.py PASS with 19 release files and 18 patterns.
  - Checkers and render checks unchanged.
- **Live run** from Claude-kit-fixes after the sweep and D's copy are committed:
  - 12 `in-store` items: the ten above, plus the planner's P and D.
  - 5 `record` items dated 2026-09-23, proposed as `2026-09-23-08` to `-12` in this order: JDb 13:26:08Z, Ftd 22:34:08Z, JDb 22:39:08Z, 22:49:56Z, 23:21:01Z.
  - 10 items dated 2026-09-24 (01JDb -01, -02; 01Vss -01 to -08): `refused` if the run is on 2026-09-24 UTC. If it is on the 25th, they are `record`, proposed `2026-09-24-10` to `-19` in Preserved order.
  - No `stop` items; exit 0.

Your mechanism prediction goes in the intent, as usual.

# Finish line and abort conditions
Finish line, pushed on kit-v0.2-t3:
1. The sweep.
2. D's copy and the intent.
3. The tests-only commit.
4. The tool with release.json and README.
5. The terminal.

Then PR kit-v0.2-t3 -> main open, with CI green on its final commit. No merge, no tag.

Abort and report on any of these:
- any Base-and-state mismatch, other than D's name as allowed above
- P's or D's hook name already taken in the store by a different file
- a needed change outside the scope boundary
- a new test failing first for a reason other than the missing tool
- two failed fixes on one symptom
- a denylist hit (report the pattern; no rewording)
- an owner message in the planner log after line 87 that bears on this dispatch (as of this dispatch there is none)

# Checks
1. Tests in tests/test_find_dispatches.py. The fixture is a temp git repo with commit A (a dispatch_guard.py without `dispatch-seq`) and commit B (one with it). The main checkout's tracked store holds a few files. There are three linked worktrees, one at A and two at B. The tests run the tool as a subprocess from the main checkout. Cases:
   - (a) a T1+ file with a free name keeps its name and gets a copy command and a citation
   - (b) two pre-T1 files of one past date, in different worktrees, one of them in a worktree at B whose header HEAD is A, get consecutive numbers above the store's highest, in Preserved order
   - (c) a file byte-identical to a store file under another name is `in-store`, naming it
   - (d) a T1+ file whose name is taken in the store by other content is `stop`, and the tool exits 1
   - (e) a T1+ file whose name's date differs from its Preserved date is `stop`
   - (f) a header HEAD not in the repo is `stop`
   - (g) a file with no hook header is `stop`
   - (h) a pre-T1 file Preserved today (UTC) is `refused` with no command, and exits 0 if nothing else stops
   - (i) the same content in two worktrees is one item
   - (j) the running checkout's own untracked store file is not scanned and is counted in the warning
   - (k) read-only: every file under the fixture root, `.git` included, has the same bytes and mtime before and after a run
2. Sabotage checks. Run each against the committed tests, do not commit it, and restore with `git checkout`:
   - (s1) skip the store sha256 match: (c) fails
   - (s2) treat every file as T1+: (b) fails
   - (s3) drop the today refusal: (h) fails

   Report each failure message.
3. Every count from Base and state, before and after. Both checkers before every commit touching RECORD.md.
4. The live run above. Paste its output in the hand-back and compare it with the prediction. Copy nothing it proposes.
5. Live history_guard check, not tried before: make one of your commits with a heredoc message containing an apostrophe (`git commit -F - <<'EOF'` … `EOF`), followed in the same Bash command by `git push origin kit-v0.2-t3`. Report the hook's decision verbatim. If it blocks, record it under Deviations and continue with message files.

# Out of scope
- Copying or recording any backlog file.
- coder.md, hooks, kit.json, check_record.py, check_prompts.py, install.py, release_check.py, CI.
- The history_guard backlog items (quoted heredoc markers, `<< EOF`, the merge-check docstring line).
- test_install's 3000-character stderr tail.
- check_prompts' marker-less header search.
- Writing to any harness worktree, the main checkout or the counter.
- Merge, tag, deleting anything.

# Device items
None.