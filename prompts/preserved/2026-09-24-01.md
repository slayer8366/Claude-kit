HEAD: 93904076a95498b3a877438f6d354fc1b1b88419
Target subagent: coder
Type: build
Preserved: 2026-09-24T06:15:47Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Claude-kit v0.2: commit the spec and task list (documents only)

## Role
You are the coder. You execute this dispatch, you make no design decisions, and a gap is a stop-and-ask. Record it as `coder.md` and the checkers require, and run `check_record.py` and `check_prompts.py` before every commit that touches `RECORD.md`.

## Base and state
- `slayer8366/Claude-kit`: PR #2 merged at 2026-09-24T05:57:50Z; `origin/main` = merge commit `aeb4d0b24a9e59ca9520131f8ad2d6ea746ae4ab`. No tags.
- `~/Zynergy/Claude-kit-fixes`: on `kit-v0.1-fixes` at `6ef4d22`, clean. Work here: `git fetch origin`, then `git switch -c kit-v0.2-plan origin/main`.
- `~/Zynergy/Claude-kit`: on `kit-v0.1` (`a544ca9`), with untracked `.claude/worktrees/`, `prompts/preserved/2026-09-23-05.md` and `-06.md`. Do not touch it (Step 0 below is for a later dispatch).
- This dispatch is saved by the planner's v0.1 hook in harness worktree `~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01VssxEZCn7qBnENwZjcVycP/prompts/preserved/` (expected `2026-09-24-10.md`; identify by content and header time).
- On `origin/main`, RECORD.md's last entry is `2026-09-23-12` (terminal for intent 07) and the store's last file is `2026-09-23-09.md`... verify the actual last store file and entry; if any intent is open on main, stop.
Verify all of this first; if anything differs, stop.

## Scope boundary
Create two documents, record this dispatch, commit, push, open a PR, CI green. Nothing else: no code, hook, test, `kit.json` or checker changes; no v0.2 item built; nothing under `.claude/worktrees/*` touched (reading and copying from is fine); nothing in `~/Zynergy/Claude-kit` or Forager. No merge, no tag. Delete nothing.

## Closed decisions
Owner, 2026-09-24, on the planner's v0.2 draft (new planner log, `/home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-01VssxEZCn7qBnENwZjcVycP/ac03f257-0aff-541c-9135-20bb81e09317.jsonl`; quote the lines with numbers): "Merged. Go ahead and draft it"; then "1 approved as written", "2 all four phases can run.", "I'll run your recommendation on order". Meaning: (1) the planner gains SendMessage, sending a prompt to a named agent unchanged, and TaskStop (items 9-11), as written; (2) all four phases are in v0.2; (3) order is Step 0, then phases 1, 2, 3, 4, with phase 1 before phase 3.

Naming (planner, applying the hook's own rule at `.claude/hooks/dispatch_guard.py`, the UTC-date-plus-next-sequence logic, against the fix store): this dispatch's store copy is `prompts/preserved/2026-09-24-01.md` and the new intent is `2026-09-24-01`, the terminal `2026-09-24-02`. If the store or record already holds any `2026-09-24-*`, use the next free number and say so. The name collides with harness files `2026-09-24-01.md` in 01JDb and 01Vss (different content); note it in the intent as the known R6(a) issue, which this spec's T1 addresses.

Document 1: `docs/specs/2026-09-24-kit-v0.2-spec.md`, with exactly this content (fill `<merge>` with `aeb4d0b`):

```
# Claude-kit v0.2 specification

Status: approved by the owner 2026-09-24 (scope, planner tools, order). Base: main at <merge>.
Sources: RECORD.md entries 2026-09-23-04 (R6), 2026-09-23-07 (deferred list), and the kit v0.1 fixes intent (owner ruling items).

## Goal
Stop the stops. The v0.1 fixes needed six re-sent dispatches. The causes were four gaps: dispatches saved in scattered places under colliding names, sessions running stale hooks, a planner that cannot reach a running coder, and no clean way to resume a lost session. v0.2 closes those, then the earlier ruling items.

## Decisions (owner, 2026-09-24)
- The planner role gains SendMessage, sending a prompt to a named agent unchanged, and TaskStop.
- All four phases are in v0.2.
- Order: Step 0, then phases 1, 2, 3, 4. Phase 1 comes before phase 3 because the naming rule shapes the recording tool and the resume helper.

## Step 0: housekeeping (no code)
- Move ~/Zynergy/Claude-kit from kit-v0.1 to main.
- Confirm new harness worktrees start from main; the five existing ones sit at 9390407 with v0.1 hooks.
- Settle the untracked 2026-09-23-05.md and -06.md in that checkout; their copies are recorded on main.

## Phase 1: dispatch storage and naming
1. One naming rule for saved dispatches, the same in every worktree (R6(a), extended by the UTC-date naming and by per-worktree numbering colliding).
2. A written rule for the store names of dispatches saved outside the store.
3. A recording tool that finds hook-saved dispatches across all harness worktrees, hashes them, and produces the copies and citations.
4. Record format: a dispatch-note outcome `stopped`; an entry kind for continuing an intent (R6(b)); a fixed order for a dispatch's own copy against its notes.

## Phase 2: session integrity
5. A session-start check that warns when the session's checkout or hooks differ from what is expected.
6. Resuming after a disconnect: a written procedure, and a helper that reads the lost session's log and lists what was sent and what came back.
7. A session launcher with a time limit, a check for the expected session, and cleanup on failure.
8. A live-exercise runner that drives the exercise cases and collects the log lines as evidence.

## Phase 3: planner-to-agent communication
9. The planner may use SendMessage (R6(c)), so a ruling reaches a running or stopped coder without a full re-send.
10. The planner may send a prompt to a named agent unchanged.
11. The planner may use TaskStop.

## Phase 4: ruling items and smaller fixes
12. history_guard over-blocking on unparseable push commands.
13. Upgrades removing dropped files.
14. A kit bypass table.
15. Backups: "created after" means file birth time, written down.
16. Blocked Agent calls are logged with `toolDenialKind`, not a `deny` decision (Claude Code behaviour): document it and match on that field in evidence checks.

## Open questions (decided at the start of the named task, not now)
- T1: which store the one numbering sequence is drawn from (for example the main checkout's store via the git common directory), and what happens when that store is unreachable.
- T5: where the expected checkout and hook identity are declared (kit.json or elsewhere), and whether the check warns or blocks.
- T7: the launcher's time limit, and what "expected session" is checked against.
- T9-T11: whether SendMessage and TaskStop are limited to agents the planner itself started.

## Out of scope
Forager, any tag, and removing existing harness worktrees.
```

Document 2: `docs/specs/2026-09-24-kit-v0.2-tasks.md`, with exactly this content:

```
# Claude-kit v0.2 task list

Order per the spec: Step 0, phase 1, 2, 3, 4. Each task is one dispatch unless noted. Every code task builds its failing test first and shows it failing for the right reason, then passes, then a revert check; hook and install/release test counts are reported before and after.

| Task | Spec item | Depends on | Done when |
|---|---|---|---|
| T0 | Step 0 | none | ~/Zynergy/Claude-kit on main; the two untracked exercise files settled by owner ruling; a new harness worktree confirmed to start from main |
| T1 | 1 | T0 | The dispatch hook names a saved dispatch from one sequence shared by all worktrees; a test shows two worktrees can no longer produce the same name for different files |
| T2 | 2 | T1 | coder.md states the store-name rule; check_prompts.py enforces it, with a test that fails on a wrongly named copy |
| T3 | 3 | T1, T2 | A recording tool lists every untracked hook-saved dispatch across all harness worktrees with size and sha256, and emits copy commands and citation lines; tested on a fixture with three worktrees |
| T4 | 4 | T2 | check_record.py accepts outcome `stopped`, a continuation entry kind, and enforces the copy-versus-notes order; each with a failing-first test |
| T5 | 5 | T0 | A session-start hook warns when checkout or hook identity differs from the declared one; tested with a stale-hook fixture |
| T6 | 6 | T3 | A written resume procedure, and a helper that, given a session log, lists Agent calls sent and results returned; tested on a truncated-log fixture |
| T7 | 7 | T5 | A launcher that starts a session with a time limit, checks it is the expected one, and cleans up on failure; tested with a hanging-command fixture |
| T8 | 8 | T7 | A runner that drives the exercise cases and writes the evidence lines; one real run recorded |
| T9 | 9 | T1 | role_guard allows the planner SendMessage; tests for allowed and denied targets |
| T10 | 10 | T9 | The planner can send a prompt to a named agent unchanged; tested end to end |
| T11 | 11 | T9 | role_guard allows the planner TaskStop; tests |
| T12 | 12 | T0 | history_guard allows unparseable push commands it cannot judge, per a rule written first; failing-first test |
| T13 | 13 | T0 | An upgrade removes files the new version dropped; tested |
| T14 | 14 | T0 | A bypass table is documented and read by the hooks; tested |
| T15 | 15 | T0 | The backup rule names file birth time; documented |
| T16 | 16 | T0 | The toolDenialKind behaviour is documented and evidence checks match on it |

Phase 2 and phase 4 tasks that depend only on T0 may run in parallel with phase 1.
```

## Prediction
- Both checkers pass with the new intent and the store copy.
- Test counts unchanged from `aeb4d0b`'s: hook tests 83, install and release tests 18 (report the actual numbers before and after).
- `release_check.py` passes; the two new files are under `docs/` and do not trip the denylist. If they do, stop and report the pattern; do not reword.

## Finish line and abort conditions
Finish: both documents committed byte for byte as given (apart from `<merge>`), intent and terminal recorded, pushed, PR `kit-v0.2-plan` → `main` open, CI green on its final commit; then STOP and report with the PR URL. Abort on: any Base-and-state mismatch; any checker or test failure you would need to change code to fix; a denylist hit; two failed fixes on one CI symptom. Any owner message in the planner's log after "I'll run your recommendation on order" that bears on this dispatch: stop and report.

Steps:
1. Verify.
2. Create the branch; copy this dispatch into the store as named above; append the intent, uncommitted.
3. Write both documents.
4. Run all checks; commit; push.
5. Write the terminal; run all checks; commit; push.
6. Open the PR; CI green.
7. STOP and report.

## Checks
Every commit passes, in `~/Zynergy/Claude-kit-fixes`:
- `python3 -m unittest discover -s .claude/hooks/tests -v`
- `python3 check_record.py --render-check`
- `python3 check_prompts.py --render-check`
- `python3 -m unittest discover -s tests -v`
- `python3 release_check.py`

## Out of scope
Building any v0.2 task, Step 0, merging, tags, `~/Zynergy/Claude-kit`, harness worktrees, Forager.

## Device items
None.