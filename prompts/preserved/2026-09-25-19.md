HEAD: 8b1c88a69086b52698fa63e6f6a05937acf5685f
Target subagent: coder
Type: build
Preserved: 2026-09-25T08:25:29Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit v0.2 task T5 (`docs/specs/2026-09-24-kit-v0.2-tasks.md` line 12): a SessionStart hook that warns when the session's hooks differ from what is expected, tested with a stale-hook fixture. Follow `.claude/agents/coder.md` at your base, including item 10 for the merge.

# Base and state
Premises to verify:
- **main.** origin/main is 80f3f8c47865d3ec9489b73cc387d778a0e21455 (PR #17 merge, done by a coder under T17). Nothing is tagged.
- **PR #18** (kit-v0.2-merge-17 -> main, head a80cb4a) is open. It carries store copy `2026-09-25-18.md`, intent 2026-09-25-30 and terminal -31.
  - Branch `kit-v0.2-t5` from `origin/kit-v0.2-merge-17` (a80cb4a), so that entry IDs continue from -31.
  - Your PR will carry #18's commits. Merging it lands them, and GitHub then shows #18 as merged. The owner was told this and did not object.
- **Planner worktree W** (~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB, at 8b1c88a). Your session's hooks run from W, and your Bash cwd resets to W before every call.
  - This dispatch is expected in W's untracked store as `2026-09-25-19.md`. If the hook gave another name, that name governs.
  - D2, D5, `-17` and `-18` are also untracked there; leave them.
  - No sweep is expected: -17 and -18 are in the store on your base.
- **Planner log.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl.
- **Code** (verify each):
  - `.claude/settings.json` registers only PreToolUse.
  - guardlib's `run()` denies on a config error. Don't use it for this hook, which must never block.
  - release.json vendors each hook and hook test by path.
  - Check how harness.py copies hooks for tests.
- **Premise P, to prove before code from the Claude Code hooks docs (https://code.claude.com/docs/en/hooks.md), quoting the lines:** SessionStart exists with matchers startup/resume/clear/compact/fork, and cannot block. Which output fields exist:
  - plain stdout added as context
  - `hookSpecificOutput.additionalContext`
  - whether a top-level `systemMessage` shows a warning to the user

  If `systemMessage` does not exist, stop and report.

# Scope boundary
Files:
- **New `.claude/hooks/session_check.py`,** with the rule in its docstring.
- **`.claude/settings.json`:** register it under `SessionStart`.
- **New `.claude/hooks/tests/test_session_check.py`.**
- **`release.json`:** add both new files to `vendored`.
- **README.md:** the hook in its layout and hook descriptions, and one sentence that a settings change needs a new session.
- **`docs/specs/2026-09-24-kit-v0.2-tasks.md`:** T5's done-when only, if its wording no longer matches ("warns" stays).

Record:
- this dispatch's copy and intent 2026-09-25-32
- the tests-only commit, the fix, and terminal 2026-09-25-33

No change to other hooks, guardlib, kit.json keys, templates, install or check_kit, coder.md, checkers, or CI.

# Closed decisions
The owner, in the planner log. Cite by line and timestamp:
- **The AskUserQuestion result** where T5 was answered:
  - "Default branch, no key (Recommended)"
  - "Warn only (Recommended)"

  Quote both option texts from its tool_use.
- **"Authorize the coder to merge T5 once green".**
- **"Instruct a coder to merge 17 into main"** (the precedent run).

The planner's rules:
1. **The hook finds its repository** with `git rev-parse --show-toplevel` from the payload's `cwd`. It reads `.claude/kit.json` there; if that fails, it warns about it and stops there.
   - The expected branch is the first of `protected_branches`, compared as `origin/<branch>` as last fetched. The hook never fetches.
   - If that ref doesn't exist, it warns "cannot compare" and names it.
2. **Stale check.** Run `git diff --name-only origin/<branch> -- .claude/hooks .claude/agents .claude/settings.json .claude/kit.json`. This compares the working tree, not HEAD, so uncommitted edits count.
   - Any output means the session's hooks differ from the default branch.
   - Also report `git rev-list --count HEAD..origin/<branch>` if it is above 0.
3. **Lock check,** only if `.claude/kit.lock` exists: every file the lock lists is sha256-compared with the lock. Missing or differing files are reported.
4. **Output.** When anything is reported, print one JSON object on stdout:
   - `systemMessage`: a short warning for the user
   - `hookSpecificOutput`: `{"hookEventName": "SessionStart", "additionalContext": <the full text>}`

   The full text holds the differing paths, the commit count, and the command `git -C <toplevel> fetch && git -C <toplevel> merge --ff-only origin/<branch>` as advice, not run. If `.claude/settings.json` is among the differing paths, it adds "settings.json differs: start a new session after updating, since hooks are read at session start".

   When nothing differs, print nothing.
5. **Never block.** Always exit 0, including on any internal error, which it reports as a warning. It writes nothing.

# Merge
authorised: the T5 PR only (kit-v0.2-t5 -> main), with `--merge`, once CI is green on its final commit (the terminal commit), after coder.md item 10's backup. The merge command may run with cwd W: W and the fixes checkout share one git directory, as the #17 merge showed. Merge nothing else. Don't merge PR #18 separately.

# Prediction
- **Tests-only commit** (test_session_check.py, running the hook as a script against fixture repos with a bare origin):
  - t1: up to date → no output, exit 0
  - t2: origin/main has a newer `.claude/hooks/role_guard.py` → the warning names it and has the ff command, exit 0
  - t3: settings.json differs → the restart sentence
  - t4: an adopter fixture with kit.lock and one edited file → names it
  - t5: no `origin/main` ref → "cannot compare", exit 0
  - t6: kit.json unreadable → a warning, exit 0
  - t7: uncommitted edit to a hook → reported

  Each fails because the hook file doesn't exist. Hook tests: 117 plus 7. test_install's vendored-hook-tests case: the new test file isn't vendored yet at that commit, so test_install stays OK unless harness needs a change. Report which.
- **After the fix:**
  - all hook tests OK
  - 39 tests/ OK: test_install vendors and runs the new test in an adopter
  - render checks 30/30 and 10/10, both checkers PASS
  - release_check PASS with 21 files and no denylist hit
- **Revert check:** deleting session_check.py (not committed) gives the same failures.
- **Live check, read-only:** run the hook as a script with cwd W, which is at 8b1c88a and behind origin/main 80f3f8c by the #17 merge. Expect the warning: the commit count, and no `.claude/` difference, since #17 changed only record files. Then run it with cwd set to the fixes checkout on your branch, before you push the fix, against origin/main: it should report settings.json and the new hook files as differing.

Your mechanism prediction goes in the intent. It includes P, as proved.

# Finish line and abort conditions
Finish line, pushed on kit-v0.2-t5:
1. the copy and the intent
2. the tests-only commit
3. the fix
4. the terminal

Then PR kit-v0.2-t5 -> main with CI green on the terminal commit. Then the merge under the Merge section, with the backup. Report the merge commit, and state that PR #18 now shows merged. No tag.

Abort and report on any of these:
- any Base-and-state mismatch other than the dispatch's name
- P false (no systemMessage)
- a needed change outside scope
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit
- CI not green on the final commit: don't merge
- history_guard denying the merge after the backup. Quote it; no workaround.

Owner messages after this dispatch's Agent call: an abort only if one tells you to do or not do something in T5's scope, or changes a decision here. Record every other message in the terminal. A planner message follows coder.md item 6.

# Checks
- Failing-first run and revert check.
- Counts before and after.
- Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
- Tests in their own Bash call.
- The merge's backup evidence: manifest check, `git bundle list-heads`, the INDEX line, and history_guard's decision.

# Out of scope
- Fast-forwarding W.
- The dispatch hook's untracked-copy problem.
- D2, D5 and the T3 backlog.
- T6, T7 and T10.
- Other PRs, tags, deleting anything.

# Device items
None. A session restart that shows the warning in a real new session is the owner's check, listed in the hand-back as not done.