HEAD: db742d2bd6f0926d908ad5919f9fc28806a68547
Target subagent: coder
Type: build
Preserved: 2026-09-25T18:21:52Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are a coder updating the main checkout. Move its three untracked store copies aside, then fast-forward it to origin/main. Follow `.claude/agents/coder.md` in ~/Zynergy/Claude-kit. The exception is recording: that goes to the next dispatch's sweep, as with 2026-09-25-28 and -30. This is a repeat of dispatch 2026-09-25-30 with new names and SHAs.

# Base and state
Premises to verify (read-only) first:
- C = ~/Zynergy/Claude-kit, on main at db742d2bd6f0926d908ad5919f9fc28806a68547, with no tracked changes.
- `git ls-remote origin main` = 9e6ff6b2c2b06bbd8fd4d73f4a04b23e415046ad (the PR #26 merge, T6).
- C's untracked files:
  - `.claude/worktrees/`: leave it alone.
  - `prompts/preserved/2026-09-25-30.md`, `-31.md` and `-32.md`: the three to move. -30 is the earlier move-and-pull, sha256 73b02adcafcbad5a1309c6a495c3a04da7f157b3b2de1f6cd7c96bb356316a9a. -31 is the T6 pulse and -32 the T6 dispatch.
  - This dispatch's copy, saved by the hook (expected `-33.md`; the hook's name governs). It is not on origin/main, so it stays.
- K1: after `git -C C fetch origin`, each of the three files' sha256 in C equals `git -C C show origin/main:prompts/preserved/<name> | sha256sum`.

# Scope boundary
1. `git -C C fetch origin`, then K1.
2. K2: `mv -n` the three into a new ~/forager-backups/<next free UTC-date folder>/prompts/preserved/, in the layout of backup 2026-09-25-13. Add a MANIFEST.sha256 over them that passes `sha256sum -c`, and one INDEX.md line modelled on 2026-09-25-13's.
3. K3: `git -C C status --porcelain --untracked-files=all` then shows only `.claude/worktrees/…` and this dispatch's copy.
4. `git -C ~/Zynergy/Claude-kit pull --ff-only origin main`, as one Bash call. Report its output verbatim.
5. After the pull:
   - C's HEAD equals 9e6ff6b.
   - The three files are tracked (`git ls-files --error-unmatch`) at the K1 hashes.
   - Report `git -C C diff db742d2 9e6ff6b --stat -- .claude/`, and whether `.claude/settings.json` changed.

Write nothing else: no RECORD.md entry, commit, push, branch or merge, and nothing deleted. Report this dispatch's copy's name, size and sha256 for the next sweep.

# Closed decisions
- The owner, this session, verbatim: "Yes go ahead and send the move-and-pull for the main checkout, -30 to -32, bringing it to 9e6ff6b".
- Precedent: 2026-09-25-30 (backup 2026-09-25-13).

# Merge
not authorised

# Prediction
- K1: all three match.
- K2: the manifest shows 3 OK, and INDEX.md grows by one line.
- K3: only `.claude/worktrees/` and this dispatch's copy remain.
- The pull prints "Updating db742d2..9e6ff6b" and "Fast-forward", and exits 0 with no hook deny.
- The `.claude/` diff is empty: T6 changed only session_agents.py, tests/, README, release.json, RECORD.md and the store. settings.json is unchanged.

# Finish line and abort conditions
**Finish line:** K1-K3 done, and the pull fast-forwarded C to 9e6ff6b. Report the after-checks and the store copy's details.

Abort and report on any of these:
- C not on main, or with tracked changes.
- origin/main anything other than 9e6ff6b.
- Any of the three differing from origin/main (don't move it).
- A manifest mismatch.
- Untracked files beyond those listed.
- The pull refusing (report it verbatim, no workaround).
- A hook denying something (quote it).
- An owner message after this Agent call that tells you to do or not do something in this scope.

# Checks
- K1 hashes on both sides.
- `sha256sum -c` output.
- The INDEX line.
- Status before and after.
- The pull output.
- HEAD after.
- The `.claude/` diff stat.
- The store copy's name, size and sha256.

# Out of scope
- The record, PRs and tags.
- `.claude/worktrees/` and every worktree.
- Other checkouts.
- Deleting anything.

# Device items
None.