HEAD: d59280fceb136d04d2165d3851bdd2466a04ace6
Target subagent: coder
Type: build
Preserved: 2026-09-25T03:56:56Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for a small follow-up to Claude-kit v0.2 T13, on the same branch before it merges. install.py's upgrade trusts the paths in the old `.claude/kit.lock`, so a bad lock could make it remove any file. Make it reject such a lock. Follow `.claude/agents/coder.md` at your base.

# Base and state
Premises to verify:
- **Branches.** origin/kit-v0.2-t13 is at 7f8fe222c6e7e2c156dd40558019e1c699e9d36e (T13's terminal 2026-09-25-19). PR #14 is open and unmerged. origin/main is 3a5a13d. Nothing is tagged. Work on `kit-v0.2-t13` in ~/Zynergy/Claude-kit-fixes, from 7f8fe22.
- **Record.** The last entry on that branch is 2026-09-25-19. Intent -18 is closed; no intent is open.
- **Planner worktree W** (~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB). This dispatch is expected in its untracked store as `2026-09-25-13.md`. If the hook gave another name, that name governs. D2 and D5 stay cited. No sweep is expected: `2026-09-25-11` and `-12` are on the branch.
- **Code** (install.py at 7f8fe22; confirm the lines):
  - `plan_upgrade` reads the old lock's `files` map and does not check its paths.
  - The owned paths are defined near :32 (`OWNED` or similar) and rejected for releases.
  - The previous coder's flag: a lock listing an absolute path, a `..` path or an owned file with a matching hash would make an upgrade remove that file.
- **Planner log.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl.

# Scope boundary
Files:
- **`install.py`:** the lock-path check, plus one sentence in its docstring.
- **`tests/test_install.py`:** test u9.

Record:
- this dispatch's store copy and intent 2026-09-25-20
- the tests-only commit, the fix, and terminal 2026-09-25-21

Nothing else. No README change unless the "Install and drift" section's upgrade text states which locks are malformed; if so, add one clause there.

# Closed decisions
- The owner's "1, go ahead and dispatch it real quick" answers the planner's option 1: "Hold #14 and I dispatch the path fix onto the same branch (recommended). You then merge once with both."
- The rule, from the planner's message before that: "treat a lock as malformed if any path is absolute, contains `..`, or is adopter-owned (`.claude/kit.json`, `RECORD.md`, `CLAUDE.md`, `prompts/`), so the upgrade stops before writing anything."

Cite both by line and timestamp.

The planner's rules:
- The check applies to every key in the old lock's `files` map, before any other planning. A path is rejected if any of these holds:
  - `PurePosixPath(p).is_absolute()`, or it starts with `/` or a drive letter
  - any part is `..`
  - it equals an owned path, or lies under `prompts/`
- A rejection follows T13's malformed-lock rule: stop before writing or removing anything, name the lock and each offending path with its reason, exit 1, and leave the target byte-for-byte unchanged.

# Prediction
- **Tests-only commit.** u9, with subTests:
  - (a) an absolute path to a file outside the adopter, whose sha256 matches the lock
  - (b) `../outside.txt`, whose file exists with a matching hash
  - (c) `RECORD.md`, with a matching hash

  Each path is in the old lock and dropped by the new tag. Before the fix, each subTest fails with "0 != 1" on the exit code: the upgrade succeeds and removes the file. Assert the exit code first, then that the file still exists. tests/: 38 run, 3 failure records. Hook tests: 106.
- **After the fix:** 38 OK and 106 OK. Render checks 30/30 and 10/10, both checkers PASS, release_check PASS with 19 files.
- **Revert check:** install.py from 7f8fe22 gives the same 3 failures.

**Before running (a) or (b):** each subTest must create its outside file inside the test's own temporary root, a sibling of the adopter directory. Never point at a real file.

Your mechanism prediction goes in the intent.

# Finish line and abort conditions
Finish line: the copy and intent, the tests-only commit, the fix and the terminal, all pushed on kit-v0.2-t13. PR #14 then shows CI green on the new final commit. Don't open a new PR. No merge, no tag.

Abort and report on any of these:
- any Base-and-state mismatch other than the dispatch's name
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a needed change outside scope

Owner messages after this dispatch's Agent call: an abort only if one tells you to do or not do something in this scope, or changes a decision above. Record every other message in the terminal. A planner message follows coder.md item 6.

# Checks
- Failing-first run and revert check.
- Counts before and after. Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
- Run tests in their own Bash call, never with a commit or push.

# Out of scope
check_kit.py, `docs/standing-rulings.md`, T17, T5, merge, tag, deleting anything outside test temp directories.

# Device items
None.