HEAD: d59280fceb136d04d2165d3851bdd2466a04ace6
Target subagent: coder
Type: build
Preserved: 2026-09-25T05:24:00Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit v0.2 task T17, a new task the owner added: a coder may merge a pull request when the dispatch authorises it, and only after writing a backup that lets the owner undo the merge. history_guard checks the backup. Follow `.claude/agents/coder.md` at your base.

# Base and state
Premises to verify:
- **main.** origin/main is the merge of PR #15. Get the SHA with `gh pr view 15 --json mergeCommit` and check it with `git ls-remote`. Nothing is tagged. The record's last entry is 2026-09-25-25; no intent is open. Create `kit-v0.2-t17` from origin/main in ~/Zynergy/Claude-kit-fixes (currently on kit-v0.2-rulings2).
- **Planner worktree W** (~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB, at d59280f, whose hooks run in your session). This dispatch is expected in its untracked store as `2026-09-25-16.md`. If the hook gave another name, that name governs. D2 and D5 are still dated today; leave them. No sweep is expected.
- **Code** (verify each, with lines):
  - history_guard.py blocks `gh pr merge` for everyone (:192-194 at 742d10f) and treats `git merge` on a protected branch at :200-209. After T12 it parses the push check per segment. It has no notion of role today.
  - role_guard.py does not list `gh pr merge` in GH_READ, so the planner and pulse are denied it there too.
  - guardlib.py: CONFIG_REQUIRED and CONFIG_DEFAULTS (:33-36); unknown keys are invalid (:54-56).
  - `.claude/kit.json`'s `required_sections` for build and device.
  - templates/kit.json.
- **Planner log.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl.
- **history_guard** blocks any Bash command whose text contains the literal GitHub CLI merge command. Write files that contain it with Edit or Write, and keep it out of Bash commands, commit messages and the PR body.

# Scope boundary
Files:
- **`docs/specs/2026-09-24-kit-v0.2-spec.md`:** add item 17 under a new "Owner additions" heading.
- **`docs/specs/2026-09-24-kit-v0.2-tasks.md`:** add a T17 row. Depends on T12; Done when as in the Finish line.
- **`.claude/hooks/guardlib.py`:** the optional key `backup_dir`, in CONFIG_DEFAULTS with default None; a string when present.
- **`.claude/hooks/history_guard.py`:** the merge rule below, stated first in its module docstring.
- **`.claude/hooks/tests/`:** test_history_guard.py, test_config.py, and harness.py if its test config needs the key.
- **`.claude/kit.json`** (this repo's own, not released): `"backup_dir": "~/forager-backups"`, and "Merge" appended to required_sections for build and device.
- **`.claude/agents/coder.md`:** a new item under "The record" (text below).
- **README.md:** a kit.json table row for `backup_dir`; a short "Merging and undoing a merge" section; `Merge` noted as a section this repo requires.

Record:
- this dispatch's copy and intent 2026-09-25-26
- the rules commit, the tests-only commit, the fix, and terminal 2026-09-25-27

Not changed: templates/kit.json (adopters opt in), role_guard, dispatch_guard code, install and release tooling, checkers, find_dispatches, `docs/standing-rulings.md`, CI.

**Commit order.** `.claude/kit.json`'s new key must land in the same commit as guardlib's acceptance of it, which is the fix commit. Otherwise any session running this tree would find the config invalid and deny every tool.

# Closed decisions
The owner, in the planner log. Cite each by line and timestamp:
- **Line 712:** "coders should be able to merge at the discretion of the planner. All merged work must contain a backup, so any merge should be able to be undone by the owner later on".
- **The answers at 716/717:**
  - "Bundle in forager-backups (Recommended)"
  - "history_guard checks (Recommended)"
  - "Next, after T13 (Recommended)"

  Quote the option texts.
- **The T17 AskUserQuestion answered twice.** The first round was interrupted by the owner and re-asked at the owner's request ("Show the options again"). The second round governs:
  - Backup dir = "Optional key, no default (Recommended)"
  - Authorise = "Required \"Merge\" section (Recommended)"
  - Merge form = "Also squash"

  Quote the option texts from the second tool_use. Record that the first round's "Merge commit only" was superseded by the re-ask.
- **"Merged"** (PR #15).

The planner's rules:
1. **What history_guard allows.** A Bash command containing `gh pr merge` is denied unless every condition holds:
   - **(a) Role.** The calling agent's role (guardlib's agent_roles, from the payload's agent_type) is `coder`. The main session is the planner and is denied.
   - **(b) Form.** The command is one segment, with no other command in it. Its tokens are `gh pr merge <N>`, where N is all digits, and exactly one method flag: `--merge`/`-m` or `--squash`/`-s`. The only other flags allowed are `--subject`/`-t <text>`, `--body`/`-b <text>` and `--match-head-commit <sha>`. Anything else is denied by name: `--rebase`/`-r`, `--auto`, `--admin`, `--delete-branch`/`-d`, `--repo`/`-R`, `--disable-auto`, any unknown flag, and a missing N.
   - **(c) Config.** `backup_dir` is set. If it is unset, deny, saying to set it in kit.json. Expand `~`.
   - **(d) The backup.** Exactly one folder directly under `backup_dir` holds a `merge.json` with `"pr": N`. That file is a JSON object with `pr` (int), `branch` (string), `sha` (40 lowercase hex) and `bundle` (a file name in that folder). In that folder:
     - `MANIFEST.sha256` lists at least merge.json and the bundle, and every listed file's sha256 matches.
     - `git bundle list-heads <bundle>` lists `sha`.
     - `backup_dir/INDEX.md` has a line containing the folder's name.
   - **(e) Freshness.** `git rev-parse origin/<branch>` in the payload's cwd equals `sha`. The hook does not fetch; coder.md tells the coder to fetch first.

   Each denial names the failed condition. `git merge` handling is unchanged.
2. **coder.md.** A new item after 9, verbatim:
   "10. **Merging.** Merge a pull request only if the dispatch's `Merge` section reads `authorised`. First `git fetch`, then write the backup: a new folder under the `backup_dir` that `.claude/kit.json` names, holding a bundle of `origin/<base branch>` (`git bundle create`), `merge.json` (`pr`, `branch`, `sha` of `origin/<base branch>`, `bundle`), and `MANIFEST.sha256` over both, with one line naming the folder added to that directory's `INDEX.md`. Then merge by pull request number with a merge commit or a squash, and cite the backup folder in your terminal. history_guard refuses the merge if any of this is missing."
3. **README's "Merging and undoing a merge"** states how the owner undoes a merge: revert the merge or squash commit, or reset the branch to `merge.json`'s `sha`, a force push the owner makes. If the remote is lost, restore from the bundle.

# Merge
not authorised

# Prediction
- **Tests-only commit** (history_guard tests, with a fixture: a temp repo with a bare `origin`, `origin/main` fetched, and a helper that writes a real backup):
  - m1: the coder role, `--merge`, a valid backup → allowed
  - m2: the coder role, `--squash`, a valid backup → allowed
  - m3: `backup_dir` unset → denied, naming backup_dir
  - m4: no merge.json for that PR → denied
  - m5: a bundle altered after the manifest → denied, naming the manifest
  - m6: `sha` ≠ origin/main → denied, naming freshness
  - m7: INDEX.md with no line naming the folder → denied
  - m8: subTests for `--rebase`, `--auto`, `--admin`, `--delete-branch`, `-R x/y`, no PR number, and a compound command → each denied, naming the flag or the form
  - m9: the planner (no agent_type) and the pulse role, with a valid backup → denied. Assert deny only.

  Config tests: c1, `backup_dir` present as a string → accepted; c2, as a non-string → invalid.

  Before the fix:
  - m1 and m2 fail with today's "is blocked" deny.
  - m3 to m8 fail on their reason assertions.
  - m9 passes.
  - c1 fails as an unknown key; c2 fails as the wrong message, or passes. Report which.

  Hook tests: 106 plus the new ones. test_install's vendored-hook-tests case fails at that commit, with its message possibly cut off.
- **After the fix:**
  - all hook tests OK
  - 39 tests/ OK
  - render checks 30/30 and 10/10, both checkers PASS
  - release_check PASS with 19 files, and no denylist hit: `forager-backups` appears only in `.claude/kit.json`, which is not released
- **Revert check:** history_guard.py and guardlib.py from base against the committed tests give the same failures.

Your mechanism prediction goes in the intent.

# Finish line and abort conditions
Finish line, pushed on kit-v0.2-t17:
1. the copy and the intent
2. a rules commit: the spec and task rows, the history_guard docstring rule, coder.md item 10 and the README sections, with no code and no kit.json change
3. the tests-only commit
4. the fix, including `.claude/kit.json`
5. the terminal

Then PR kit-v0.2-t17 -> main open, with CI green on its final commit. No merge (this dispatch's Merge section says not authorised), no tag.

Abort and report on any of these:
- any Base-and-state mismatch other than the dispatch's name or line drift
- a needed change outside scope, including role_guard or dispatch_guard code
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit

Owner messages after this dispatch's Agent call: an abort only if one tells you to do or not do something in T17's scope, or changes a decision above. Record every other message in the terminal. A planner message follows coder.md item 6.

# Checks
1. Failing-first run and revert check.
2. Counts before and after. Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
3. Run tests in their own Bash call.
4. **A read-only live check:** run the new history_guard as a script with a payload for the coder role against a scratch repo and backup under /tmp. Show one allowed and one denied (freshness). Merge nothing real.

# Out of scope
- The T3 backlog, T5 and T10, and updating W.
- `docs/standing-rulings.md` (B-11's "Enforced by" becomes stale when this merges; flag it).
- templates/kit.json.
- Merging this PR, tags, deleting anything outside temp directories.

# Device items
None.