HEAD: 8b1c88a69086b52698fa63e6f6a05937acf5685f
Target subagent: coder
Type: build
Preserved: 2026-09-25T10:04:18Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for a housekeeping run: update the planner's harness worktree W with the new `update_worktree.py`, then record it. Follow `.claude/agents/coder.md` at your base, including item 10 for the merge.

# Base and state
Premises to verify:
- **main.** origin/main is 3847590469634ed45759e429ce7900be07a4d169 (PR #20 merge, update_worktree.py). Nothing is tagged. The record's last entry is 2026-09-25-35; no intent is open.
- **W** is ~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB, at 8b1c88a on its own branch, with no commits of its own. Its hooks run in your session, and your Bash cwd resets to W. Its untracked store files:
  - `2026-09-25-17` to `-20`: all on origin/main now
  - D2 `-02` and D5 `-05`: not on main
  - this dispatch, expected as `2026-09-25-21.md` (not on main). If the hook gave another name, that name governs.
- **The script.** W doesn't have `update_worktree.py` yet. Run the copy in ~/Zynergy/Claude-kit-fixes, which is on kit-v0.2-update-wt at 8250b56. Check that its `update_worktree.py` equals origin/main's (`git diff 8250b56 origin/main -- update_worktree.py` is empty).
- **Backups.** W's kit.json has `backup_dir` "~/forager-backups". INDEX.md's last folder is `2026-09-25-05`.
- **Planner log.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl.

# Scope boundary
1. `python3 ~/Zynergy/Claude-kit-fixes/update_worktree.py <W literal path>`: a dry run. Show its output.
2. If the dry run exits 0, run the same command with `--apply`. Show its output.
3. Nothing else in W: no other git command that writes.
4. **Record,** on branch `kit-v0.2-wt-update` from origin/main in ~/Zynergy/Claude-kit-fixes:
   - this dispatch's store copy
   - intent 2026-09-25-36, committed and pushed before step 2
   - terminal 2026-09-25-37
   - a PR

No code, docs or config change. No other worktree touched. Nothing deleted: the script moves files.

# Closed decisions
The owner, in the planner log: "Go ahead and have a coder run it", answering the planner's message that gave the `--apply` command using the fixes checkout's copy. Cite it by line and timestamp.

# Merge
authorised: this dispatch's record PR only (kit-v0.2-wt-update -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup. The merge command may run with cwd W. Merge nothing else.

# Prediction
- **Dry run:**
  - moves: `-17`, `-18`, `-19` and `-20`
  - left alone: `-02`, `-05` and `-21`
  - fast-forward `8b1c88a..3847590`
  - exit 0
- **`--apply`:**
  - it fetches
  - it moves the four files into `~/forager-backups/2026-09-25-06/prompts/preserved/`, with MANIFEST.sha256 and one INDEX line
  - it fast-forwards W to 3847590, or to the current origin/main; report which
- **Afterwards:**
  - W's HEAD equals origin/main.
  - `git -C W status --porcelain --untracked-files=all` shows only `-02`, `-05` and `-21`.
  - W's `.claude/settings.json` equals origin/main's and registers SessionStart. Report that the planner session needs a restart to run the new hook.
  - Run W's own `session_check.py` as a script with a SessionStart payload, cwd W. It should print nothing, since W is now current.
- **Counts** in the fixes checkout, unchanged: 124 hook tests, 47 tests/, render checks 30/30 and 10/10, both checkers PASS, release_check PASS with 22 files.
- **The merge's backup,** written after the record PR is green, is a separate folder from the script's (the next NN).

Your mechanism prediction goes in the intent.

# Finish line and abort conditions
Finish line:
- W updated
- the record pushed, and its PR merged under the Merge section with a checked backup
- the merge commit reported
- no tag

Abort and report on any of these:
- any Base-and-state mismatch other than the dispatch's name
- the dry run exiting 1: don't run `--apply`; report its reasons
- `--apply` exiting 1: report its output verbatim; no workaround
- CI not green: don't merge
- history_guard denying the merge after the backup: quote it
- two failed fixes on one symptom

Owner messages after this dispatch's Agent call: an abort only if one tells you to do or not do something in this scope, or changes a decision here. Record every other message in the terminal. A planner message follows coder.md item 6.

# Checks
- Each command and its output in the hand-back.
- Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
- Tests in their own Bash call.

# Out of scope
- D2, D5, the T3 backlog, T6-T10 and T14-T16.
- Any other worktree.
- Other PRs, tags, deleting anything.

# Device items
None. The owner restarts the planner session and looks for T5's warning (or its absence). List that as not done.