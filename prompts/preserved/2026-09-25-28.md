HEAD: 1599a53975b8f980035c594d52f7d125de768370
Target subagent: coder
Type: build
Preserved: 2026-09-25T16:53:10Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are a coder running one command the owner asked for: a fast-forward-only pull of the main checkout. Follow `.claude/agents/coder.md` in ~/Zynergy/Claude-kit, except for the recording, which this dispatch hands to the next dispatch's sweep (see Scope boundary).

# Base and state
Check these before you run anything (read-only):
- C = ~/Zynergy/Claude-kit, on branch main.
- The planner read `git rev-parse HEAD` = 1599a53975b8f980035c594d52f7d125de768370, and `git ls-remote origin main` gave the same SHA.
- Untracked: `.claude/worktrees/` and `prompts/preserved/2026-09-25-27.md` (the stopped dispatch), plus this dispatch's copy saved by the hook (expected `2026-09-25-28.md`; whatever name the hook gave governs).

# Scope boundary
- Run exactly `git -C ~/Zynergy/Claude-kit pull --ff-only origin main` as one Bash call, and report its output verbatim.
- Before and after it, run read-only checks: `git -C ~/Zynergy/Claude-kit rev-parse HEAD`, `git -C ~/Zynergy/Claude-kit status --porcelain --branch`, `git -C ~/Zynergy/Claude-kit ls-remote origin main`.
- Write nothing else. No RECORD.md entry, no commit, no push, no branch, no backup, no merge. The next dispatch sweeps this dispatch's store copy with a dispatch-note; report its name, size and sha256 so it can.

# Closed decisions
- The owner, this session, verbatim: "Have a coder run git -C ~/Zynergy/Claude-kit pull --ff-only origin main", then "I won't authorize the send until you have a coder run that command".
- The planner told the owner that C and origin main were both at 1599a53, so the pull should be a no-op. The owner still wants it run by a coder.

# Merge
not authorised

# Prediction
- Before: HEAD 1599a53, `## main...origin/main`, remote main 1599a53.
- The pull prints "Already up to date." (possibly after a "From ..." fetch line) and exits 0.
- After: HEAD unchanged. If remote main has moved since the planner read it, the pull fast-forwards to it instead; report the new SHA.

# Finish line and abort conditions
- **Finish line:** the command has run and its output is reported, with the before and after checks and the store copy's name, size and sha256.
- **Abort:**
  - C not on main, or C with tracked changes: don't run the command.
  - The pull refusing, for example "Not possible to fast-forward": report it verbatim, with no workaround and no retry.
  - A hook denying the command: quote the deny.

# Checks
The before and after outputs above, verbatim. The store copy's name, size and sha256.

# Out of scope
- Everything else: T14-16's terminal, PR #25, the backup, the record.
- The worktrees under `.claude/worktrees/`, and deleting anything.

# Device items
None.