HEAD: 45f053b8a2719824dbdc2cfa02157e0e4e98bb79
Target subagent: coder
Type: build
Preserved: 2026-09-24T19:42:54Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Claude-kit v0.2 T1: one naming sequence for saved dispatches across all worktrees

## Role
You are the coder. You execute this dispatch, you make no design decisions, and a gap is a stop-and-ask. Record it as `coder.md` and the checkers require, and run `check_record.py` and `check_prompts.py` before every commit that touches `RECORD.md`.

## Base and state
- `slayer8366/Claude-kit`: PR #4 (kit-v0.2-t0) merged at 2026-09-24T16:32:18Z; `origin/main` = `f97a5d196344d8a298778329861a3f7f5c764fd2` (read with `git ls-remote`). No tags. `docs/specs/2026-09-24-kit-v0.2-tasks.md` defines T1 (depends on T0).
- On `origin/main`: RECORD.md's last entry is `2026-09-24-04` (terminal, closes intent `2026-09-24-03`); no intent is open. The store's last file is `prompts/preserved/2026-09-24-02.md`.
- `~/Zynergy/Claude-kit-fixes`: on `kit-v0.2-t0` at `7bdbeac`, clean. Work happens here on a new branch `kit-v0.2-t1` from `origin/main`.
- `~/Zynergy/Claude-kit` (main checkout): on `main` at `45f053b` (one merge behind origin/main); untracked only `.claude/worktrees/`. Not touched by this dispatch.
- The planner's harness worktree `~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01TVuxEhXQ9gEiCqR5vw4nGZ` (HEAD `45f053b`) holds two untracked hook-saved files, both needed here:
  - P, the planner's one-question pulse, saved as `prompts/preserved/2026-09-24-02.md` (header target pulse, type pulse, HEAD 45f053b8...). Its hand-back reported `git rev-parse HEAD` = `45f053b8a2719824dbdc2cfa02157e0e4e98bb79` in that worktree.
  - D, this dispatch, expected `prompts/preserved/2026-09-24-03.md` (header target coder, type build). Identify it by header and content.
- Other harness worktrees hold untracked hook-saved files (01Ftd: 1, 01JDb: 8, 01Vss: 11). They are left to T3; do not copy or record them.
Verify all of this first; if anything differs, stop.

## Scope boundary
Code: `.claude/hooks/dispatch_guard.py` (the `preserve` function and its module docstring point 4) and `.claude/hooks/tests/test_dispatch_guard.py`. If README.md states the per-worktree naming rule, correct that sentence only. Record: store copies of P and D, one dispatch-note, one intent, one terminal. Nothing else: no change to check_prompts.py, check_record.py, coder.md, kit.json, other hooks, install/release tooling or CI. No store-name rule (that is T2). No merge, no tag. Do not touch any harness worktree (reading is fine). Delete nothing.

## Closed decisions
Owner rulings, in the planner log `/home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-01TVuxEhXQ9gEiCqR5vw4nGZ/a4155372-2845-559f-b1d8-b1e1db6614c8.jsonl` (quote with line numbers in the intent): the owner's handover message opening this planner session (the v0.2 state, order, and T1 as next task), and the owner's answer to the T1 question, "Option A as you recommend". Both are ruled on here. Option A as the planner put it:
"A locked counter file in the repository's shared `.git` folder (found with `git rev-parse --git-common-dir`), counting per UTC date, shared by every worktree of the clone. Each saved file stays in its own worktree's `prompts/preserved/`. At the first use each day, the counter starts above the highest name for that date already in the store." When it cannot be reached: "Block the dispatch."

Therefore, in `preserve` (names stay `<UTC date>-<NN>.md`, zero-padded to two digits, header unchanged):
1. Resolve the common dir with `git rev-parse --git-common-dir`, made absolute against the worktree root if git returns a relative path. Counter directory `<common dir>/claude-kit/`, counter file `dispatch-seq` holding one line `YYYY-MM-DD NN`, lock file `dispatch-seq.lock`.
2. Under an exclusive `fcntl.flock` on the lock file: last = NN if the counter's date is today (UTC) else 0; local = highest NN for today in this worktree's `prompts/preserved/`; seq = max(last, local) + 1. Create the prompt file with mode "x"; on FileExistsError increment and retry (as now). Then write the counter (today, the seq used) via a temp file and `os.replace`. Release the lock.
3. Any failure in steps 1-2 raises, so the existing handler denies the dispatch. The deny message must say the shared dispatch counter could not be reached and name its path.
4. Python 3.8 compatible (CI runs 3.8 and 3.14).

Record naming (planner-assigned, the store's next free names): P's store copy `prompts/preserved/2026-09-24-03.md`, dispatch-note `2026-09-24-05` (Type pulse, Outcome answered); D's store copy `prompts/preserved/2026-09-24-04.md`, intent `2026-09-24-06`, terminal `2026-09-24-07`. Copies byte for byte, header included; confirm with `cmp` and sha256. If a name is taken, use the next free one and say so.

The dispatch-note for P also records T0's third point (listed as pending the owner in terminal `2026-09-24-04`) as confirmed: harness worktrees bridge-cse_01TVuxEhXQ9gEiCqR5vw4nGZ and bridge-cse_012Hz1TBraerMpNm4CRgDtQr are at `45f053b` (run `git worktree list` yourself and quote it; confirm with `git log 45f053b..<branch>` that neither branch has commits of its own). Record the caveat as observed: they started from the main checkout's local `main` (45f053b), not from `origin/main` (f97a5d1), so a new worktree carries the hooks of whatever the main checkout last pulled. Creation times were not read.

## Prediction
Planner prediction (stated, not withheld):
- Test-only commit: a two-worktree test (fixture repo, `git worktree add`, one dispatch preserved in each) fails because both files are named `<date>-01.md`; an unreachable-counter test (make `<common dir>/claude-kit` a regular file, so the directory cannot be created) fails because the dispatch is allowed rather than denied. A seeding test (a worktree whose store already holds `<date>-05.md` gets `<date>-06.md` from an empty counter) passes before and after; say so.
- After the fix: all pass. Revert check (restore `preserve` from origin/main against the committed tests): the two failing tests fail again with the same messages.
- Hook tests 83 before, 83 plus the new tests after; install and release tests 18 unchanged; both checkers pass; release_check.py PASS, 18 release files.
Write your own mechanism prediction in the intent.

## Finish line and abort conditions
Finish: the record, the failing tests, the fix and the terminal are pushed on `kit-v0.2-t1`; PR `kit-v0.2-t1` → `main` open with CI green on its final commit; then STOP and report with the PR URL, the before and after test counts, the failing-first output and the revert-check output.
Abort on: any Base-and-state mismatch; a needed change outside the scope boundary; a new test that fails for a reason other than the predicted one; two failed fixes on one symptom; any owner message in the planner log after "Option A as you recommend" that bears on this dispatch.

Steps, one commit each, each pushed:
1. Verify. Branch `kit-v0.2-t1` from `origin/main`.
2. Sweep: P's store copy and dispatch-note `2026-09-24-05`.
3. D's store copy and intent `2026-09-24-06`.
4. Tests only; show them failing as predicted.
5. The fix; tests pass; then the revert check (do not commit the revert).
6. Terminal `2026-09-24-07`; all checks; PR; CI green.
7. STOP and report.

## Checks
Every commit passes, in `~/Zynergy/Claude-kit-fixes` (step 4 excepted for the new tests only):
- `python3 -m unittest discover -s .claude/hooks/tests -v`
- `python3 check_record.py --render-check`
- `python3 check_prompts.py --render-check`
- `python3 check_record.py` and `python3 check_prompts.py`
- `python3 -m unittest discover -s tests -v`
- `python3 release_check.py`
Write commit messages and record entries to files under `/tmp/kitv02_t1_*` first and use them from there; history_guard over-blocks commands that combine push with text it cannot parse (known, T12). Record any block as a deviation.

## Out of scope
T2-T16; the store-name rule; the untracked files in other harness worktrees; the main checkout (including pulling it); merging; tags; Forager.

## Device items
None gate this dispatch. Owner check after merge, not a gate: pull `main` in `~/Zynergy/Claude-kit` so new harness worktrees carry the T1 hook; the planner will then confirm with a pulse that two sessions draw distinct names.