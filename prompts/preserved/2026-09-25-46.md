HEAD: 671b05d75db5ba71fce947aac906114047aabd9a
Target subagent: pulse
Type: pulse
Preserved: 2026-09-25T23:21:47Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** pulse

# Role
You are the pulse for Claude-kit. Read-only: Read, Grep, Glob, and Bash limited to git and gh reads. Change nothing, run no tests, write no files. Answer with file:line evidence at the named commit; where you cannot verify, say "unverified".

# Base and state
- Read at origin/main 671b05d75db5ba71fce947aac906114047aabd9a (the PR #33 merge). Confirm with `git rev-parse origin/main` in your checkout and name the checkout you read from. If your checkout is not at 671b05d, say so and cite lines against `git show 671b05d:<path>` instead.
- The main checkout C is ~/Zynergy/Claude-kit; the fixes worktree is ~/Zynergy/Claude-kit-fixes.
- Context: a planner is preparing dispatches that fix findings from a review of history_guard.py, role_guard.py, update_worktree.py, run_exercise.py, session_agents.py, launch_session.py and their tests. The questions below are the premises those dispatches will assert. Every answer is a citation, not a summary.

# Rules
- Read-only. No test runs, no file writes, no `git fetch`.
- Cite `path:line` at 671b05d for every claim; quote the line or a short excerpt where the wording matters.
- Do not print long prompt files or planner logs. Do not open any file under ~/.claude/projects.
- If a question's premise is wrong, say so and give what is there instead.
- Keep the answer under 400 lines.

# Questions
1. **Hooks: subprocess calls and timeouts.** In `.claude/hooks/device_guard.py`, `.claude/hooks/session_check.py`, `.claude/hooks/dispatch_guard.py` and `.claude/hooks/history_guard.py`: list every `subprocess.run` (or Popen/check_output) call with its line and whether it passes `timeout=` (value if so). For device_guard, cite the `run()` helper and its timeout value.
2. **test_bypasses.py structure.** In `.claude/hooks/tests/test_bypasses.py`: the helper(s) tests use to send a payload (name, line, signature), how a row's status (`accepted`/`fixed`) is tied to its assertion, and the line ranges of `test_b01_push_through_wrapper_or_git_path`, `test_b02_interpreter_splits_words`, `test_b08_pulse_and_planner_read_outside_checkout`, `test_b09_pr_merge_through_curl`. For b08 and b09: which payloads carry `agent_type` (and which value) and which hook each assertion calls. Also cite the test that checks every "Known bypasses" citation is a row.
3. **test_history_guard.py fixtures.** In `.claude/hooks/tests/test_history_guard.py`: `run_hook` (or equivalent) signature and line; `init_repo` and class `RelativeDashC` lines; the merge-rule tests (m1..m6 or however named) with lines, and which one fetches before asserting freshness; the heredoc tests' lines. List every test whose assertion depends on a denial that matches words anywhere in the command text rather than at command position: for example a test that sends `sh -c 'git push --force ...'`, or `echo`/`grep`/heredoc text containing `git merge`, `gh pr merge`, `--force`, `filter-repo`, and expects a deny. Give test name, line, the command sent and the expected decision.
4. **test_role_guard.py.** In `.claude/hooks/tests/test_role_guard.py`: list every test that asserts a NAMED_PATTERNS denial (`git commit`, `git push`, `git checkout`, `git reset`, `git merge`, `rm`, `mv`, `sed -i`, `gradle`) with its line and the command sent. Note any whose command has the pattern's words somewhere other than as the command word and subcommand (for example inside a quoted argument).
5. **test_config.py and harness.py.** In `.claude/hooks/tests/harness.py`: how the hooks are copied and the test config written (lines), the test config's `protected_branches`, `agent_roles` and `backup_dir` values, and the default `cwd` a hook runs with.
6. **update_worktree.py.** Cite: the survey's untracked-file listing command (the `git ls-files` or `status` call) and its flags; the fast-forward merge call; the docstring lines that say ignored files are never touched and nothing is deleted; the README.md lines that repeat those promises (the "Updating a harness worktree" section and the Layout table row). In `tests/test_update_worktree.py`, say whether any test creates an ignored file (a `.gitignore` in the fixture) and cite it.
7. **run_exercise.py and its tests.** In `run_exercise.py`: the `judge` function's case-1 branch (lines), case-2 branch, and `subagent_ran` (lines); whether anything checks that the dispatch was saved under the worktree's `prompts/preserved/`. In `tests/test_run_exercise.py`: `completed()` fixture lines and what `is_error` it sets; `test_c_case2_call_ran_fails` lines and its assertions; `test_a_all_four_pass` lines and the assertion on `lines[3]`.
8. **session_agents.py and its tests.** In `session_agents.py`: the hand-back dedup key lines, the `results` first-per-id line, the line where the fallback returns the first line of the denial text. In `tests/test_session_agents.py`: `test_b_background_notice_handback_sorted_keys_wire_inputs` lines and `test_d` (or the test with two `completed` notices) lines; whether any test has several notices for one agent where the last differs from the first.
9. **launch_session.py and its tests.** In `launch_session.py`: the `pending` state lines (signal while starting), the "already stopping" branch, `exit_now` (signal after the leader exited), `group_left`, and the `os._exit` after `ended()`. In `tests/test_launch_session.py`: every assertion that bounds wall-clock elapsed time (line and bound); whether any test covers the pending, already-stopping or exit_now paths.
10. **README.md.** Line numbers at 671b05d of: the "Merging and undoing a merge" section start; the undo procedure's two bullets (revert; reset and force-push); the sentence "history_guard lets the merge through only ..."; the Layout row for `update_worktree.py`; the "Resuming after a disconnect" section start; the "Tests" section's count of test files.
11. **coder.md.** Line numbers at 671b05d of items 1, 3, 10 and of the Non-negotiables "Checks fail first" and "Report what happened".
12. **check_record.py.** Line numbers of `NOTE_OUTCOMES`, `TERMINAL_REQUIRED`, `_validate_note`, `_validate_continuation`, `check_duplicate_terminals`, the `total = 30` line in `render_check`, and the module docstring's "Claude-kit v0.2 addition" paragraph.
13. **Store and worktrees now.** `git -C ~/Zynergy/Claude-kit status --short` (list only the untracked entries under `prompts/preserved/`), `git -C ~/Zynergy/Claude-kit log -1 --format=%H`, `git -C ~/Zynergy/Claude-kit-fixes status --short` (count of entries only, plus the branch from `git -C ~/Zynergy/Claude-kit-fixes rev-parse --abbrev-ref HEAD`), `git -C ~/Zynergy/Claude-kit-fixes log -1 --format=%H%x20%s`, `git ls-remote origin kit-v0.2-fd-gc`, and `gh pr list --repo slayer8366/Claude-kit --state open --json number,title,headRefName`. Also `git worktree list` from C.
14. **CI check names.** From `gh pr checks 33 --repo slayer8366/Claude-kit` (or `gh run view` on the latest run), the exact names of the two CI check contexts as GitHub reports them (expected "python 3.8" and "python 3.14"; confirm or correct).