HEAD: 1599a53975b8f980035c594d52f7d125de768370
Target subagent: coder
Type: build
Preserved: 2026-09-25T16:56:34Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder finishing Claude-kit v0.2 T14-T16. The original coder did everything up to the fix and opened PR #25, then died of an API error. Follow `.claude/agents/coder.md` in ~/Zynergy/Claude-kit (now at origin/main 1599a53), including items 6, 7, 8, 10 and 11. The T14-16 dispatch is `prompts/preserved/2026-09-25-26.md` on kit-v0.2-t14-16. Read it in full: its scope, rules and Merge section carry over, except where this dispatch says otherwise.

# Base and state
Premises to verify (read-only) before you write anything:
- **Main checkout C** = ~/Zynergy/Claude-kit, on main at 1599a53975b8f980035c594d52f7d125de768370. It equals `git ls-remote origin main`. No tags. This session's hooks run from C, so history_guard has T17's merge check, and kit.json has `backup_dir` "~/forager-backups".
- **Untracked store files in C** (`prompts/preserved/`):
  - `2026-09-25-27.md`: the stopped dispatch. 7732 bytes, sha256 5a3538b2fe339ffe20f9385085d7f018bd824085cb8effe0f85b6d38e69e09b9, Preserved 16:31:54Z, HEAD cda554b. It stopped before writing anything, because update_worktree.py refuses a protected branch.
  - `2026-09-25-28.md`: the owner-requested `pull --ff-only`. 2871 bytes, sha256 d1bbaa48a3433bc4adc359f7b73ae40b410914086a75abc348a1b7b8548c59d9, Preserved 16:53:10Z. It printed "Already up to date.", with HEAD unchanged at 1599a53, and wrote nothing.
  - this dispatch, expected as `2026-09-25-29.md`. The hook's name governs.
  - `.claude/worktrees/` stays untouched.
- **kit-v0.2-t14-16**, in ~/Zynergy/Claude-kit-fixes with a clean tree, at 4497d61c8d3445cb5ec3f4f93169312666b588bd, equal to origin. It holds:
  - the sweep, d710a86
  - the copy and intent 2026-09-25-60, aa7737a
  - docs, 119f97a
  - tests-only, a94f354
  - the fix, 4497d61

  There is no terminal for -60. Before every push, fetch and confirm the origin branch is still where you last saw it. If anyone else has pushed, stop and report.
- **PR #25** (kit-v0.2-t14-16 -> main) is OPEN and MERGEABLE at 4497d61, with all four checks passing (runs 36153197791 and 36153318247).
- **The original coder's end:** the other planner's log (791cc457…jsonl, named in -26) records it at 15:26:35.561Z: "Agent terminated early due to an API error … (EAI_AGAIN)". Its last message was "Revert check matches; restored, 137 OK, tree clean. Opening the PR now so CI runs on the fix commit." Its hand-back does not exist.
- **This planner's log:** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit/2d9316b0-e46a-5c35-b7e4-125fac5c9a3e.jsonl. Cite by timestamp first.

# Scope boundary
All record work goes on kit-v0.2-t14-16, in order. Use the next free IDs, and let check_record.py and check_prompts.py decide the entry kinds.

1. **Sweep:** copy -27 and -28 into the store byte for byte.
   - -27 gets a dispatch-note with Outcome `stopped` (coder.md item 8), citing its hand-back from this planner's log.
   - -28 gets a dispatch-note for an executed build that opened no intent, if check_record allows one; otherwise the entry kind the checkers accept. Cite the pull's output.
2. **This dispatch:** its store copy, with a `continuation` entry continuing intent 2026-09-25-60, which is open. If check_record rejects that, use an intent instead and say why. Push.
3. **Terminal closing 2026-09-25-60, Outcome `completed`.**
   - Evidence you gather yourself:
     - the five commits
     - counts at 4497d61
     - a failing-first run at a94f354 (only the consistency test fails)
     - a revert check: 4497d61 without BYPASSES.md makes only the consistency test fail
     - CI on #25
     - candidates (1)-(6) as BYPASSES.md and test_bypasses.py record them
   - Deviations:
     - the original coder's death and the missing hand-back
     - the ID shift from -61
     - the -27 stop and the -28 pull
   - Report #25's merge in the hand-back, as 2026-09-25-37 did. Push, and wait for CI to go green on this commit.
4. **Merge.**
   - The coder.md item-10 backup, after `git fetch`, in the next free ~/forager-backups folder: main.bundle, merge.json (`pr` 25, branch main, sha origin/main), and MANIFEST.sha256.
   - Check it with `sha256sum -c`, `git bundle list-heads` and `git bundle verify`, and add one INDEX.md row naming the folder, #25 and the SHA.
   - Then `gh pr merge 25 --merge` as one Bash call with nothing else in it, run from cwd C.

You may write RECORD.md, the three store copies, the backup folder and INDEX.md. No guard, test, doc or config change.

# Closed decisions
The owner, in this planner's log, verbatim:
- "Agent is waiting for CI runs but is halted, so it can't check. So go ahead and pick up from the CI check." (16:17:33.724Z)
- "Have a coder run git -C ~/Zynergy/Claude-kit pull --ff-only origin main". Then "I won't authorize the send until you have a coder run that command". Then "Send it" for this dispatch, after the planner described this dispatch's steps.
- The Merge section of 2026-09-25-26: "authorised: this PR only (kit-v0.2-t14-16 -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup."

# Merge
authorised: PR #25 only (kit-v0.2-t14-16 -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup. The merge may run with cwd C and without `--repo`. Merge nothing else.

# Prediction
- **Checkers:** both PASS after each record commit, with -25 to -29 all claimed.
- **Counts at 4497d61:**
  - hook tests "Ran 137 tests" OK
  - tests/ "Ran 47 tests" OK
  - render checks 30/30 and 13/13
  - release_check PASS with 24 files
- **At a94f354:** 137 run, 1 failure (the consistency test).
- **Merge:** history_guard lets `gh pr merge 25 --merge` through. #25 goes to MERGED with first parent 1599a53, unless main has moved.

Put your mechanism prediction in the continuation or intent entry.

# Finish line and abort conditions
**Finish line:** pushed on kit-v0.2-t14-16, in order:
1. the sweep of -27 and -28
2. this dispatch's copy and entry
3. the terminal closing -60

Then CI green on the last commit, the backup verified, #25 merged with `--merge`, and the merge commit reported. No tag.

Abort and report on any of these:
- any Base-and-state mismatch other than the dispatch's name
- anyone else pushing to the branch
- a checker refusing an entry you can't make valid within scope
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- CI not green (don't merge)
- history_guard denying the merge after the backup (quote it)
- an owner message after this Agent call that tells you to do or not do something in this scope, or changes a decision here
- a planner message that widens the scope or changes a closed decision without quoting an owner ruling

# Checks
- Both checkers before every commit that touches RECORD.md. Read the planner log in a separate call before each such commit.
- Tests in their own Bash call.
- Counts, the failing-first run and the revert check.
- The backup evidence, and the merge commit with its parents.

# Out of scope
- T6-T8.
- D2 and D5.
- W and the other worktrees.
- Other PRs, tags, closing any bypass, deleting anything.

# Device items
None.