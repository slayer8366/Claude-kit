HEAD: e4cfab25c98ac87bd7a556a2900a4b9acf0581cc
Target subagent: coder
Type: build
Preserved: 2026-09-25T20:50:57Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit v0.2 fix batch 3. It covers the two findings from T8's terminal (2026-09-25-78). Follow `.claude/agents/coder.md` in ~/Zynergy/Claude-kit, including items 1, 6, 8, 10 (with its last step) and 11.

# Base and state
Premises to verify:
- **origin/main** is e4cfab25c98ac87bd7a556a2900a4b9acf0581cc (PR #29 merge, T8), with no tags. The record's last entry is 2026-09-25-78, and no intent is open.
- Create `kit-v0.2-batch3` from origin/main in ~/Zynergy/Claude-kit-fixes, and push with `git -C ~/Zynergy/Claude-kit-fixes push -u origin kit-v0.2-batch3`.
- **C** is ~/Zynergy/Claude-kit, on main at e4cfab2, with only `.claude/worktrees/` untracked, plus this dispatch's copy (expected `-40.md`; the hook's name governs). No sweep is needed.
- **Planner log B** is /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit/2d9316b0-e46a-5c35-b7e4-125fac5c9a3e.jsonl. Cite by timestamp and tool_use id.
- **Finding 1 (T8 terminal -78):** under `--permission-prompts none`, an Agent call that dispatch_guard sent to approval ("ask") and that nobody answered logs `is_error` with `toolDenialKind` "permission-rule", the same as a hook block. The result text reads "Error: Permission for this tool use was denied. It requires approval, and this session has no approval surface…". Other cases:
  - A hook block reads "Error: PreToolUse:Agent hook error: dispatch_guard: …".
  - A user deny reads "Error: Denied by user" (planner log B had one).
  - session_agents.py reports all of these as `denied: <first line>`.
  - The T8 evidence is in the case logs, whose paths are in the T8 terminal and evidence.txt. The "ask" decision is an `attachment` `hook_success`, `hookName` "PreToolUse:Agent", `toolUseID`, and `stdout` holding the decision JSON.
- **Finding 2 (T8 terminal -78):** role_guard lets the pulse (and the planner) use Read, Grep and Glob on any path. PULSE_TOOLS and PLANNER_TOOLS are at role_guard.py:61-66, with no path check. In T8 case 1 the pulse read its worktree's `.git` and tried the main clone's `.git/worktrees/…/HEAD`. Claude Code's permission check denied that, not role_guard.
- **BYPASSES.md** (`.claude/hooks/BYPASSES.md`) has rows B-01 to B-07, all `open`. Proving tests are in `.claude/hooks/tests/test_bypasses.py`, with a consistency test. Each guard's docstring has a "Known bypasses:" line.

# Scope boundary
**Fix 1: session_agents.py tells denials apart.** An Agent call's `denied` outcome becomes one of:
- `denied: approval not given`, when a PreToolUse:Agent `hook_success` for that toolUseID carries decision "ask" and the result is an error;
- `denied: blocked by <guard>`, when the text has "hook error: <guard>:" or starts "<guard>:"; name the guard;
- `denied: by user`, when the text is "Denied by user";
- otherwise `denied: <first line>`, as today.

The summary still counts all of these under "denied". The module docstring states the rule. README "Blocked calls in the session log" gets these additions:
- `toolDenialKind` "permission-rule" also marks an unanswered approval under `--permission-prompts none` (T8's case 2);
- only dispatch_guard's "ask" decision tells the two apart;
- session_agents.py shows which.

**Fix 2: record, don't restrict.**
- Add a row B-08 to BYPASSES.md, Status `open`. Guard: role_guard. Bypass: the pulse and planner roles may Read, Grep and Glob any path, including outside the checkout; only Claude Code's own permission check limits where. Evidence: T8 case 1.
- Add `test_b08_…` to test_bypasses.py, proving role_guard returns no decision for a pulse Read and a planner Read of a path outside the checkout (for example `/etc/hostname`), and a pulse Grep and Glob with such a path. Use hook payloads only.
- Add B-08 to role_guard's "Known bypasses:" docstring line.
- No change to role_guard's behaviour.

**Tests-only commit** (first):
- New tests in tests/test_session_agents.py, using hand-built fixture logs:
  - (p) an ask decision, then the no-approval-surface error, gives `approval not given`;
  - (q) a dispatch_guard block gives `blocked by dispatch_guard`;
  - (r) a role_guard block gives `blocked by role_guard`;
  - (s) "Denied by user" gives `by user`;
  - (t) the summary counts all four as denied.
- test_b08 in test_bypasses.py.

**Record:**
1. this dispatch's copy and intent
2. the tests-only commit
3. the fix: session_agents.py, README, BYPASSES.md, role_guard.py's docstring line only
4. the terminal

Then the PR, the merge, and item 10's update of C.

# Closed decisions
- **The owner in B, verbatim:** "Do the two T8 findings as small tasks, then the bypass-table rulings".
- **The owner's AskUserQuestion answer in B:** "Record it, don't restrict (Recommended)". Quote the option text from the tool_use.
- **Planner's choices:** the outcome wording, and folding both findings into one batch.
- **B-11/B-13:** coders merge at the planner's discretion.

# Merge
authorised: this PR only (kit-v0.2-batch3 -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup. Run it from cwd C without `--repo`, as one Bash call. If it is denied, quote the denial and stop. Merge nothing else. Then do item 10's update of C.

# Prediction
**Tests-only commit:**
- (p), (q), (r) and (s) fail, since today's outcome is `denied: <first line>`.
- (t) passes, because the counts are unchanged.
- test_b08 passes (it proves today's behaviour).
- The consistency test still passes, since no docstring cites B-08 yet.
- Report the actual counts.

**After the fix:**
- all tests/ and hook tests OK
- render checks 30/30 and 13/13
- both checkers PASS
- release_check PASS with 27 files and no denylist hit

**Revert check:** reverting session_agents.py brings back the same four failures. Separately, adding the B-08 docstring citation without the BYPASSES.md row fails the consistency test. Show both.

**Live check:** run session_agents.py on T8 case 2's log; it prints `denied: approval not given`. On cases 3 and 4 it prints `blocked by dispatch_guard`. If those logs are gone, say so.

Put your mechanism prediction in the intent.

# Finish line and abort conditions
**Finish line**, pushed on kit-v0.2-batch3:
1. the copy and intent
2. the tests-only commit
3. the fix
4. the terminal

Then CI green on the final commit, the backup, the merge with `--merge`, and the merge commit reported. After that, C updated by item 10's last step. No tag.

**Abort and report on:**
- any Base-and-state mismatch other than the dispatch's name
- a needed change to any guard's behaviour
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit
- CI not green (no merge)
- the merge denied by anything (quote it)
- the dry run or `--apply` exiting 1
- an owner message after this Agent call that tells you to do or not do something in this scope
- a planner message that widens the scope without quoting an owner ruling

# Checks
- The failing-first run and both revert checks.
- Counts before and after.
- Both checkers before every commit touching RECORD.md; read B in a separate call before each.
- Tests in their own Bash call.
- The live-check output.
- The backup evidence and the merge commit's parents.
- The update of C's outputs.

# Out of scope
- Restricting any role's read paths.
- Ruling on any bypass row (the owner does that next).
- D2 and D5.
- Other PRs, tags, and deleting anything.

# Device items
None.