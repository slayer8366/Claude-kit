HEAD: 93904076a95498b3a877438f6d354fc1b1b88419
Target subagent: coder
Type: build
Preserved: 2026-09-23T23:21:01Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Claude-kit v0.1 fixes: record the live exercise, restore Claude-kit, report, PR

## Role
You are the coder. You execute this dispatch, you make no design decisions, and a gap is a stop-and-ask. Record it as `coder.md` and the checkers require (in the fix worktree's RECORD.md), and run `check_record.py` and `check_prompts.py` before every commit that touches `RECORD.md`.

## Base and state
- `~/Zynergy/Claude-kit-fixes`: branch `kit-v0.1-fixes`, HEAD = `origin/kit-v0.1-fixes` = `0e7f5f9`, clean. Store holds `2026-09-23-01` to `-04`, all tracked. Open intents: 2026-09-23-04 and 2026-09-23-06.
- `~/Zynergy/Claude-kit`: detached HEAD at `0e7f5f9`. Untracked: `.claude/worktrees/`, `prompts/preserved/2026-09-23-05.md` (Target pulse, Type pulse, preserved 2026-09-23T23:05:56Z) and `prompts/preserved/2026-09-23-06.md` (Target coder, Type build, preserved 2026-09-23T23:13:35Z), both saved by `dispatch_guard.py` with `HEAD: 0e7f5f9…`.
- Harness worktree `~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01JDbTJ4Gj9C6xygXM5tPdHz/prompts/preserved/` (untracked, v0.1 hook): `-02` (stopped continuation, cited under R5), `-03` and `-04` (copied as fix `-03`, `-04`), `-05` (stopped launcher, sha256 `009446d4672d76fbdc3a93848b74715ef13c21be4021beb92115c4b121072c8c`), `-06` (stopped tmux launcher, sha256 `d4a0a5648ad2bf744df52e0654b98477493930a01f24026914cacc769277f35a`), and this dispatch as the next file (expected `-07`; identify by content and header time).
- Processes: PID 100263 `claude remote-control --help` (hung stray, started by a coder). PID 101054 `claude --rc Kit live exercise`, cwd `~/Zynergy/Claude-kit` (the owner's exercise session). Other `claude` processes are the owner's; leave them alone.
- `origin/main` = `9390407`. No tags.
Verify all of this first; if anything differs, stop.

## Scope boundary
Record the live exercise and the launcher attempts, end PID 100263, return `~/Zynergy/Claude-kit` to `kit-v0.1`, write the completion report and the terminals, open the PR, get CI green. Nothing else. No merge, no tag. Never touch `.claude/worktrees/*` (reading is fine) or Forager. Do not edit hooks, `kit.json` or tests: the code is final at `0e7f5f9`. Delete nothing anywhere.

## Closed decisions
Owner, 2026-09-23: the live exercise ran in `~/Zynergy/Claude-kit` (owner ruling "The whole test goes in Claude-kit"), in the session "Kit live exercise", which the owner started by hand. Results as observed by the owner and the planner:
1. Pulse → `pulse`: ran without an approval prompt; report returned (HEAD `0e7f5f9…`; `PULSE_TOOLS` at `role_guard.py:42` includes `SubagentHandback`).
2. Build → `coder`: "Allow Claude to Agent?" appeared for "Live exercise 2 build"; the owner pressed Deny; the session got "Denied by user."; no agent started.
3. `Type: pulse` → `coder`: `PreToolUse:Agent hook error: dispatch_guard: Type 'pulse' may only be dispatched to 'pulse', not 'coder'.`
4. → `general-purpose`: `PreToolUse:Agent hook error: dispatch_guard: subagent type 'general-purpose' may not be dispatched. Only coder, pulse may; built-in agent types are blocked.`

Therefore:
- H1, evidence: find the exercise session's log under `~/.claude/projects/` (the session whose messages contain "Live exercise 1: pulse hand-back", cwd `~/Zynergy/Claude-kit`) and the pulse subagent transcript under it. Quote each hook decision for the four cases from the log, verbatim, with file path and line number. For case 2, quote the dispatch hook's own "ask" decision and reason, showing that the prompt the owner declined came from the hook. If the log doesn't show a hook "ask" for case 2, say so plainly; do not infer it.
- H2, backup, before any commit: to a new `~/forager-backups/<UTC date>-<next free NN>/`, copy that session log and its subagent transcripts, plus every `/tmp/kitfix_*` path created after backup 2026-09-23-04 (e.g. `kitfix_c04_*`, `kitfix_saved/c06_item4b/`, `kitfix_rc*`, `kitfix_manifestcheck.*`, `kitfix_msg_c06_*`, `kitfix_wt_before.txt`, `kitfix_kit_status_before_checks.txt`) and the hung process's task output file named in the launcher coder's report if it still exists. `MANIFEST.sha256`, verified on both sides; one line appended to `~/forager-backups/INDEX.md` (append-only).
- H3, record:
  - Copy `~/Zynergy/Claude-kit/prompts/preserved/2026-09-23-05.md` and `-06.md` byte for byte into the fix store under the SAME numbers (`-05`, `-06`), sha256 on both sides. Each is claimed by a dispatch-note entry. Pick the outcome (`answered`, `declined`, `exercise`) by the definitions in `coder.md` and `check_record.py`. If more than one fits either file, stop and ask.
  - Copy this dispatch (harness `-07`, expected) byte for byte into the fix store as `2026-09-23-07.md`. A new intent claims it (next free entry number).
  - That intent cites the two stopped launcher dispatches (harness `-05` and `-06`, hashes above) by path and hash, not copied, as R5 did. It records as observations: the owner's failed first exercise attempt (app sessions spawned as worktrees at `9390407`; prompts answered by the planner; the mismatch rerouted to `pulse`; that pulse without a report); the hung `claude remote-control --help`; tmux not installed; and the owner starting "Kit live exercise" by hand.
  - It lists as deferred to v0.2, not built: the four ruling items; R6 (a)-(c); a session-start check that warns when a session's checkout or hooks differ from what's expected; a live-exercise runner; a session launcher with a time limit, a check for the expected session, and cleanup on failure; a tool for recording hook-saved dispatches; a way to send a prompt to a named agent unchanged; and whether the planner may use TaskStop.
- H4: end PID 100263. First confirm `/proc/100263/cmdline` is still `claude remote-control --help`; if not, don't signal it and report. SIGTERM; after 10 seconds, SIGKILL if it's still alive. Confirm it's gone.
- H5: before returning `~/Zynergy/Claude-kit`, confirm PID 101054 has exited. If it is still running, do not switch, do not signal it, and do not write intent 06's terminal. Do everything else and report that the restore is waiting for the owner to close the session. If it has exited: `git -C ~/Zynergy/Claude-kit checkout kit-v0.1`, then confirm HEAD is `a544ca9` on `kit-v0.1`. The untracked `prompts/preserved/2026-09-23-05.md` and `-06.md` stay there, untouched. Note in the report that the checkers at `kit-v0.1` in that checkout may flag them as unclaimed.
- H6, terminals:
  - Intent 04: `completed`, finish line met (items 3, 1, 2, 6, 4 and the live exercise).
  - Intent 06: `completed` once the restore is done (see H5).
  - The new intent: `completed` at the end.
- H7, completion report at `docs/audits/2026-09-23-kit-v0.1-fixes-completion-report.md`, in the shape of the v0.1 extraction report. It covers:
  - every commit with its hash;
  - the failing-first and sabotage evidence per item (from the commit messages and the c04/c06 re-runs);
  - the H1 quotes;
  - old and new test counts;
  - the behaviour diff output;
  - what was not tested: role_guard's unknown-agent deny is proven by unit tests only, because dispatch_guard blocks first; and the live "unknown agent" was a built-in (`general-purpose`), per D7;
  - the flags raised along the way: the pulse's line-197 guess, which the planner refuted by reading lines 190-197; history_guard blocking heredoc commit messages; the one `publickey` push failure;
  - the full v0.2 list from H3.
- H8, PR `kit-v0.1-fixes` → `main`, body per the repo's PR template if one exists. CI green on the final commit. No merge, no tag.

## Prediction
- H1: the log shows `allow` (or no decision) for case 1, `ask` with a reason naming Type `build` for case 2, and `deny` for cases 3 and 4 with the texts above.
- H3: both checkers pass with the new entries.
- Test counts unchanged from `0e7f5f9`: hook tests 83, install and release tests 18.
- Behaviour diff: 49 source tests; the only failures are the two Grep/Glob ones.

## Finish line and abort conditions
Finish: H1-H8 done (H5 and intent 06's terminal possibly waiting on the owner, as above), PR open, CI green on its final commit, then STOP and report with the PR URL. Abort on: any Base-and-state mismatch; any change needed to hooks, `kit.json` or tests; any behaviour-diff failure beyond the two expected; a manifest or sha256 mismatch; two failed fixes on one CI symptom (then gather data, not a third hypothesis).

Steps:
1. Verify; H2 backup.
2. H1 evidence.
3. H3 record; run all checks; commit; push.
4. H4.
5. H5.
6. H6 terminals and H7 report; `python3 workshop/behaviour_diff.py --source ~/Zynergy/Forager`, full output pasted; run all checks; commit; push.
7. H8 PR; CI green.
8. STOP and report.

## Checks
Every commit passes, in the fix worktree:
- `python3 -m unittest discover -s .claude/hooks/tests -v`
- `python3 check_record.py --render-check`
- `python3 check_prompts.py --render-check`
- `python3 -m unittest discover -s tests -v`
- `python3 release_check.py`

## Out of scope
Building any v0.2 item, merging the PR, any tag, removing harness worktrees or their untracked files, the owner's other `claude` processes, and Forager.

## Device items
None.