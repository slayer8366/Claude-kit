HEAD: 3847590469634ed45759e429ce7900be07a4d169
Target subagent: coder
Type: build
Preserved: 2026-09-25T11:11:46Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit v0.2 task T10 (`docs/specs/2026-09-24-kit-v0.2-tasks.md` line 17). The planner can re-send a stored dispatch unchanged to a named agent. The hook records it as a re-send, and the checker verifies it is identical. Follow `.claude/agents/coder.md` at your base, including item 10 for the merge.

# Base and state
Premises to verify:
- **main.** origin/main is 905ac6415eb1551fc088cfa2235591bc2bc55f45 (PR #23 merge, the backlog). Nothing is tagged. The record's last entry is 2026-09-25-56; no intent is open. Create `kit-v0.2-t10` from origin/main in ~/Zynergy/Claude-kit-fixes.
- **Planner worktree W** (~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB, at 3847590). Your hooks run from W, and your Bash cwd resets to W. Its untracked store files:
  - D2 `-02` and D5 `-05`: not on main
  - `-21`, `-22` and `-23`: on main
  - this dispatch, expected as `2026-09-25-24.md`. If the hook gave another name, that name governs.
- **Code** (verify, with lines):
  - dispatch_guard.py `preserve` writes the header (HEAD, Target subagent, Type, Preserved, delimiter) and the text, and returns the path.
  - `guard` puts the saved path into its allow/ask reason.
  - check_prompts.py's `store_name_errors` reads headers up to the delimiter.
- **Planner log.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl. Cite by timestamp first.

# Scope boundary
Files:
- **`.claude/hooks/dispatch_guard.py`:** the re-send rule, with its docstring point.
- **`.claude/hooks/tests/test_dispatch_guard.py`:** tests r1-r4.
- **`check_prompts.py`:** the re-send check, its docstring paragraph, and render-check cases p11-p13 with `total` updated.
- **`.claude/agents/coder.md`:** item 11 (text below).
- **README.md:** one sentence where it describes the dispatch hook.

Record:
- this dispatch's copy and intent 2026-09-25-57
- the tests-only commit, the fix, and terminal 2026-09-25-58

**After the merge:** run `python3 <fixes checkout>/update_worktree.py <W literal path>` (a dry run), then with `--apply`, so the planner's session gets the new hook. This changes nothing else in W.

No change to other hooks, guardlib, kit.json, the release set beyond the edited files, install, find_dispatches, or update_worktree.

# Closed decisions
The owner, in the planner log. Cite by timestamp:
- **The T10 AskUserQuestion answer** "Re-send a stored dispatch (Recommended)". Quote its option text: "The planner re-dispatches a dispatch already in the store, byte for byte, to a named agent (coder or pulse). dispatch_guard recognises a prompt identical to a stored dispatch's text and saves the new copy with a `Resend-of: <file>` header line, so the record shows it is the same dispatch, not a new one. This covers lost sessions and stopped coders that can't be resumed."
- **"do 1 and 2 together sequentially, then 3"** (10:25:43Z).

The planner's rules:
1. **dispatch_guard.** After every existing check passes, and before writing, look for a file under this worktree's `prompts/preserved/` (tracked or untracked) named `YYYY-MM-DD-NN.md` whose text after its first delimiter line equals the new prompt text exactly (bytes after UTF-8 encoding).
   - If found, the new copy's header gets one more line, `Resend-of: preserved/<name>`, before the delimiter. Name the earliest match by (date, number).
   - The allow/ask reason adds "re-send of preserved/<name> (identical text)".
   - Numbering, approval and blocking are otherwise unchanged. A near-match gets nothing.
2. **check_prompts.** For every file under `preserved/` whose header has a `Resend-of:` line, the named file must exist under `prompts/` and have identical text after its delimiter. Otherwise it is an error naming both. This applies to all files, whatever the cutoff.
3. **coder.md item 11,** verbatim: "11. **Re-sends.** A store copy whose header carries `Resend-of:` is the named dispatch sent again, unchanged. If that dispatch's intent is still open, record the re-send with a continuation (item 7) citing both copies; otherwise record it as its own dispatch, citing the original."

# Merge
authorised: this PR only (kit-v0.2-t10 -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup. The merge command may run with cwd W and without `--repo`. Merge nothing else.

# Prediction
- **Tests-only commit:**
  - dispatch_guard tests:
    - r1: identical text to a stored file → the header has `Resend-of: preserved/<that>`
    - r2: one byte different → no `Resend-of`
    - r3: two identical stored files → the earlier is named
    - r4: the reason says "re-send of"

    r1, r3 and r4 fail today; r2 passes. Hook tests: 129 run, 3 failures.
  - check_prompts render cases:
    - p11: a valid re-send passes
    - p12: text mismatch → error
    - p13: the named file is missing → error

    p12 and p13 fail today; p11 passes. Result: 2 of 13.
  - test_install's vendored-hook-tests case fails with the same 3 failures, possibly cut off. Otherwise tests/ is 47.
- **After the fix:** 129 OK and 47 OK. Render checks 30/30 and 13/13, both checkers PASS on the real store (no file has `Resend-of` yet). release_check PASS with 22 files.
- **Revert check:** dispatch_guard.py and check_prompts.py from base, with the tests kept, give the same failures.
- **After the merge and the W update:**
  - W's HEAD equals origin/main.
  - W's dispatch_guard contains the rule.
  - The helper moves `-21` to `-24`, which are on main then, and leaves D2 and D5.

Your mechanism prediction goes in the intent.

# Finish line and abort conditions
Finish line:
1. the copy and the intent
2. the tests-only commit
3. the fix
4. the terminal, all pushed on kit-v0.2-t10
5. the PR with CI green, the merge under the Merge section, with the merge commit reported
6. W updated with the helper, with its output reported

No tag.

Abort and report on any of these:
- any Base-and-state mismatch other than the dispatch's name
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a needed change outside scope
- a denylist hit
- CI not green: don't merge
- history_guard denying the merge after the backup: quote it
- the helper's dry run exiting 1: don't apply; report

Owner messages after this dispatch's Agent call: an abort only if one tells you to do or not do something in this scope, or changes a decision here. Record every other message in the terminal. A planner message follows coder.md item 6.

# Checks
- Failing-first run and revert check.
- Counts before and after.
- Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
- Tests in their own Bash call.
- The merge's backup evidence.

# Out of scope
- D2 and D5.
- The end-to-end re-send itself: the planner does that after you hand back.
- T6, T7, T8 and T14-T16.
- Other PRs, tags, deleting anything.

# Device items
None.