HEAD: 671b05d75db5ba71fce947aac906114047aabd9a
Target subagent: coder
Type: build
Preserved: 2026-09-25T23:55:36Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit review fix R1: the pre-main branch model, the record's `merge` and `revert` entry kinds, the terminal outcome `partial` with deferrals, and the plan document. Follow `.claude/agents/coder.md` in ~/Zynergy/Claude-kit, including items 1, 2, 5, 6, 8, 10 and 11. This build changes the rules in coder.md item 10 and adds an item 12; you follow the rules as they stand at the base for everything up to your terminal, and the new rules for the terminal itself (see Finish line).

# Base and state
Premises to verify before relying on any of them. Line numbers are at 0319ccd; treat drift as drift, not as a mismatch.
- **origin/main and origin/pre-main** are both 0319ccd7a8818dbabec7faa2f67c31cde349e11f (the PR #34 merge). No tags. `pre-main` was created by the owner from that commit. GitHub protects both branches: pull request required, zero reviews, enforced for admins, required checks `python 3.8` and `python 3.14`.
- **Branch:** create `kit-review-r1` from origin/pre-main in ~/Zynergy/Claude-kit-fixes (currently on kit-v0.2-fd-gc at 3b584dc, clean). Push with `git -C /home/zynergy-labs/Zynergy/Claude-kit-fixes push -u origin kit-review-r1`.
- **C** is ~/Zynergy/Claude-kit, now on `pre-main` at 0319ccd, with only `.claude/worktrees/` untracked.
- **The record** at 0319ccd ends with terminal 2026-09-25-89 (closing -88, Outcome abandoned). No intent is open. Every terminal Outcome in the record is one of `completed`, `superseded`, `abandoned`; every dispatch-note Outcome is one of `answered`, `exercise`, `stopped`. Verify with a grep before adding the outcome set below.
- **The planner's worktree W** is /home/zynergy-labs/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01RGmfuBWv4x1Nvnpa3fBQDZ, on branch `worktree-bridge-cse_01RGmfuBWv4x1Nvnpa3fBQDZ` at 671b05d (one merge behind). The planner of this build runs there, so the dispatch hook saved copies there, not in C: `prompts/preserved/2026-09-25-46.md` (a pulse, answered: "Premises for review-fix dispatches") and this dispatch's copy (expected `2026-09-25-47.md`, or `2026-09-26-01.md` if saved after 00:00 UTC; the hook's name governs). Both are shared-counter names and free in the store. Copy each byte for byte from W and keep the hook's name; the intent records path, size and sha256 as -88 did.
- **Planner log F** is /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-01RGmfuBWv4x1Nvnpa3fBQDZ/d5592fbe-c371-5027-aaf9-18ab19a5613c.jsonl. Read it in a separate call before each record commit; do not print its long lines.
- **check_record.py** at 0319ccd: `TERMINAL_REQUIRED` :85, `NOTE_OUTCOMES` :120, `_validate_note` :126, `_validate_continuation` :163, `validate_entries` :258 (terminal outcome checks at :336-342), `check_duplicate_terminals` :428, `render_check` :739, `total = 30` :1236, docstring paragraph "Claude-kit v0.2 addition" :54-66. No terminal outcome set exists today: any Outcome text is accepted for a terminal.
- **coder.md** at 0319ccd: item 1 :40-43, item 3 :48-50, item 10 :80-95, Non-negotiables heading :102, "Checks fail first" :104-105.
- **README.md** at 0319ccd: Layout row for update_worktree.py :22 (still says "fast-forwards a harness worktree", flagged in -74); "Merging and undoing a merge" :119-151; "Updating a harness worktree" :183-207.
- **kit.json**: `protected_branches` is `["main"]`. session_check, launch_session, run_exercise and update_worktree all use the first protected branch.
- **Merges to backfill:** `git log --merges --first-parent 8b1c88a..origin/pre-main` lists 17 merge commits: PRs #17, #19 to #34 (there is no #18 merge). B-13's Evidence in docs/standing-rulings.md names the backups for #17, #19, #20, #21; later terminals cite their backup folders; ~/forager-backups/INDEX.md has one line per merge backup naming `#N`.
- Counts at 0319ccd: hook tests "Ran 147 tests" OK; tests/ "Ran 94 tests" OK; render checks 30/30 and 13/13; release_check 27 files.

# Scope boundary
Files: `check_record.py`, `.claude/kit.json`, `.claude/agents/coder.md`, `README.md`, new `docs/specs/2026-09-26-review-fixes-plan.md`, `RECORD.md`, and the two store copies. Nothing else changes. No hook code changes.

1. **check_record.py.**
   - New entry Kind `merge`. Required fields: Kind, ID, Timestamp, PR (digits), Head (branch merged), Base (branch merged into), Merge-commit (40 lowercase hex), Pre-merge (40 lowercase hex, the merge commit's first parent), Backup (a folder name under backup_dir, or `none`), Merged-by (`coder`, followed by the dispatch's store file, or `owner`), Carries (free text naming the record entries the PR carried, or `record only`). Forbidden: Closes, Superseded-by, Outcome, Finish line, both prediction fields. Merge-commit and Pre-merge must differ.
   - New entry Kind `revert`. Required: Kind, ID, Timestamp, Reverts (the ID of a `merge` entry earlier in the file), Revert-commit (40 hex), PR (digits, the revert's own PR), Reason, Decided-by. Same forbidden list. Reverts naming a non-merge entry, an unknown ID, or a later entry is an error, checked by position as `_check_continues` does.
   - Terminal outcomes become a set: `completed`, `partial`, `superseded`, `abandoned`; anything else is an error naming the entry and the value. `partial` requires a non-empty `Deferred` field; `completed` carrying `Deferred` is an error.
   - `NOTE_OUTCOMES` gains `merged`: a merge dispatch's copy, which opens no intent.
   - Render checks, added first as the failing tests: a well-formed merge entry accepted and counted as opening and closing nothing; a merge entry missing a required field, naming the field; Merge-commit not 40 hex; a merge entry carrying Closes; a revert naming an intent, an unknown ID and a later merge entry (one check, three cases); `partial` without Deferred; `completed` with Deferred; an unknown terminal outcome; a note with Outcome `merged` accepted. Update `total`, the docstring, and the merge and revert helpers alongside `_minimal_note`.
2. **kit.json.** `protected_branches` becomes `["pre-main", "main"]`. Nothing else.
3. **coder.md.** Keep the numbering; the wording is yours, the rules are these.
   - Item 1, the sweep, also writes: one `merge` entry for each merge commit on the first-parent chain of each protected branch newer than the newest `merge` entry with that Base, with the fields above from `git log`, `gh pr view N --json number,headRefName,baseRefName,mergeCommit,mergedAt`, and backup_dir's INDEX.md (Backup `none` and Merged-by `owner` when no INDEX line names `#N`); and one dispatch-note with Outcome `merged` for each store copy of a merge dispatch.
   - Item 3: the four terminal outcomes. A build's finish line ends at the PR open, CI green on its final commit, the backup written and the terminal pushed; the merge and the checkout updates come after the terminal and are recorded by the next sweep's `merge` entry, so `completed` is true when written.
   - Item 10: two merge kinds. (a) A build's PR into the first protected branch, authorised by the build dispatch's `Merge` section. (b) A promotion PR from the first protected branch into a later protected branch, authorised only by a merge dispatch sent after the owner has read the evidence; its copy is claimed by a `merged` note. Both need the backup as today, of `origin/<base>`. After the merge, update the main checkout as today, and any other checkout the dispatch names. Leaving work out before a promotion is a revert PR: `git revert -m 1` of the feature merge on a branch from the first protected branch, RECORD.md and prompts/preserved restored from HEAD (the record is append-only), a `revert` entry, its own PR and backup.
   - New item 12, deferral. Only when the dispatch's Finish line says deferral is allowed. An item whose check fails while every other check passes without it may be deferred: revert that item's commits on the feature branch with `git revert` (the reverted commits stay pushed), give the terminal Outcome `partial` and a `Deferred` field naming the commits and the failing check, and list every deferral in the hand-back under a heading "Deferred". Never for the predicted failure of a tests-first commit, never when the rest depends on the item, and never in place of an abort condition.
   - Non-negotiables: add "A met abort condition is a stop. It is not a prediction miss, a counting miss or a deferral."
4. **README.md.** A short "Branches" section: the first protected branch is the working trunk that builds merge into; later protected branches are release trunks that receive promotion PRs after the owner's evidence gate; leaving out by revert; deferral. Update "Merging and undoing a merge" for the two merge kinds and the `merge` entry; fix the Layout row at :22 to say it fast-forwards a harness worktree or the main checkout. Leave the sentence about what history_guard proves for R4.
5. **The plan document** `docs/specs/2026-09-26-review-fixes-plan.md`: the text under "Plan text" at the end of this dispatch, verbatim, under a title line and a first line "Written by the planner on 2026-09-25/26; committed by R1's coder from dispatch <this copy's store name>."
6. **The record**, in this order: the sweep (a note for -46, Outcome `answered`, plus the 17 backfilled `merge` entries, which need the new checker, so the sweep's merge entries go in the fix commit's record change, and the -46 note goes first as usual); this copy and the intent; the tests-first commit; the fix; the docs commit; the terminal. Put the planner's outcome prediction below in the intent's `Prediction (outcome — planner)` field verbatim, not the sentinel.

# Closed decisions
From planner log F, the owner, verbatim. Cite each by its timestamp and line in F.
- "Answer is B. Maybe set up a pre-main in the specific branch, to act as a dummy main so that all work is reversible. Only after passing the evidence gate does it qualify for main. The owner can choose to leave out parts of the code that doesn't qualify and pass the rest, if the passed code doesn't depend on the failed code. A coder can make a similar choice, but in a non destructive way, to where the non qualified code is deferred to a list to present to the owner at the end of the run"
- "Done. And go, I'll take your recommendations" — the recommendations, from the planner's message before it: leaving out by revert on pre-main; pre-main protected in kit.json with a backup per merge; deferral only when the dispatch says so; the three B-01 forms move to `fixed` in R2/R3; R4 uses an ls-remote freshness check; the tag waits until R4 is promoted.
Option B, from the planner's plan in F: coders merge, the human gate is a merge dispatch approved after the PR exists, GitHub protection on the branches.
Planner's choices: the field names above; the set of four terminal outcomes; `merged` as a note outcome; backfill from PR #17; the plan document's path.
Not standing: B-13 stays a Part B proposal; do not edit docs/standing-rulings.md.

# Merge
authorised: this PR only (kit-review-r1 -> pre-main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup of origin/pre-main. Run it from cwd C without `--repo`, as one Bash call. If it is denied, quote the denial and stop. Then update C with the merged update_worktree.py (dry run, then `--apply`), and then W the same way (dry run, then `--apply`; W is a harness worktree on an unprotected branch, and its two store copies are identical to pre-main's after the merge, so they are moved aside). Report all four outputs.

# Prediction
Planner's outcome prediction (goes in the intent's planner field):
- Tests-first commit: `python3 check_record.py --render-check` prints "FAIL: 9 of 39 checks failed", naming exactly the nine new checks; the 30 existing checks pass. Hook tests 147 OK and tests/ 94 OK, unchanged. CI on that commit fails at "Checker self-tests" in both jobs.
- After the fix: 39 of 39; `check_record.py` on the real record PASS with 17 new merge entries and no unterminated intent; check_prompts PASS; hook tests 147 OK; tests/ 94 OK (the kit.json order change touches no test); release_check PASS, 27 files, no denylist hit.
- Backfill: 17 `merge` entries, #17 and #19 to #34; those with an INDEX.md line get Merged-by `coder` and a Backup folder; the rest `owner` and `none`. I expect #22 to #24 and #17 to #21 all to have backups except where a terminal says otherwise; report the split.
- Revert check: check_record.py from 0319ccd over the new render checks gives the same nine failures.
Put your mechanism prediction in the intent.

# Finish line and abort conditions
Finish line, pushed on kit-review-r1: (1) the sweep note for -46; (2) this copy and the intent; (3) the tests-first commit; (4) the fix, with the 17 merge entries; (5) kit.json, coder.md, README and the plan document; (6) the terminal, Outcome `completed`, written under the new rule: it closes at the PR open, CI green on the final commit and the backup written. Then the merge and the two updates, reported in the hand-back only. No tag. Deferral is not allowed in this build.

Abort and report on any of:
- a Base-and-state mismatch other than the copies' names or line drift
- any existing test or render check failing after the fix
- any existing record entry failing the new checks (report it; do not loosen the check)
- a needed change to any hook's code
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit
- CI not green on the final commit (no merge)
- the merge denied by anything (quote it)
- any dry run or `--apply` exiting 1
- an owner message in F after this dispatch that tells you to do or not do something in this scope
- a planner message that widens the scope without quoting an owner ruling

# Checks
- The failing-first run and the revert check, with the exact FAIL lines.
- Counts before and after, tests in their own Bash call.
- Both checkers before every commit touching RECORD.md; F read in a separate call before each.
- The 17 merge commits listed by `git log --merges --first-parent --format=%H%x20%P%x20%s 8b1c88a..origin/pre-main`, each matched to its entry.
- The backup evidence and the merge commit's parents; the four update outputs.
- The hand-back ends with "Decisions I made", "Flags outside scope" and "Deferred" (empty, since none is allowed).

# Out of scope
- Any hook behaviour; history_guard, role_guard, update_worktree code.
- templates/kit.json, .claude/settings.json, the owner's settings files.
- docs/standing-rulings.md.
- The T3 backlog beyond -46 and this copy (D2, D5 and the older harness worktrees).
- Other PRs, tags, deleting anything.

# Device items
None.

# Plan text
# Claude-kit review fixes: plan

Source: the Fable 5.1 review of PRs #25 to #32 (store copy 2026-09-25-44, note 2026-09-25-87). Read at 671b05d; re-read by the planner at 0319ccd.

## Decisions taken by the owner (planner log F, 2026-09-25/26)
- D1: option B. Coders merge; the human gate is a merge dispatch approved after the PR exists; GitHub protection on main and pre-main (pull request required, enforced for admins, both CI checks). pre-main is the working trunk, main the release trunk; promotion by PR after the owner's evidence gate; leaving out by revert; coder deferral to a list.
- D3: the `sh -c`, `env` and `/usr/bin/git` forms of B-01 move to `fixed` in R2/R3.
- D4: history_guard checks freshness with one `ls-remote`, timed out and failing closed.
- D5: the v0.2 tag waits until R4 is promoted to main.

## Tasks, in order
| Task | Findings | Files | Size |
|---|---|---|---|
| R1 | P3, P12 text, N5, branch model | check_record.py, kit.json, coder.md, README, this plan | L |
| R2 | P2, P9, N1 | history_guard.py, tests, BYPASSES | M |
| R3 | P4, P7, newline reach | history_guard.py, role_guard.py, tests, BYPASSES | L |
| R4 | P5, P6, N4, README undo | history_guard.py, tests, README | M |
| R6 | P10 | hooks, settings.json, README | S |
| R5 | P8 | update_worktree.py, tests | S |
| R7 | P11, P15, P16 | tests/, run_exercise.py, docstrings | M |

- R1: Kind `merge` and `revert`, terminal outcome `partial` with `Deferred`, note outcome `merged`; a build's finish line ends at PR open, CI green, backup written; the next sweep records merges; pre-main first in protected_branches; coder.md items 1, 3, 10, 12.
- R2: the segment walk tracks `cd` and `pushd`, strips shell keywords and wrappers before looking for git, treats any command whose basename is git as git, denies `+` refspecs as force pushes; MERGE gains --git-dir and --work-tree; new fixed rows.
- R3: whole-text regexes (FORCE_PUSH, PR_MERGE, GH_API, FILTERS, MERGE) move onto the segment walk at command position; strings given to `sh -c` and friends are parsed as nested commands; GraphQL mergePullRequest and `gh alias set` denied; role_guard's named patterns match the command word and subcommand from tokens; B-02 stays accepted.
- R4: the bundle must list the pair of SHA and origin ref; INDEX lines match whole tokens; freshness by ls-remote; README says what the check proves and rewrites the undo procedure.
- R5: step 1 proves or disproves the ignored-file clobber; if it clobbers, survey refuses on ignored files at paths the range adds.
- R6: timeouts on history_guard's three and dispatch_guard's two subprocess calls, device_guard's below 60, explicit hook timeouts in settings.json.
- R7: the named tests test what they name; case 1 checks the saved dispatch; the launcher's three untested paths; no wall-clock bounds; P15 and P16 as docstring limits.
- Then: the T3 backlog (D2, D5, W's copies), the v0.2 completion report with the review's findings and their status, the tag.

## Planner practice
A premise pulse before each build; "or line drift" in every abort list; the planner authors the outcome prediction; a met abort condition is a stop; owner questions carry no marked recommendation, with the planner's view and its strongest counter on a separate line.