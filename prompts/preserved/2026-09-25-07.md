HEAD: cda554b0a77bca36469193987ee5925188ebc2c5
Target subagent: coder
Type: build
Preserved: 2026-09-25T01:41:01Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for a housekeeping task. Bring the planner's harness worktree onto main (the T9 merge), so that the planner session running from it gets T9's role_guard, which lets it SendMessage its own agents. This changes no code in the repository. Follow `.claude/agents/coder.md`. Record it like T0: a branch, a store copy, an intent, a terminal, and a PR.

# Base and state
Premises to verify:
- **main.** origin/main is d59280fceb136d04d2165d3851bdd2466a04ace6 (PR #9 merge, T9, 2026-09-25T01:40:11Z). Nothing is tagged. The record's last entry is 2026-09-25-08 (terminal); no intent is open.
- **W.** The worktree is ~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB, on branch `worktree-bridge-cse_013ve7bxjxrGv4tLa8p1kdHB` at cda554b. It has no commits of its own: `git log cda554b..<branch>` is empty. It is locked by the harness; the lock prevents only removal and pruning.
  - Its untracked files should be exactly these, under `prompts/preserved/`:
    - `2026-09-24-08.md`, `2026-09-24-09.md`, `2026-09-25-01.md`, `-03.md`, `-04.md`, `-06.md`. Call these the six. Each should be byte-identical to the same name on origin/main.
    - `2026-09-25-02.md` (D2) and `-05.md` (D5): not on main, cited and not copied (R5). Leave them.
    - This dispatch, expected as `2026-09-25-07.md` (the counter should read `2026-09-25 07`). If the hook gave another name, that name governs.
- **Planner session.** It is running and its hooks run from W. The hook scripts are read on every call, so W's files take effect at once. `.claude/settings.json` is read when the session starts. Report whether `git diff cda554b d59280f -- .claude/settings.json` is empty.
- **Fixes checkout.** ~/Zynergy/Claude-kit-fixes; your record branch comes from origin/main.
- **Backups.** ~/forager-backups/INDEX.md exists. T0's backup (2026-09-24-02) is the precedent for folder naming, MANIFEST.sha256 and the one INDEX line. Read INDEX.md for the next folder name.

# Scope boundary
In W only:
- move the six into a new ~/forager-backups/<next>/prompts/preserved/, with MANIFEST.sha256 and one INDEX.md line
- `git -C W merge --ff-only origin/main`, after `git fetch origin`

Nothing else in W: D2, D5 and this dispatch's file stay untracked where they are. No other worktree or checkout is touched, the main checkout ~/Zynergy/Claude-kit included.

In the fixes checkout, branch `kit-v0.2-wt-ff` from origin/main:
- this dispatch's store copy
- intent 2026-09-25-09
- terminal 2026-09-25-10

No code, test, hook, doc or config change. No merge into main, no tag. Nothing deleted: the six are moved, not removed.

# Closed decisions
The owner, in the planner log /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl:
- "Merged. Option 1 please". This is the latest owner message before this dispatch. It answers the planner's preceding message, whose option 1 reads: "I dispatch a small coder task (recommended): it checks each of the six against main by sha256, moves them into `~/forager-backups` with a manifest, fast-forwards my worktree to `origin/main`, and confirms role_guard there has SendMessage."

Cite its line and timestamp in the intent.

Owner messages after this dispatch's Agent call: an abort only if one tells you to do or not do something in this scope, or changes a decision here. Record every other message in the terminal and continue. If unsure, stop.

# Prediction
- **K1:** all six match origin/main by sha256.
- **K2:** they are moved, and the manifest passes `sha256sum -c`.
- **K3:**
  - The fast-forward succeeds: cda554b..d59280f.
  - W's `git status --porcelain` then shows only D2, D5 and this dispatch's file as untracked, and the six as tracked, with sha256 matching K1.
  - W's `.claude/hooks/role_guard.py` equals origin/main's and contains `"SendMessage"` in PLANNER_TOOLS.
  - The fast-forward would have refused with the six in place, as untracked files it would overwrite. That is inferred, not tested; don't test it.
- **K4:** run W's role_guard.py as a script against the real planner log, as in T9's Check 4, with a SendMessage payload.
  - Target: your own agent ID. It is in the planner log as the `toolUseResult.agentId` of this dispatch's Agent call result, `isAsync` true, which is written when you launch. Expect None.
  - Target `main`: expect a deny.
  - Send no message.
- **Fixes checkout counts** (no code change): 99 hook tests OK, 29 tests/ OK, render checks 23/23 and 8/8, both checkers PASS, release_check PASS with 19 files.

# Finish line and abort conditions
Finish line:
- K1-K4 done
- the record pushed on kit-v0.2-wt-ff
- PR kit-v0.2-wt-ff -> main open with CI green on its final commit
- no merge, no tag

Abort and report on any of these:
- any Base-and-state mismatch other than this dispatch's name
- W has commits of its own, or untracked files beyond those listed
- any of the six differs from origin/main
- a manifest mismatch
- the fast-forward refusing after the move, for any reason
- two failed fixes on one symptom
- an owner message that qualifies under the rule above

# Checks
- K1-K4 as above, with commands and output in the hand-back.
- Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
- Run tests in their own Bash call, never with a commit or push.

# Out of scope
- T10, T11, T4 and the backlog.
- Copying D2 or D5.
- Any other harness worktree.
- Pulling the main checkout ~/Zynergy/Claude-kit.

# Device items
None.