HEAD: 0ff36345d0812c956ab1a423089d0b26ecf65468
Target subagent: coder
Type: build
Preserved: 2026-09-26T01:12:22Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit review fix R3: history_guard and role_guard judge commands by their command word and arguments per segment, not by words anywhere in the text; strings given to `sh -c` and `eval` are parsed as commands; GraphQL merges and `gh alias set` are denied. Follow `.claude/agents/coder.md` at the base (0ff3634), including items 1, 2, 5, 6, 10 and 12. Deferral is not allowed in this build.

Practical note: until this build lands, history_guard's whole-text patterns deny any Bash call whose text mentions `gh pr merge`, `git merge`, `--force` or `filter-repo`, including a heredoc that quotes a docstring. Write such patches and record text to /tmp with the Write tool and apply them by path, as R1's and R2's coders did; report each such correction.

# Base and state
Premises to verify. Line numbers are at 0ff3634; drift is drift.
- **origin/pre-main** is 0ff36345d0812c956ab1a423089d0b26ecf65468 (the PR #36 merge). origin/main is 0319ccd. No tags. Both branches carry GitHub protection (pull request, enforced for admins, checks `python 3.8` and `python 3.14`).
- **Branch:** create `kit-review-r3` from origin/pre-main in ~/Zynergy/Claude-kit-fixes (on kit-review-r2 at 55ad7fd, clean). Push with `git -C /home/zynergy-labs/Zynergy/Claude-kit-fixes push -u origin kit-review-r3`.
- **C** (~/Zynergy/Claude-kit) is on pre-main at 0ff3634, clean but for `.claude/worktrees/`. **W**, your Bash cwd, is /home/zynergy-labs/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01RGmfuBWv4x1Nvnpa3fBQDZ at 0ff3634; its only untracked file is this dispatch's copy (expected `prompts/preserved/2026-09-26-02.md`; the hook's name governs). Copy it byte for byte, keep the name, record path, size and sha256 in the intent.
- **The record** at 0ff3634 ends with terminal 2026-09-26-23 (closing -22, completed). No intent is open. The sweep owes one `merge` entry: PR 36, Head kit-review-r2, Base pre-main, Merge-commit 0ff36345d0812c956ab1a423089d0b26ecf65468, Pre-merge 24e4f7fc54ef6c6e534ac35bebeb19c18c7fabf4, Backup 2026-09-26-02, Merged-by `coder preserved/2026-09-26-01.md`, Carries 2026-09-26-21 to -23, merged 2026-09-26T01:07:15Z. Derive the values yourself; these are what I expect. INDEX.md's row 2026-09-26-03 is update_worktree's move of W's copy, not a merge backup.
- **Planner log F** is /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-01RGmfuBWv4x1Nvnpa3fBQDZ/d5592fbe-c371-5027-aaf9-18ab19a5613c.jsonl. Read it in a separate call before each record commit; do not print its long lines.
- **history_guard.py** at 0ff3634 (700 lines): whole-text patterns `FORCE_PUSH` :141-143, `PR_MERGE` :144, `FILTERS` :145, `MERGE` :146, `GIT_PUSH` :148, `GH_API` :161, `MERGE_ENDPOINT` :162-163, `API_METHOD` :164, `API_WRITE_FLAG` :165-166; `command_start` :283-307; `follow_directory_change` :318-337; `git_invocation` :340-357; `push_targets_protected` :360-399; `push_segment` :402-429; `push_problem` :432-453; `merge_form` :469-508 (its (b) checks the whole command is one segment starting `gh pr merge`, :471-481); `merge_endpoint_write` :628-642; `guard` :645-695 (whole-text checks :652-669, the `git merge` loop :671-680, the parse and push walk :682-694). Docstring :35-64 and :113-117 state the whole-text rules and the unparseable-push rule.
- **role_guard.py** (unchanged since 671b05d): `GIT_PREFIX` :71, `NAMED_PATTERNS` :73-84 (regexes over the raw text), `check_bash` :176-200 (parse :177-180, redirection :181-182, named patterns :183-185, adb :186-187, punctuation :188-189, head :192-200).
- **Tests**: `.claude/hooks/tests/test_history_guard.py` (`OuterRepos` base with `outer` on feature, `outer/sub` on main, `outer/sub2` on feature; `PushWalk`; `MergeRule` with `assertMergeDenied`; `test_unparseable_push_and_continuation_still_blocked` sends `git push origin feature && echo 'oops` expecting "could not parse", and `test_unrelated_commands_untouched` expects `git commit -m 'push --force later'` to pass). `.claude/hooks/tests/test_bypasses.py`: `assertGetsThrough` :64, `assertBlocked` :69, `test_b01_push_through_wrapper_or_git_path` :75 (now sends `sh -c 'git push origin main'` and a `python3 -c` subprocess push, both expected through), b10 :85, b11 :97, b12 :104, `test_b02_interpreter_splits_words` :111 (python forms through), `test_b03_pr_merge_through_gh_api_is_blocked` :128 (asserts "gh pr merge" in the reason), `test_b03_get_of_merge_endpoint_passes` :143, b09 :211, `test_table_docstrings_and_tests_agree` :222. `.claude/hooks/tests/test_role_guard.py`: `test_named_patterns` :76-95 (every command has its pattern at the command word), `test_outside_the_allowlist_denied` :107-115 (`git log; rm x`, `git log $(rm x)`, assert deny only), `OtherRoles.test_pulse_writes_and_device_input_denied` :352-366 (`adb shell 'getprop; rm -rf /sdcard/x'` asserts "pulse" in the reason; the adb read check denies it on its own for shell punctuation). No existing test asserts a denial for a guarded word outside command position.
- **BYPASSES.md**: rows B-01 to B-12; B-01 `accepted` (its text now names the `sh -c` and interpreter forms), B-02 `accepted`, B-09 `accepted`.
- Counts at 0ff3634: hook tests "Ran 158 tests" OK; tests/ "Ran 94 tests" OK (its install test runs the vendored hook tests, so it fails whenever they do); render checks 39/39 and 13/13; release_check 27 files, no match.

# Scope boundary
Files: `.claude/hooks/history_guard.py`, `.claude/hooks/role_guard.py`, their two test files, `.claude/hooks/tests/test_bypasses.py`, `.claude/hooks/BYPASSES.md`, `RECORD.md`, the store copy. guardlib, the merge rule's conditions (a) to (e) and their messages, and the heredoc and newline handling stay as they are.

1. **One walk.** history_guard's `guard` strips heredocs, splits lines and tokenizes once, then walks segments with the effective-directory state R2 added. Every rule is decided on a segment's tokens after `command_start`:
   - A command word whose basename is `git`: read the global options (`-C <dir>`, `-c <k=v>`, `--no-pager`, `--git-dir=<d>`, `--work-tree=<d>`, and the two-token `--git-dir <d>` and `--work-tree <d>`), then the subcommand. `push`: a force flag (`--force`, `--force-with-lease`, with or without `=value`, or a short cluster containing `f`) or a `+` refspec is a force push, denied for every destination; then the protected-branch check as today. `merge`: the branch is read in the `-C` directory resolved against the effective directory, else the effective directory, and a protected branch is denied; with `--git-dir` or `--work-tree` present, or an unknown effective directory, the merge is denied because the checkout cannot be determined. `filter-repo` and `filter-branch` as the subcommand, or a command word whose basename is `git-filter-repo` or `git-filter-branch`, are denied.
   - A command word whose basename is `gh`: `pr merge` runs the merge rule as today, with (b) unchanged (the whole command must be that one segment with nothing before `gh`). `api`: the merge endpoint check over the segment's tokens (an endpoint token matching MERGE_ENDPOINT, the method from `-X`/`--method` with or without `=`, the write flags), same messages as today; and any `api` call whose tokens contain `mergePullRequest` is denied for every role, naming it. `alias set` is denied for every role: an alias can hide any command.
   - A command word whose basename is `sh`, `bash`, `dash`, `zsh` or `ksh` with a `-c` flag (alone or in a cluster such as `-lc`, `-ec`): the next token is a command string, walked with the same rules and a copy of the directory state, so a `cd` inside it does not reach the outer segments. `eval`: its arguments, joined by spaces, are walked the same way. A string that cannot be parsed is denied if it mentions a guarded command (below), else let through. Interpreters (`python3 -c` and the like) are not parsed: B-02 stays accepted.
   - Nothing is matched anywhere else: a guarded word inside an argument (`grep`, `echo`, `git commit -m`, `git log --grep`, a Python string) denies nothing.
2. **Unparseable text.** A command that cannot be tokenized is denied when its text mentions any guarded command: `git ... push`, `git ... merge`, `gh ... pr merge`, `gh ... api`, `filter-repo`, `filter-branch`, `mergePullRequest`, or a `--force`/`-f` after a `git push`; the reason says it could not be parsed and what it mentions. Otherwise it is let through, as today. The mention patterns exist only for this case.
3. **Delete** `FORCE_PUSH`, `PR_MERGE`, `FILTERS`, `MERGE`, `GIT_PUSH`, `GH_API` and `merge_endpoint_write` as whole-text checks, or reduce them to the mention patterns of item 2. Keep every existing deny message's wording where a test asserts on it.
4. **role_guard.** `NAMED_PATTERNS` becomes a check over tokens: the command word of each segment (split at PUNCTUATION tokens, after dropping the same keywords, wrappers and assignments as history_guard's `command_start`; import or duplicate it, your choice, stated) is `git` by basename with subcommand `commit`, `push`, `checkout`, `reset` or `merge` after git's global options; or `rm`, `mv`; or `sed` with a token that is `-i`, starts with `-i` as a short option, or is `--in-place` with or without `=`; or a basename `gradle`, `gradlew` or `gradlew.bat`. The message stays "`<name>` changes the repository or build". The check runs before the punctuation rule, as today, so `git log; rm x` still names `rm`. A guarded word inside an argument denies nothing.
5. **Docstrings.** history_guard's :35-64 and :113-117 and role_guard's :44-46 are rewritten to state the rules above; the docstring remains the one statement of the rules.
6. **BYPASSES.md.** B-01's text keeps only the interpreter form (`python3 -c` calling subprocess with a push), still `accepted`; its test sends only that. New rows, `fixed`, with tests that assert the block: B-13 (a push or merge inside a `sh -c`, `bash -lc` or `eval` string), B-14 (GraphQL `mergePullRequest` through `gh api graphql`, and `gh alias set`), B-15 (quoted or split subcommand words, `gh "pr" merge 5 -m` and `gh pr mer""ge 5 -m`). Citation lines: fixed rows are not cited; B-01, B-02, B-04, B-09 stay cited by history_guard; B-08, B-09 by role_guard.
7. **Tests, first.** Allowed (all from a repo on main, coder role, unless said): `git grep -E "git merge|--force" -- .`; `git commit -m "gh pr merge 5 --merge later"`; `echo git push --force origin main`; `git push origin feature` followed by a newline and `rm -rf build` (from a repo on feature); `git log --grep filter-repo`; `python3 -c "print('git push origin main')"`; `sh -c 'git push origin feature'` from a repo on feature; planner role in role_guard: `git grep -n "sed -i" -- x`, `git log --grep "rm "`, `gh pr view 5 --json title`. Denied: `sh -c 'git push origin main'`; `bash -lc "cd sub && git push origin"` with the OuterRepos fixture (payload cwd `outer`); `eval 'git push origin main'`; `sh -c 'git push --force origin x'` with "force"; `gh "pr" merge 5 -m` and `gh pr mer""ge 5 -m` (denied by the merge rule; assert "denied"); `gh api graphql -f query='mutation { mergePullRequest(input: {pullRequestId: "x"}) { clientMutationId } }'` with "mergePullRequest"; `gh alias set m 'pr merge'`; `cd sub && git merge x` with the fixture (sub is on main); `git --git-dir=sub/.git merge x` from `outer` with "cannot be determined"; the existing `gh api -X PUT repos/o/r/pulls/5/merge -f merge_method=merge` (B-03) still denied with "gh pr merge" in the reason; `git push origin feature && echo 'oops` still "could not parse"; an unparseable `echo 'oops; gh pr merge 5 -m` denied naming the mention. In test_bypasses, the narrowed b01 and the new b13, b14, b15. Every existing test stays as it is except b01.

**Record**, in order: the sweep (the merge entry for #36); this copy and the intent; the tests-only commit; the fix; the terminal.

# Closed decisions
- The owner's D1 and "I'll take your recommendations" (F lines 157 and 165), recorded in intent 2026-09-26-02 and docs/specs/2026-09-26-review-fixes-plan.md at 0ff3634, whose D3 moves B-01's `sh -c` form to `fixed` (this build) and whose R3 row lists this build's scope.
- Planner's choices: the shell list for `-c`; `eval`; denying `git merge` with `--git-dir`/`--work-tree` or an unknown directory rather than resolving them; `gh alias set` denied outright; the mention rule for unparseable text; the row numbers B-13 to B-15; the test list.
- Standing: none cited. The merge rests on this dispatch's Merge section.

# Merge
authorised: this PR only (kit-review-r3 -> pre-main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup of origin/pre-main. One Bash call from a checkout of this clone (W is one), no `--repo`. If denied, quote the denial and stop. Then update C with the merged update_worktree.py (dry run, then `--apply`), then W the same way; both should target origin/pre-main; if either targets another branch, stop before `--apply`. Report all four outputs.

# Prediction
Planner's outcome prediction (verbatim in the intent's planner field):
- Tests-only commit: the hook suite fails on about 19 cases: the allowed-mention cases (denied today by the whole-text patterns), the `sh -c`, `bash -lc`, `eval`, quoted-word, GraphQL, `gh alias set`, `cd sub && git merge x` and `--git-dir` merge cases (let through today), and b13 to b15; the B-03, unparseable-push and narrowed-b01 cases pass already. Name the exact count and any case failing for another reason. tests/ shows the same failures through its install test.
- After the fix: hook tests 158 plus the new tests, all OK; tests/ 94 OK; render checks 39/39 and 13/13; both checkers PASS; release_check PASS, 27 files, no denylist hit; CI green on the final commit. Your own Bash calls that quote merge or force wording stop being denied once the fix is in the hooks W runs (after this PR merges and W is updated, not during the build, since your hooks run from W at 0ff3634).
- Revert check: history_guard.py and role_guard.py from 0ff3634 under the new tests give the same failures as the tests-only commit.
Put your mechanism prediction in the intent.

# Finish line and abort conditions
Finish line, pushed on kit-review-r3: (1) the sweep; (2) this copy and the intent; (3) the tests-only commit; (4) the fix; (5) the terminal, Outcome `completed`, closing at the PR open, CI green on its final commit and the backup written. Then the merge and the two updates, reported in the hand-back. No tag.

Abort and report on any of:
- a Base-and-state mismatch other than the copy's name or line drift
- any existing test failing after the fix, other than b01's narrowed assertion
- a needed change to guardlib, the merge rule's conditions, or the heredoc and newline handling
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit
- CI not green on the final commit (no merge)
- the merge denied by anything (quote it)
- a dry run targeting a branch other than pre-main, or any dry run or `--apply` exiting 1
- an owner message in F after this dispatch that tells you to do or not do something in this scope
- a planner message that widens the scope without quoting an owner ruling

# Checks
- The failing-first run and the revert check, with the exact FAILED lines.
- Counts before and after, each suite in its own Bash call.
- Both checkers before every commit touching RECORD.md; F read in a separate call before each.
- The merge entry's values against `git log --format=%H%x20%P` and `gh pr view 36`.
- The backup evidence and the merge commit's parents; the four update outputs.
- The hand-back ends with "Decisions I made", "Flags outside scope" and "Deferred".

# Out of scope
- R4: the merge rule's backup and freshness checks, INDEX matching, README undo.
- R6: subprocess timeouts.
- `git pull`, `rebase`, `reset`, `cherry-pick` on protected branches (review N2, unruled).
- update_worktree.py, docs, templates, settings files, docs/standing-rulings.md.
- The T3 backlog, other PRs, tags, deleting anything.

# Device items
None.