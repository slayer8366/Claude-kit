HEAD: ff54f06048b639fd3e7f52d766720252a3d05015
Target subagent: coder
Type: build
Preserved: 2026-09-23T07:22:50Z by the coder from the session log (first user message of subagent transcript /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-017qto3G4Cp8mMH47GPv2RCL/ffddbdd3-46b7-55a7-9543-3f866eb4544b/subagents/agent-a6a6ce766ed39f887.jsonl), because the dispatch was unguarded and no hook preserved it
--- verbatim prompt follows ---
**Type:** build

# Claude-kit v0.1 fixes, before any tag (ruling items 1-6)

## Role
You are the coder. You execute this dispatch, you make no design decisions, and a gap is a stop-and-ask. The kit's record is used exactly as `.claude/agents/coder.md` and the checkers require: `RECORD.md` (sweep, intent, terminal) and `prompts/preserved/`. Run `check_record.py` and `check_prompts.py` before every commit that touches `RECORD.md`.

## Base and state
- Base: `origin/main` `93904076a95498b3a877438f6d354fc1b1b88419` (the PR #1 merge). No tags exist. Verify both before starting; if either differs, stop.
- Build ONLY in a new worktree: `git -C ~/Zynergy/Claude-kit worktree add -b kit-v0.1-fixes ~/Zynergy/Claude-kit-fixes origin/main`. Never edit files in `~/Zynergy/Claude-kit` or any `.claude/worktrees/*` checkout: a session there may load those hooks and that `kit.json` live, and an invalid `kit.json` denies every tool.
- **This dispatch is unguarded (owner's Option B).** It is sent from a planner session in the harness worktree `~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_017qto3G4Cp8mMH47GPv2RCL`, which sits at `ff54f06` (the initial commit, no `.claude/`), so no kit hook ran: nothing preserved this prompt and no approval prompt appeared. The owner's approval is in that session's chat (quoted under Closed decisions). So:
  - Save this prompt yourself as `prompts/preserved/2026-09-23-02.md` in the worktree. Take the text byte-exactly from your own subagent transcript (the first user message) under `~/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-017qto3G4Cp8mMH47GPv2RCL/`; do not retype it. Head it in the hook's format (HEAD, Target subagent: coder, Type: build, a `Preserved:` line saying it was saved by the coder from the session log because the dispatch was unguarded, then `--- verbatim prompt follows ---`). Record the transcript path and the sha256 of the prompt text in the intent.
  - The intent carries a **Bootstrap** field stating all of the above, as v0.1's intent 2026-09-23-01 did. Its `Dispatch-file` claims `preserved/2026-09-23-02.md`.
- Premises the planner verified (re-verify each before editing):
  - The extraction report (`docs/audits/2026-09-23-kit-v0.1-extraction-completion-report.md`, "Flags outside scope") flags 1, 3, 4 and 10 are ruling items 1, 2, 3 and 4.
  - Approval is hard-coded at `dispatch_guard.py:141`.
  - `role_guard.py:169` restricts only the literal `pulse`.
  - `checkers_dir` is read at `guardlib.py:35,88` and `dispatch_guard.py:78-82,140`. It also appears in `harness.py:33`, `test_config.py:74,93`, `test_dispatch_guard.py:191`, both `kit.json` files, `workshop/behaviour_diff.py:59`, `coder.md:33` and `README.md:47`.
  - The private-record citations in released files are `check_record.py:97`, `check_prompts.py:172` and `test_role_guard.py:41`. `README.md:110,274` quotes them but is not released.

## Scope boundary
Items 1-4 and 6 below, the scratch copy, and the item 5 live-exercise evidence and record. Every change to a `kit.json` key lands in the same commit as the hook and validator that read it, across `.claude/kit.json`, `templates/kit.json`, `.claude/hooks/tests/harness.py` (TEST_CONFIG) and `workshop/behaviour_diff.py`. Nothing in Forager, forager-app or forager-forecast. No tag.

## Closed decisions
Owner ruling, 2026-09-23 (verbatim):
```
Kit v0.1, owner ruling, 2026-09-23.

Merge PR #1 as it stands. No tag.

Fix dispatch before any tag, planned from a session in the
Claude-kit checkout:
1. Approval fails closed: every dispatch type requires approval
   unless kit.json explicitly exempts it (default: pulse only).
2. Agents map to roles in kit.json. Role restrictions come from
   the role. An unknown agent or role is denied everything.
3. Drop the checkers_dir key. Checkers stay at the root.
4. Released files cite the kit by release version, not by a
   private record path. Add the pattern to release_check.
5. Live exercise in a kit-repo session: a pulse, a build
   awaiting approval, a blocked mismatch, and a blocked
   unknown agent.
Each fix failing-first with sabotage.

Recorded for v0.2: history_guard over-blocking on unparseable
push commands; dispatch-note outcome `stopped`; upgrades
removing dropped files; a kit bypass table.

Scratch in /tmp (kitsrc-b2435ef, egd-35e39d1, kit_*, first-run
scratch): copy into ~/forager-backups with an INDEX.md line. Do
not delete.
```
Item 6 (owner confirmed after the ruling, 2026-09-23): the pulse role's allowlist blocks the hand-back tool, so every pulse runs and delivers nothing. Add it to the pulse role only; the planner role does not gain it. Tool name established from evidence: `SubagentHandback` (the role_guard denial "the pulse role may not use SubagentHandback" in Forager pulse subagent logs, e.g. 2026-09-23T02:47:34Z, `-home-zynergy-labs-Zynergy-Forager--claude-worktrees-bridge-cse-016CLyXZt76dm7Hr3APUi1Sz/.../subagents/agent-ac113d35100c6dbee.jsonl`; Forager fix `0eba963` on branch `pulse-handback`, not merged, uses the same name).

Owner approval of this dispatch and of D1-D8, verbatim from the planner session: "Approve. Option B, go ahead and dispatch it." Option B means this dispatch runs unguarded, sent from the planner session above, with the prompt saved by the coder (see Base and state).

Planner decisions (owner-approved):
- D1 (item 1): new REQUIRED key `approval_exempt_types` (a list of Types; may be empty; every entry must be a Type in `type_targets`, else the validator rejects it). It is required so that every exemption is explicit. Both `kit.json` files and TEST_CONFIG carry `["pulse"]`. The guard returns `ask` for every Type not listed and `allow` for listed ones; messages name the Type (no literal "pulse dispatch").
- D2 (item 2): new REQUIRED key `agent_roles` (agent name -> role). The kit's roles are `planner`, `pulse` and `coder`, defined once in code. The main session (no `agent_type`) is always `planner`. Every `dispatchable_agents` entry must have a role (validator). At runtime, role_guard denies every tool, with a message naming the agent, to (a) an `agent_type` not in `agent_roles`, and (b) an agent whose role is not one of the kit's roles. An unknown role name is a runtime deny, not a config error. The `planner` and `pulse` restrictions apply by role, not by agent name; `coder` stays unrestricted by role_guard. `dispatchable_agents` is kept. Both `kit.json` files and TEST_CONFIG: `{"coder": "coder", "pulse": "pulse"}`.
- D3 (item 3): remove `checkers_dir` from the defaults and code; a `kit.json` still carrying it fails validation as an unknown key (fail closed). The hook runs `<root>/check_prompts.py`. `coder.md` and the README say the checkers sit at the repository root.
- D4 (item 4): new line in `workshop/denylist.txt` (the file `release_check.py` reads): `(?i)claude-kit[^\n]{0,40}RECORD\.md`. It deliberately does not match the upstream EGD citations the owner approved keeping (e.g. `check_prompts.py:36`). The replacement wording is "Claude-kit v0.1 addition" (e.g. `# Claude-kit v0.1 addition: a dispatch-note claims ...`). Update the README's quoted diffs to match. Add a test in `tests/test_release_check.py` that plants `Claude-kit RECORD.md 2026-09-23-01` and expects failure.
- D5 (item 6): `SubagentHandback` joins the pulse role's allowed tools only. Add tests: pulse allowed, planner denied.
- D6: commit order, one commit per item: 3, 1, 2, 6, 4. Each commit carries its failing-first test.
- D7 (item 5): the "unknown agent" live case is a dispatch to the built-in `general-purpose`, which dispatch_guard blocks. role_guard's unknown-agent deny cannot be reached live (the dispatch hook blocks first), so it is proven by unit tests only; the report says so. The "mismatch" case is a `Type: pulse` prompt sent to `coder`.
- D8: this build's own `/tmp` scratch uses the prefix `/tmp/kitfix_*` (never `kit_*`). The scratch copy (step 2) runs before any new scratch is made.

## Prediction
- Item 1: a TEST_CONFIG with an added Type `review` -> coder, not exempt, fails on v0.1 with `'allow' != 'ask'`.
- Item 2: an agent `reader` mapped to `pulse` calling Write fails on v0.1 with `None != 'deny'`. So do `general-purpose` calling Read, and an agent mapped to role `nosuch`.
- Item 3: a `kit.json` with `checkers_dir` fails on v0.1 with `None != 'deny'` (the key is accepted).
- Item 4: after adding the pattern, `release_check.py` fails on exactly `check_record.py:97`, `check_prompts.py:172` and `.claude/hooks/tests/test_role_guard.py:41`.
- Item 6: `tool("SubagentHandback", "pulse")` fails on v0.1 with `'deny' is not None`.
- Behaviour diff: still exactly 49 source tests, and the only failures are the two Grep/Glob subtests of `test_allowlisted_tools_pass`. Item 6 changes no source test: b2435ef's tests never name SubagentHandback. `behaviour_diff.py`'s Forager-equivalent config: `agent_roles` {coder: coder, pulse: pulse}, `approval_exempt_types` ["pulse"], no `checkers_dir`.

## Finish line and abort conditions
Finish: every step below done with its evidence, the live exercise recorded, a terminal entry written, and a PR `kit-v0.1-fixes` -> `main` with CI green on its final commit. No tag.

Abort and report on any of these:
- a premise in "Base and state" is wrong;
- any behaviour-diff failure beyond the two expected ones;
- the prompt can't be recovered byte-exactly from the transcript;
- any need to edit outside the worktree (other than the backup step) or to touch Forager;
- two failed fixes on one CI symptom (then gather data, not a third hypothesis).

Steps:
1. Worktree, saved prompt, sweep (the store holds only `2026-09-23-01`, already claimed, so the sweep is expected to be vacuous; verify it), and the intent. The v0.2 items are recorded in the intent as deferred and not built. Commit and push.
2. Scratch copy:
   - sources: `/tmp/kitsrc-b2435ef`, `/tmp/egd-35e39d1`, every `/tmp/kit_*` path, `/tmp/kitsrc`, `/tmp/egd`, `/tmp/kit_pulse.txt`;
   - destination: a new folder `~/forager-backups/<UTC date>-<next free NN>/` (2026-09-23-01 and -02 exist; check again at copy time);
   - a `MANIFEST.sha256` of every file, verified on both sides;
   - one line appended to `~/forager-backups/INDEX.md` in its existing table format. The index is append-only: a mistake gets a correction line, never an edit;
   - nothing deleted from `/tmp`.
3. Items 3, 1, 2, 6, 4, one commit each, each pushed.
4. STOP and report back "ready for live exercise", with evidence so far. Do not continue. The owner will start a new Claude Code session in `~/Zynergy/Claude-kit-fixes`, with `.claude/agents` present at start. In that session the planner sends four dispatches:
   - a pulse (it must run unasked and return its report);
   - a build (the approval prompt must appear; the owner declines);
   - a `Type: pulse` prompt to `coder` (must be blocked);
   - a prompt to `general-purpose` (must be blocked).

   Do not edit hooks or `kit.json` in that worktree after step 4.
5. When resumed: quote each hook message from that session's log under `~/.claude/projects/`, sweep dispatch-notes for the preserved pulse and the declined build, write the completion report under `docs/audits/` and the terminal entry, open the PR against `main`, and get CI green.

## Checks
- For items 1-4 and 6:
  - the new test fails first against v0.1, with its message pasted;
  - then a sabotage run: save a copy to `/tmp/kitfix_saved/`, break the fix, show the test fails, restore FROM THE SAVED COPY (never git), and confirm with `cmp`.
- Every commit passes:
  - `python3 -m unittest discover -s .claude/hooks/tests -v`
  - `python3 check_record.py --render-check`
  - `python3 check_prompts.py --render-check`
  - `python3 -m unittest discover -s tests -v`
  - `python3 release_check.py`
- After the last item, run `python3 workshop/behaviour_diff.py --source ~/Zynergy/Forager` and paste the full output.
- Report the old test counts (68 hook tests; 16 install and release tests) and the new ones.
- End every report with **Decisions I made** and **Flags outside scope**.

## Out of scope
- The v0.2 items: history_guard over-blocking, the `stopped` outcome, install pruning, and a bypass table.
- `NOTE_TYPES` in `check_record.py`.
- The upstream checker docstrings (OSCam, `~/imx`, the EGD record citations).
- Grep and Glob placement.
- Forager, including its `pulse-handback` branch.
- Any tag.

## Device items
None. `android_package` is null and no device is used.
