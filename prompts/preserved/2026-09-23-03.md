HEAD: 93904076a95498b3a877438f6d354fc1b1b88419
Target subagent: coder
Type: build
Preserved: 2026-09-23T13:54:23Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Claude-kit v0.1 fixes, continuation with rulings: finish item 6, item 4, restore lost evidence

## Role
You are the coder. You execute this dispatch, you make no design decisions, and a gap is a stop-and-ask. The governing dispatch is `prompts/preserved/2026-09-23-02.md` on branch `kit-v0.1-fixes` (intent 2026-09-23-03). Everything in it stands except where this dispatch says otherwise. Record this dispatch as `coder.md` and the checkers require, and run `check_record.py` and `check_prompts.py` before every commit that touches `RECORD.md`.

## Base and state
- Worktree `~/Zynergy/Claude-kit-fixes`, branch `kit-v0.1-fixes`, HEAD and `origin/kit-v0.1-fixes` both `162bd98`. Uncommitted: `.claude/hooks/role_guard.py` and `.claude/hooks/tests/test_role_guard.py` (item 6, SubagentHandback added to PULSE_TOOLS; tests `test_pulse_may_hand_back` and `test_planner_may_not_hand_back`). Verify all of this before starting. If any of it differs, stop.
- The first coder under 2026-09-23-02 stopped partway through item 6 without delivering a report. Its evidence for items 3, 1 and 2 survives only as summaries in those commits' messages.
- A previous continuation dispatch (hook-saved, untracked, at `~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01JDbTJ4Gj9C6xygXM5tPdHz/prompts/preserved/2026-09-23-02.md`, 4224 bytes, sha256 `688764e5aa3b7a45c6e95990213898bb78e7d4d369c6922d71610d674503c019`) stopped at its step 1 because the record rules do not cover a continuation. It changed nothing. The rulings below settle that. This dispatch replaces it; the planner could not send the rulings to that coder because role_guard denies the planner SendMessage.
- Scratch copy (step 2 of 2026-09-23-02) is done: `~/forager-backups/INDEX.md` lines 12-13. Do not repeat it.
- The same rules still hold: work only in the fix worktree; never edit `~/Zynergy/Claude-kit` or any `.claude/worktrees/*` checkout (reading is fine); do not touch Forager; no tag; your own scratch goes under `/tmp/kitfix_*`.

## Scope boundary
Item 6 and item 4 as specified in 2026-09-23-02 (D5, D4, commit order D6), the record entries in "Rulings", plus re-running the evidence for items 3, 1 and 2. Nothing else. Before building, look for the first coder's transcript under `~/.claude/projects/` (a subagent transcript whose prompt begins "# Claude-kit v0.1 fixes, before any tag"). If you find it, cite its path and quote any item 6 evidence it holds. Whether you find it or not, re-run the checks below.

## Closed decisions
Everything in 2026-09-23-02 (the owner ruling, item 6, D1-D8), unchanged. The item 6 diff already in the worktree is the intended fix. Do not redesign it.

Rulings (owner, 2026-09-23, on the previous continuation's stop):
- R1, record, option A: a new intent 2026-09-23-04 claims THIS dispatch's saved copy and says it continues 2026-09-23-03. Intent 03 is closed by a terminal entry, outcome `superseded`, `Superseded-by: 2026-09-23-04`. That terminal says items 3, 1 and 2 landed under intent 03 (`2834f9d`, `09fb92d`, `162bd98`), that the first coder stopped partway through item 6 without delivering a report, and that 04 takes over 03's finish line. Intent 04 gets its own terminal after the live exercise (not in this dispatch).
- R2, source: THIS dispatch is saved by the v0.1 dispatch hook in the harness worktree above, under `prompts/preserved/` (likely `2026-09-23-03.md`; identify it by its content and header time). Copy that file byte for byte; confirm sha256 on both sides.
- R3, name: store it in the fix worktree as the next free number in that store (expected `prompts/preserved/2026-09-23-03.md`). The record gives the original path and sha256.
- R4, leftovers: leave the hook-saved originals untracked where they are. Read only; never edit or delete them.
- R5, the stopped continuation is not copied into the fix worktree's store. Intent 04 cites it by path and sha256 (above) as the dispatch that stopped at step 1 and was replaced by this one.
- R6, deferred to v0.2, recorded in intent 04 and not built: (a) v0.1's dispatch hook numbers saved dispatches from its own checkout's store, so one dispatch has a different number per worktree and one number can name different files; (b) the record has no entry kind for continuing an intent; (c) whether the planner role should gain SendMessage so a planner can pass rulings to a stopped coder (owner: "should be considered").

## Prediction
- Item 6: with the new tests against v0.1's role_guard, `test_pulse_may_hand_back` fails with `'deny' is not None`, and `test_planner_may_not_hand_back` passes on v0.1 as well. Say so; it is not a failing-first test.
- Item 4: as in 2026-09-23-02: `release_check.py` fails on exactly `check_record.py:97`, `check_prompts.py:172` and `test_role_guard.py:41`.
- Items 3, 1 and 2, re-run: the messages already summarized in `2834f9d`, `09fb92d` and `162bd98`.
- R1 passes `check_record.py` and `check_prompts.py` as they stand (the previous coder reported so).

## Finish line and abort conditions
Finish: the R1 record entries committed, item 6 and item 4 committed and pushed (one commit each, in that order), the full evidence for items 3, 1, 2, 6 and 4 re-run and pasted into your report, then STOP and report "ready for live exercise", exactly as step 4 of 2026-09-23-02 says. Abort conditions: those of 2026-09-23-02, plus any mismatch with the state described in "Base and state" above, plus the checkers rejecting the R1 entries.

Steps:
1. Record: copy this dispatch per R2-R3, write intent 04 and intent 03's terminal per R1, R5 and R6, run both checkers, commit, push.
2. Look for the first coder's transcript (see "Scope boundary").
3. Item 6: failing-first against v0.1's role_guard, sabotage with a saved copy in `/tmp/kitfix_saved/`, restore from that copy, confirm with `cmp`, run all checks, commit, push.
4. Item 4: same procedure, commit, push.
5. Re-run failing-first and sabotage for items 3, 1 and 2 in a scratch copy under `/tmp/kitfix_*`, leaving the worktree untouched, and paste each failing message.
6. Run `python3 workshop/behaviour_diff.py --source ~/Zynergy/Forager`, full output pasted.
7. STOP: report "ready for live exercise" with old and new test counts.

## Checks
As in 2026-09-23-02 "Checks": all five commands pass on every commit.

## Out of scope
As in 2026-09-23-02, plus: the live exercise, the completion report, intent 04's terminal and the PR. Those happen after the owner's live exercise. Building any R6 item.

## Device items
None.