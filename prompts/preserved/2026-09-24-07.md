HEAD: b54482d5f1e83408d42728ea401aff44777db17e
Target subagent: coder
Type: build
Preserved: 2026-09-24T22:03:58Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Claude-kit v0.2 T12: history_guard stops over-blocking heredocs and unparseable non-push text, and splits on newlines

## Role
You are the coder. You carry out this dispatch and make no design decisions; a gap is a stop-and-ask. Record your work as `coder.md` and the checkers require (including its new item 5, store names). Run `check_record.py` and `check_prompts.py` before every commit that touches `RECORD.md`.

## Base and state
- `slayer8366/Claude-kit`: PR #6 (kit-v0.2-t2) merged at 2026-09-24T22:01:32Z; `origin/main` = `8f632ee5befe922df0b0e2375192da186f582568`. No tags. `docs/specs/2026-09-24-kit-v0.2-tasks.md` line 19 defines T12.
- On `origin/main`: RECORD.md's last entry is `2026-09-24-10` (terminal, closes intent `2026-09-24-09`); no intent is open. The store holds 13 files, the last being `prompts/preserved/2026-09-24-06.md`, all claimed.
- `~/Zynergy/Claude-kit-fixes`: on `kit-v0.2-t2` at `afd56d7` (merged), clean. Work here on a new branch `kit-v0.2-t12` from `origin/main`.
- `~/Zynergy/Claude-kit` (main checkout): on `main` at `b54482d`, not yet pulled to 8f632ee. Not touched.
- The shared counter `~/Zynergy/Claude-kit/.git/claude-kit/dispatch-seq` read `2026-09-24 06` after T2's dispatch.
- The planner's harness worktree `~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01PuSBQqnBC6j4SPFCgt8yv2` (W; HEAD `b54482d`, T1 hook) holds these untracked files:
  - `2026-09-24-05.md` and `-06.md`, already in the store;
  - D, this dispatch, expected at `prompts/preserved/2026-09-24-07.md`, or `2026-09-25-01.md` if saved after midnight UTC. Identify it by header and content. Its store copy keeps the hook's name, as coder.md item 5 says.
- The other harness worktrees' untracked files (01Ftd 1, 01JDb 8, 01Vss 11, 01TVux 2) are left to T3: do not copy or record them.
- No sweep is expected. Confirm that every store file is claimed.
Check all of this first; if anything differs, stop.

## Scope boundary
- Code: `.claude/hooks/history_guard.py` and `.claude/hooks/tests/test_history_guard.py`.
- Docs: `docs/specs/2026-09-24-kit-v0.2-tasks.md`, the T12 row's done-when only.
- Record: D's store copy, one intent, one terminal.
Nothing else changes. `guardlib.py` in particular is not changed: `shell_tokens` keeps its behaviour for the other hooks. There are no changes to the other hooks, kit.json, the checkers, coder.md, install or release tooling, CI or README. The force, `gh pr merge`, filter and merge checks keep matching the whole command text as now. No merge, no tag. Delete nothing.

## Closed decisions
Owner rulings. They are ruled on; do not re-ask them.
- In the previous planner log `/home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-01TVuxEhXQ9gEiCqR5vw4nGZ/a4155372-2845-559f-b1d8-b1e1db6614c8.jsonl`: line 228 (2026-09-24T21:23:28Z) is the planner's options message; its T12 option A reads "Remove heredoc bodies (the text from `<<WORD` to the line with `WORD`) before parsing, since they are data, not commands. Block a command that still won't parse only if it contains an actual `git … push` call, not just the word "push"." It also proposes amending the task list's done-when in T12's own commit. Line 231 (21:30:18Z) is the owner's answer: "I'll take your recommendations".
- In this planner's session log `/home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-01PuSBQqnBC6j4SPFCgt8yv2/a5d14103-ed93-50a1-a091-899109c2144d.jsonl`, two owner messages apply; quote both with line numbers in the intent:
  - The owner's handover message that opens the session. It lists T12's details: the heredoc form `<<[-]?['"]?WORD` through the line that is only WORD; the git + options + push pattern; the rule in the module docstring first; the done-when amended in T12's own commit, with an unparseable push still blocked; and the three failing-first tests.
  - The later AskUserQuestion answer "Fix it in T12". It rules that newlines outside quotes and heredocs split commands, in history_guard only, not in guardlib. The planner's premise was inferred from guardlib.py:170-179, not run: newline is shlex whitespace, not punctuation, so `git status` newline `git push origin main` reads as one segment starting `git status`, and the push is never checked. Prove it with the failing test below before relying on it. If it is false, stop.

The rule, which is the new paragraph in history_guard's module docstring, committed before any code:
"Before the push check parses a command, heredoc bodies are removed. For each `<<WORD`, `<<-WORD`, `<<'WORD'` or `<<"WORD"`, the lines after that line, up to and including the first line that is only WORD (after `<<-`, leading tabs are allowed), are data, not commands, and are dropped. A heredoc with no such closing line is not removed. Newlines outside quotes then separate commands as `;` does; a backslash before a newline continues the line and does not separate. A command that still cannot be parsed is blocked only if it contains a git push call (`git`, git's global options, then `push`). Any other unparseable command is let through by this check. The force, `gh pr merge`, filter and merge checks above still read the whole, unstripped text."

Planner's decisions within the owner's ruling, stated so they aren't yours:
- A heredoc with no closing line is kept. Dropping it could hide a real push that follows `<<` inside a quoted string.
- For the unparseable case, use the existing `GIT_OPTS` pattern: `r"\bgit\b" + GIT_OPTS + r"\s+push\b"`, searched in the stripped text.
- The newline split must not change `guardlib.shell_tokens`.

Task-list amendment, in the commit with the docstring. Replace T12's done-when cell with, exactly:
"history_guard removes heredoc bodies and splits on unquoted newlines before parsing, and blocks a command that still cannot be parsed only if it contains a git push call, per a rule written first in its docstring (owner ruling: an unparseable push stays blocked); failing-first tests"

Record names: D's store copy keeps its hook name. Intent `2026-09-24-11`, terminal `2026-09-24-12`. Use these IDs even if the UTC date has changed; if check_record.py rejects them, stop. The copy is byte for byte, confirmed with `cmp` and sha256; if the name is taken by a different file, stop and ask.
The intent also carries a field `Correction to 2026-09-24-09:` with this text: "Its Closed decisions says the planner's log held 149 lines at 21:42Z. The read was at about 21:40Z (T2 coder's hand-back). The owner messages after it, at lines 152 (21:40:58Z, "PR merged and main pulled") and 181 (21:48:15Z, "Nope, just saying what I did before the dispatch"), concern PR #5 and the pull before T2, not T2." Check those two lines in the log before writing this. If check_record.py rejects the field, stop.

## Prediction
Planner prediction, stated and not withheld. New tests in test_history_guard.py, with the fixture repos as now. `on_feature` means a checkout on branch `feature`.
1. `test_heredoc_message_then_push_to_branch_allowed`: `"git commit -q --allow-empty -F - <<'EOF'\nDon't block this\nEOF\ngit push origin feature"` on_feature passes. It fails today with `'deny' is not None` and the reason "could not parse this push (No closing quotation)".
2. `test_word_push_in_unparseable_text_allowed`: `"echo don't push yet"` on_feature passes. It fails today the same way.
3. `test_push_to_main_after_heredoc_blocked`: `"cat <<'EOF' > /tmp/x\nit's data\nEOF\ngit push origin main"` on_feature is denied, and the reason contains "protected branch". It fails today: the command is denied, but by the parse message, which lacks "protected branch".
4. `test_newline_separates_commands`, with subTests on_feature, each denied with "protected branch": `"git status\ngit push origin main"` and `"cat <<EOF\ngit push origin main"` (no closing line, so kept). Both fail today with `got None`.
5. `test_unparseable_push_and_continuation_still_blocked`, subTests on_feature: `"git push origin feature && echo 'oops"` denied with "could not parse"; `"git push \\\norigin main"` denied with "main". Both pass today and after.
- Tests-only commit: 91 hook tests, 4 failing (tests 1-4). tests/test_install.py's `test_vendored_hook_tests_pass_in_the_adopter` also fails, showing the same 4 (17 of 18 pass). Everything else is unchanged: both checkers pass, render checks 23/23 and 8/8, release_check.py PASS with 18 files.
- After the fix: 91 OK, 18 OK.
- Revert check: restore history_guard.py's code from origin/main against the committed tests; the same 4 fail with the same messages.
Write your own mechanism prediction in the intent. Say whether the continuation case parses as the planner expects.

## Finish line and abort conditions
Finish: the intent, the rule and the amendment, the tests-only commit, the fix and the terminal are pushed on `kit-v0.2-t12`, and a PR `kit-v0.2-t12` → `main` is open with CI green on its final commit. Then STOP and report with:
- the PR URL;
- the before and after counts;
- the failing-first output;
- the revert-check output.
Abort on:
- any Base-and-state mismatch;
- the newline premise proving false;
- a needed change outside the scope boundary, including guardlib;
- a new test failing for a reason other than the predicted one;
- two failed fixes on one symptom;
- an owner message in this planner's session log after the "Fix it in T12" answer that bears on this dispatch.

Steps, one commit each, each pushed:
1. Verify. Branch `kit-v0.2-t12` from `origin/main`.
2. D's store copy and intent `2026-09-24-11`.
3. The docstring rule and the task-list amendment, with no code.
4. Tests only; show them failing as predicted.
5. The fix; checks pass; revert check (not committed).
6. Terminal `2026-09-24-12`; all checks; PR; CI green.
7. STOP and report.

## Checks
Every commit must pass these, in `~/Zynergy/Claude-kit-fixes`; at step 4, tests 1-4 and the vendored-tests case in test_install are expected to fail:
- `python3 -m unittest discover -s .claude/hooks/tests -v`
- `python3 check_record.py --render-check`
- `python3 check_prompts.py --render-check`
- `python3 check_record.py` and `python3 check_prompts.py`
- `python3 -m unittest discover -s tests -v`
- `python3 release_check.py`
Until the fix is committed, write commit messages and record entries to files under `/tmp/kitv02_t12_*` with the Write tool, and run commit and push as separate commands. Record any block as a deviation. After step 5 you may, but need not, commit the terminal with a heredoc message containing an apostrophe, pushed in the same command, as a live check. If you do, report the hook's decision.

## Out of scope
- Every other task (T3-T11, T13-T16).
- guardlib and the other hooks.
- Changing the force, merge, filter and `gh pr merge` checks.
- Branch protection on GitHub.
- The untracked files in other harness worktrees.
- The main checkout.
- Merging, tags, Forager.

## Device items
None gate this dispatch. After the owner merges, they pull `main` in `~/Zynergy/Claude-kit`.