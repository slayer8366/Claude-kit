HEAD: 8b1c88a69086b52698fa63e6f6a05937acf5685f
Target subagent: coder
Type: build
Preserved: 2026-09-25T09:05:48Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for a small released helper, `update_worktree.py`. It fast-forwards a harness worktree to the default branch after moving aside the untracked store copies that the branch now tracks byte for byte. Those copies block every fast-forward today. Follow `.claude/agents/coder.md` at your base, including item 10 for the merge.

# Base and state
Premises to verify:
- **main.** origin/main is 2e37dfc42e01e1a37d93bb47d946f1aed01dee97 (PR #19 merge, T5). Nothing is tagged. The record's last entry is 2026-09-25-33; no intent is open. Create `kit-v0.2-update-wt` from origin/main in ~/Zynergy/Claude-kit-fixes.
- **Planner worktree W** (~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB, at 8b1c88a on branch `worktree-bridge-cse_013ve7bxjxrGv4tLa8p1kdHB`). Your hooks run from W, and your Bash cwd resets to W. Its untracked store files:
  - `2026-09-25-17`, `-18` and `-19`: on origin/main
  - D2 `-02` and D5 `-05`: not on main
  - this dispatch, expected as `2026-09-25-20.md`. If the hook gave another name, that name governs.
- **Backups.** W's kit.json has `backup_dir` "~/forager-backups" and `protected_branches` ["main"]. INDEX.md's last folder is `2026-09-25-04`.
- **Planner log.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl.

# Scope boundary
Files:
- **New `update_worktree.py`** at the repo root: standard library only, Python 3.8, with a docstring stating its rules.
- **New `tests/test_update_worktree.py`.**
- **`release.json`:** add `update_worktree.py` to `vendored`.
- **README.md:** a Layout row and a short usage section.

Record:
- this dispatch's copy and intent 2026-09-25-34
- the tests-only commit, the fix, and terminal 2026-09-25-35

No change to hooks, guardlib, kit.json, templates, install, check_kit, checkers, find_dispatches, coder.md, or CI.

# Closed decisions
The owner, in the planner log:
- "Option 2 real quick"
- the AskUserQuestion answer "Update helper (Recommended)"

Quote that option's text from its tool_use, and cite both by line and timestamp.

The planner's rules. Usage: `python3 update_worktree.py <worktree path> [--apply]`:
1. **Reads.**
   - The path must be the top level of a git worktree (`git -C <path> rev-parse --show-toplevel`).
   - Read `<path>/.claude/kit.json`. The target branch is the first of `protected_branches`, as `origin/<branch>`. `backup_dir` is read from the same file.
2. **Stops** before changing anything, printing the reasons, exit 1, if any of these hold:
   - the worktree's current branch is a protected branch, or HEAD is detached (the main checkout is updated with `git pull` by its owner, not by this tool)
   - `git rev-list origin/<branch>..HEAD` is not empty (the worktree has its own commits)
   - `git status --porcelain --untracked-files=no` is not empty (changes to tracked files)
   - any untracked file (`git ls-files --others --exclude-standard`) exists at a path in `origin/<branch>`'s tree with different bytes
   - with `--apply`: `backup_dir` is unset, or origin/<branch> is missing after the fetch
3. **Dry run** (the default). Read-only; no fetch. It compares against origin/<branch> as last fetched, and says so. It prints:
   - the plan: files to move, files left alone, the fast-forward range
   - exit 0 if the plan would succeed, 1 if not
4. **`--apply`.**
   - `git -C <path> fetch origin`, then re-check rule 2.
   - Move each untracked file identical to origin/<branch>'s into a new folder `<backup_dir>/<UTC date>-NN/` (the next free NN), under its repo-relative path. Use no-overwrite moves.
   - Write `MANIFEST.sha256` over the moved files, and append one line to `<backup_dir>/INDEX.md` naming the folder, the worktree path, `update_worktree.py`, and origin/<branch>'s SHA.
   - Then `git -C <path> merge --ff-only origin/<branch>`, and print the new HEAD.
   - If the merge fails, report it and exit 1. The moved files stay in the backup; nothing is moved back.
5. **Never touched:** untracked files not in the target tree, and ignored files. Nothing is deleted.

# Merge
authorised: this PR only (kit-v0.2-update-wt -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup. The merge command may run with cwd W. Merge nothing else.

# Prediction
- **Tests-only commit.** Each test uses a fixture with a bare origin, a main checkout, and a linked worktree on its own branch whose untracked store file is identical to one origin/main adds.
  - u1: dry run → prints the plan, changes nothing (snapshot of both checkouts, and backup_dir absent or unchanged)
  - u2: `--apply` → the file is moved, the manifest passes, one INDEX line is added, HEAD equals origin/main
  - u3: an untracked file differing from origin's → exit 1, nothing changed
  - u4: the worktree has its own commit → exit 1
  - u5: a dirty tracked file → exit 1
  - u6: the main checkout, on protected `main` → exit 1
  - u7: `--apply` with no `backup_dir` → exit 1, nothing changed
  - u8: an untracked file not in the target tree is left in place, and the fast-forward proceeds

  Each fails because the script doesn't exist ("can't open file"). tests/: 39 + 8 = 47 run, 8 failures. Hook tests: 124 OK.
- **After the fix:** 47 OK and 124 OK. Render checks 30/30 and 10/10, both checkers PASS. release_check PASS with 22 files and no denylist hit.
- **Revert check:** removing the script (not committed) gives the same 8 failures.
- **Live check, read-only:** `python3 update_worktree.py <W>`, a dry run, run after your fetch. Expected:
  - moves: `-17`, `-18`, `-19` and this dispatch's `-20` if main has it by then (before this PR merges, it won't)
  - left alone: D2, D5 and `-20`
  - fast-forward 8b1c88a..2e37dfc
  - exit 0

  **Do not run `--apply` on W.** The owner decides that.

Your mechanism prediction goes in the intent.

# Finish line and abort conditions
Finish line, pushed on kit-v0.2-update-wt:
1. the copy and the intent
2. the tests-only commit
3. the fix
4. the terminal

Then the PR with CI green on the terminal commit, then the merge under the Merge section. Report the merge commit. No tag.

Abort and report on any of these:
- any Base-and-state mismatch other than the dispatch's name
- a needed change outside scope
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit
- CI not green: don't merge
- history_guard denying the merge after the backup. Quote it; no workaround.

Owner messages after this dispatch's Agent call: an abort only if one tells you to do or not do something in this scope, or changes a decision here. Record every other message in the terminal. A planner message follows coder.md item 6.

# Checks
- Failing-first run and revert check.
- Counts before and after.
- Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
- Tests in their own Bash call.
- The merge's backup evidence and history_guard's decision.

# Out of scope
- Running `--apply` on any real worktree.
- The dispatch hook, D2, D5, the backlog, T6-T10 and T14-T16.
- Other PRs, tags, deleting anything.

# Device items
None.