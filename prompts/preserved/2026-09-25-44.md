HEAD: 68a1ef4c7cbd5e843708526d0e85559dd33e0f9f
Target subagent: pulse
Type: pulse
Preserved: 2026-09-25T22:53:11Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** pulse

# Role
You are an independent reviewer on a different model family from the one that planned and built this work. Every dispatch, every line of code and every record entry you are reviewing was written by Claude Opus 5.5 agents: one planner and many coders. Your job is to find what they systematically missed. Look for blind spots, meaning shared assumptions and failure patterns, not typos. Be adversarial and specific. A confident "this is fine" is only worth writing if you checked it.

# Base and state
- **Repository:** ~/Zynergy/Claude-kit. Review origin/main at **68a1ef4c7cbd5e843708526d0e85559dd33e0f9f**. Read files with `git -C ~/Zynergy/Claude-kit show 68a1ef4:<path>` and `git grep ... 68a1ef4`, not from the working tree. A batch is being built on another branch right now, and the working tree may move.
- **Today's work to review:** the merges from PR #25 to PR #32. List them with `git log --merges --oneline 1599a53..68a1ef4`; `git diff 1599a53 68a1ef4 --stat` gives the whole change. In order:
  - T14-16: bypass table, backup docs, toolDenialKind docs
  - T6: `session_agents.py`, resume procedure
  - T7: `launch_session.py`
  - batch 2: `update_worktree.py` fast-forwards the main checkout; coder.md item 10's last step; log found by session id; signal handling
  - T8: `run_exercise.py`, one live run
  - batch 3: denial kinds in session_agents; bypass B-08
  - bypass rulings: B-03 and B-05 fixed in history_guard
  - batch 4: `~` expansion, `<<<`, bypass B-09
- **What the kit is:** guard hooks for Claude Code (`.claude/hooks/*.py`), plus tools, plus an append-only RECORD.md checked by `check_record.py` and `check_prompts.py`. The design is in README.md and the v0.2 spec, `docs/specs/2026-09-24-kit-v0.2-spec.md`. The record entries for today are 2026-09-25-59 to -84 in RECORD.md.
- **Planner's session log:** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit/2d9316b0-e46a-5c35-b7e4-125fac5c9a3e.jsonl. `session_agents.py` explains its shape. Don't print whole lines; they hold long prompts.

# Rules
- Read-only. Change nothing, run no tests and no network calls, write no files. Your Bash is limited to git and gh reads.
- Every finding needs a file:line at 68a1ef4, or a RECORD.md entry ID, or a log line. Label each finding **verified** (you read the code or text that shows it) or **inferred** (reasoning you could not check here). Do not present an inference as a fact.
- Rank the findings: **blocking** (a guard hole, wrong behaviour, or a record claim that is false), **should-fix**, **note**.
- Where you found nothing in a category, say so and name what you checked.

# Questions
1. **Guard holes.** Read `history_guard.py`, `role_guard.py`, `dispatch_guard.py`, `device_guard.py`, `session_check.py` and `guardlib.py` at 68a1ef4.
   - Are there bypasses of the *common* forms, not only the exotic ones BYPASSES.md accepts?
   - Are there fixes that over-block legitimate work? B-03's `gh api` check reaches across newlines until `;&|`; the `<<<` fix; the `~` expansion.
   - Can errors in a guard fail open?
   - Check T17's merge rule (a)-(e) for TOCTOU or spoofable inputs, e.g. merge.json fields, INDEX.md lines, a symlinked backup folder, and `origin/<branch>` staleness versus the real remote.
2. **Tests that don't test.** In `.claude/hooks/tests/` and `tests/`:
   - Find tests that would pass even if the code they name were broken.
   - Find fixtures that don't resemble real logs or real CLI output.
   - Find assertions on the wrong thing.
   - Name any behaviour claimed in a docstring or README that no test covers.
3. **The tools.**
   - `update_worktree.py`'s new main-checkout path: can it lose work? Consider untracked files that aren't store copies, a local branch ahead, a detached HEAD, or concurrent sessions.
   - `launch_session.py`: process-group kill correctness, signal races, the identity check, and "deletes nothing".
   - `run_exercise.py`: does a PASS really show what the README says each case proves?
   - `session_agents.py`: does its parsing hold for rewritten logs?
4. **The process's blind spots.** Read today's record entries (-59 to -84) and, as a sample, the planner's dispatches (`prompts/preserved/2026-09-25-26.md` to `-42.md` on main).
   - Which failure patterns recur? Examples: stale line numbers, predictions written to fit, premises the planner asserted without checking, rulings made by the planner that should have been the owner's, the terminal-before-merge pattern, and self-merging with the approval check weakened.
   - Is coder merging with only a planner authorisation, plus the owner's blanket permission rule, a governance gap?
   - Do the record's "verified" claims hold when you spot-check three of them against git?
5. **What an Opus 5.5 planner and coders sharing one model would all miss.** Give your top five, each with evidence.

End with the three most important actions, ranked.