HEAD: 9d077470c57249c450e6e3de59651bd727e05f37
Target subagent: coder
Type: build
Preserved: 2026-09-26T02:27:48Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit review fix R6: every command a hook runs has a timeout whose expiry denies by name, the hook entries in settings.json give Claude Code a longer limit than the hooks' own, and the ls-remote denial no longer echoes git's stderr. Follow `.claude/agents/coder.md` at the base (9d07747), including items 1, 2, 5, 6, 10 and 12. Deferral is not allowed in this build.

# Base and state
Premises to verify. Line numbers are at 9d07747; drift is drift.
- **origin/pre-main** is 9d077470c57249c450e6e3de59651bd727e05f37 (the PR #38 merge). origin/main is 0319ccd. No tags. Both branches carry GitHub protection.
- **Branch:** create `kit-review-r6` from origin/pre-main in ~/Zynergy/Claude-kit-fixes (on kit-review-r4 at cc431e6, clean). Push with `git -C /home/zynergy-labs/Zynergy/Claude-kit-fixes push -u origin kit-review-r6`.
- **C** (~/Zynergy/Claude-kit) is on pre-main at 9d07747, clean but for `.claude/worktrees/`. **W**, your Bash cwd, is /home/zynergy-labs/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01RGmfuBWv4x1Nvnpa3fBQDZ at 9d07747; its only untracked file is this dispatch's copy (expected `prompts/preserved/2026-09-26-04.md`; the hook's name governs). Copy it byte for byte, keep the name, record path, size and sha256 in the intent.
- **The record** at 9d07747 ends with terminal 2026-09-26-29 (closing -28, completed). No intent is open. The sweep owes one `merge` entry: PR 38, Head kit-review-r4, Base pre-main, Merge-commit 9d077470c57249c450e6e3de59651bd727e05f37, Pre-merge 48c391dd2dd1f8c71b48c1cb3690bfe56510e6b0, Backup 2026-09-26-06, Merged-by `coder preserved/2026-09-26-03.md`, Carries 2026-09-26-27 to -29, merged 2026-09-26T02:24:02Z. Derive the values yourself. INDEX.md rows 2026-09-26-03, -05 and -07 are update_worktree moves, not merge backups.
- **Planner log F** is /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-01RGmfuBWv4x1Nvnpa3fBQDZ/d5592fbe-c371-5027-aaf9-18ab19a5613c.jsonl. Read it in a separate call before each record commit; do not print its long lines.
- **Subprocess calls at 9d07747** (git grep `subprocess.run|timeout`): history_guard.py :349 (`current_branch`, no timeout), :694 (`git bundle list-heads`, none), :718 (`rev-parse`, none), :731-733 (`git ls-remote`, `timeout=20`, `TimeoutExpired` caught; the denial quotes git's first stderr line, which can carry a remote URL); dispatch_guard.py :76 (`git()`, none) and :193 (`store_check`, runs check_prompts.py, none); device_guard.py :81 (`run()`, `timeout=60`, equal to Claude Code's default hook limit, so its own expiry can never be reported); session_check.py :57 `GIT_TIMEOUT = 20`, :66-67 uses it. guardlib.py: `run(guard)` :142-167 turns any exception into a deny with a traceback; `CONFIG_DEFAULTS` :38 has `guard_env_prefix` default `KIT_GUARD_`; the harness `run_hook(hook, payload, env=None, cwd=None, config=None)` passes `env` and runs the hook with `timeout=60` (harness.py :71, :79-81).
- **.claude/settings.json** at 9d07747: four PreToolUse hook entries (role_guard on `*`, dispatch_guard on `Agent|Task`, device_guard and history_guard on `Bash`) and one SessionStart entry, none with a `timeout` field. Claude Code's hook config accepts an optional `timeout` in seconds per command, default 60 (from Claude Code's documentation; not verifiable in this repository, state it as such).
- **README.md** at 9d07747: config table row `guard_env_prefix` :55; "## Session check" :211 says hooks are read at session start; "## Tests" :524.
- Counts at 9d07747: hook tests "Ran 175 tests" OK; tests/ "Ran 94 tests" OK (its install test mirrors hook failures); render checks 39/39 and 13/13; release_check 27 files, no match.

# Scope boundary
Files: `.claude/hooks/guardlib.py`, `.claude/hooks/history_guard.py`, `.claude/hooks/dispatch_guard.py`, `.claude/hooks/device_guard.py`, `.claude/hooks/session_check.py`, `.claude/settings.json`, hook tests (`test_history_guard.py`, `test_dispatch_guard.py`, `test_device_guard.py`, `test_session_check.py` if needed, `harness.py` if a helper is needed), `README.md`, `RECORD.md`, the store copy. No rule of any guard changes except as stated.

1. **guardlib.** A module-level timeout in seconds, default 20, overridable by the environment variable `<guard_env_prefix>TIMEOUT` (a positive number; an unreadable value keeps the default). A helper `run_command(args, **kwargs)` that calls `subprocess.run` with `capture_output=True, text=True` and that timeout, and on `subprocess.TimeoutExpired` raises `CommandTimeout`, an exception whose message names the command's first two words and the seconds. `run(guard)` turns a `CommandTimeout` into a deny "<hook>: `<cmd>` timed out after N seconds; failing closed." with no traceback; every other exception is handled as today.
2. **history_guard** uses `run_command` for `current_branch`, `list-heads`, `rev-parse` and `ls-remote` (the last with the same module timeout instead of its own 20). Its ls-remote denial no longer quotes stderr: it names the exit code and says the remote could not be read; the docstring (e) sentence follows. A `CommandTimeout` from any of these propagates to guardlib's deny.
3. **dispatch_guard** uses `run_command` for `git()`; a timeout there becomes the existing "could not preserve the dispatch" deny by way of its `RuntimeError` path or `CommandTimeout`, your choice, stated. `store_check` runs check_prompts.py with the module timeout; on expiry its report text says the checker timed out, and the decision is unchanged (the store check never blocks).
4. **device_guard** uses `run_command` (dropping its own 60), so a slow adb, aapt2 or apksigner denies by name before Claude Code's limit. **session_check** keeps its own 20 (it never blocks) but reads the same environment override, stated in its docstring.
5. **settings.json.** Each of the four PreToolUse hook entries gets `"timeout": 120`; the SessionStart entry `"timeout": 60`. Two decimal places nowhere; integers.
6. **README.** The `guard_env_prefix` row names `<prefix>TIMEOUT`. A short paragraph under "Config" or "Tests": every command a hook runs has a timeout (20 s by default) and its expiry is a deny; settings.json gives each guard 120 s so that deny is emitted before Claude Code cancels the hook; what Claude Code does with a cancelled hook is not verified from the binary and is assumed to be non-blocking, which is why the margin exists.
7. **Tests, first.** With `<prefix>TIMEOUT=1` in the hook's environment and a fake `git` first on PATH that sleeps 5 seconds then exits 0 with a plausible line: history_guard `git merge x` from a repo denies with "timed out" and "failing closed"; dispatch_guard, a complete pulse dispatch in a repository, denies with "timed out" (or "could not preserve" naming the timeout). device_guard with `<prefix>ADB` set to a script that sleeps 5 seconds, config `android_package` set, command `adb shell input tap 1 1`: denies with "timed out". A test that the `ls-remote` denial (R4's m14 fixture) with a remote URL `https://user:s3cr3t@example.invalid/x.git` never contains `s3cr3t`. Each fake script is written by the test under its temp dir; nothing sleeps longer than 5 s.

**Record**, in order: the sweep (the merge entry for #38); this copy and the intent; the tests-only commit; the fix (hooks, settings.json); the README commit; the terminal.

# Closed decisions
- The owner's D1 and "I'll take your recommendations" (F lines 157 and 165), recorded in intent 2026-09-26-02 and docs/specs/2026-09-26-review-fixes-plan.md at 9d07747 (row R6).
- Planner's choices: one shared default of 20 s; the env override name; the 120 and 60 second hook limits; no stderr in the ls-remote denial; `CommandTimeout` surfaced without a traceback; the test list.
- Standing: none cited. The merge rests on this dispatch's Merge section.

# Merge
authorised: this PR only (kit-review-r6 -> pre-main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup of origin/pre-main. One Bash call from a checkout of this clone (W is one), no `--repo`. If denied, quote the denial and stop. Then update C with the merged update_worktree.py (dry run, then `--apply`), then W the same way; both should target origin/pre-main; if either targets another branch, stop before `--apply`. Report all four outputs. Note that settings.json's new limits take effect only in new sessions.

# Prediction
Planner's outcome prediction (verbatim in the intent's planner field):
- Tests-only commit: the hook suite fails on 4 cases: the three timeout tests (today the fake git or adb is waited for and no deny names a timeout) and the credential test (today the stderr line is quoted). tests/ mirrors them through its install test. Name the exact count and any case failing for another reason.
- After the fix: hook tests 175 plus the new tests, all OK; tests/ 94 OK; render checks 39/39 and 13/13; both checkers PASS; release_check PASS, 27 files, no denylist hit (settings.json and guardlib are in the release set); CI green on the final commit.
- Revert check: the hooks and guardlib from 9d07747 under the new tests give the same 4 failures.
Put your mechanism prediction in the intent.

# Finish line and abort conditions
Finish line, pushed on kit-review-r6: (1) the sweep; (2) this copy and the intent; (3) the tests-only commit; (4) the fix; (5) the README commit; (6) the terminal, Outcome `completed`, closing at the PR open, CI green on its final commit and the backup written. Then the merge and the two updates, reported in the hand-back. No tag.

Abort and report on any of:
- a Base-and-state mismatch other than the copy's name or line drift
- any existing test failing after the fix
- a needed change to any guard's rules beyond the timeout and the stderr wording
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit
- CI not green on the final commit (no merge)
- the merge denied by anything (quote it)
- a dry run targeting a branch other than pre-main, or any dry run or `--apply` exiting 1
- an owner message in F after this dispatch that tells you to do or not do something in this scope
- a planner message that widens the scope without quoting an owner ruling

# Checks
- The failing-first run and the revert check, with the exact FAILED lines.
- Counts before and after, each suite in its own Bash call; note the suite's added wall time from the sleeping fakes.
- Both checkers before every commit touching RECORD.md; F read in a separate call before each.
- The merge entry's values against `git log --format=%H%x20%P` and `gh pr view 38`.
- The backup evidence and the merge commit's parents; the four update outputs.
- The hand-back ends with "Decisions I made", "Flags outside scope" and "Deferred".

# Out of scope
- R5 (update_worktree.py), R7 (tests/, run_exercise.py, session_agents.py, launch_session.py).
- Any guard rule; role_guard.py (it runs no subprocess).
- templates/, docs/standing-rulings.md, the owner's settings files, coder.md.
- The T3 backlog, other PRs, tags, deleting anything.

# Device items
None.