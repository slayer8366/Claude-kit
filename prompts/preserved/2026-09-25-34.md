HEAD: 9e6ff6b2c2b06bbd8fd4d73f4a04b23e415046ad
Target subagent: pulse
Type: pulse
Preserved: 2026-09-25T18:31:43Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** pulse

# Role
You are a pulse answering read-only questions for the planner, who is about to put two open questions to the owner and then write the T7 dispatch. The sources:
- T7 task row (`docs/specs/2026-09-24-kit-v0.2-tasks.md:14`): "A launcher that starts a session with a time limit, checks it is the expected one, and cleans up on failure; tested with a hanging-command fixture".
- Spec item 7 (`docs/specs/2026-09-24-kit-v0.2-spec.md:28`).
- The open question at spec :49: "T7: the launcher's time limit, and what 'expected session' is checked against".
- T8 depends on T7 (tasks :15): "A runner that drives the exercise cases and writes the evidence lines".

# Base and state
- ~/Zynergy/Claude-kit on main at 9e6ff6b2c2b06bbd8fd4d73f4a04b23e415046ad.
- Session logs are under /home/zynergy-labs/.claude/projects/.
- The planner's log is /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit/2d9316b0-e46a-5c35-b7e4-125fac5c9a3e.jsonl.

# Rules
- Read-only. Change no file.
- Never print a whole prompt or log line. Cut string values to about 150 characters.
- Every claim needs a file:line, or a log path and line. Mark anything inferred "inferred".

# Questions
1. **Origin.** Where does item 7 come from? Find the RECORD.md entries and audits describing:
   - how sessions were launched for live exercises in v0.1 and the v0.1 fixes (for example `claude -p`, headless runs, the exercise of the dispatch hook, 2026-09-23-10);
   - what went wrong: hangs, wrong session or worktree, stale hooks, leftovers.

   Quote the lines that motivated "time limit", "expected session" and "cleanup". Check RECORD.md 2026-09-23-04 (R6) and -07 (deferred list) first.
2. **Past launches.** For each past live exercise, give:
   - the exact command used to start the session, and the flags;
   - the cwd or worktree;
   - how its session log was found afterwards;
   - how long it ran.

   Cite the entries and any scripts (workshop/, docs/audits/).
3. **Session identity.** In session logs, which fields identify a session and its checkout, and are they on the first lines? Candidates: sessionId, cwd, gitBranch, version, entrypoint, the SessionStart hook attachments that session_check writes. Give examples from a headless session's log if one exists under ~/.claude/projects (look for `entrypoint` values such as "sdk-cli" or "cli").
4. **Leftovers.** What does a harness session leave behind that "cleanup" might mean? For example:
   - `.claude/worktrees/bridge-cse_*` directories and their `worktree-bridge-cse_*` branches (`git worktree list`, `git branch --list 'worktree-*'`);
   - tasks/ output files;
   - hook-saved dispatch copies in prompts/preserved.

   Which worktrees exist now, and at which commits?
5. **Exercise cases (for T8's needs).** Are they listed anywhere (workshop/, docs/audits/, RECORD.md), and what does each case require of a launched session: its prompt, its agent type, and the log lines expected as evidence?
6. **Existing code.** Does any existing code spawn processes with timeouts that T7 could follow in style (install.py, update_worktree.py, tests using subprocess with `timeout=`)? Give file:line.
7. **Python.** What does the kit require (CI matrix in .github/workflows/ci.yml, README), and do any tools use `subprocess.run(timeout=...)`, process groups or `os.killpg`?