HEAD: 93904076a95498b3a877438f6d354fc1b1b88419
Target subagent: coder
Type: build
Preserved: 2026-09-23T22:39:08Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Claude-kit v0.1 fixes: start a Remote Control session in ~/Zynergy/Claude-kit for the live exercise

## Role
You are the coder. You execute this dispatch, you make no design decisions, and a gap is a stop-and-ask. Record it as `coder.md` and the checkers require (in the fix worktree's RECORD.md), and run `check_record.py` and `check_prompts.py` before every commit that touches `RECORD.md`.

## Base and state
- `~/Zynergy/Claude-kit-fixes`: branch `kit-v0.1-fixes`, HEAD = `origin/kit-v0.1-fixes` = `0e7f5f9`, clean. Open intents: 2026-09-23-04 and 2026-09-23-06.
- `~/Zynergy/Claude-kit`: detached HEAD at `0e7f5f9`, clean apart from `.claude/worktrees/`. `git worktree list` there shows the fix worktree plus seven harness worktrees under `.claude/worktrees/`, all at `9390407`.
- `origin/main` = `9390407`. No tags.
Verify all of this first; if anything differs, stop.

What happened (for the record, observed by the planner and the owner, 2026-09-23): the owner's first attempt at the live exercise ran from the Claude mobile app. Each session the app started got a new harness worktree at `9390407` (the Remote Control server on this machine is spawning sessions in worktree mode), so all four sessions ran v0.1's hooks, not the fixes. In addition, the prompts reached each session's planner as plain messages rather than as dispatches, and in the mismatch case the planner changed the target to `pulse` itself; that pulse ended without a report (the v0.1 handback bug). None of the four cases was exercised against the fixes. The four harness worktrees `bridge-cse_01RDr69LzAyJukJWKHmde1ii`, `01VoEUxPfehfyrQFsE7Gzekz`, `01FtdNjEDPodJQK4fcRRigJp`, `01Y7ufbWujREADW8ggjVZPyA` came from that attempt; leave them alone.

## Scope boundary
Start one dedicated Remote Control session whose working directory is `~/Zynergy/Claude-kit` itself, reachable from the owner's phone, so the owner can run the live exercise there. Record this dispatch. Nothing else. Do not stop, restart or signal any existing `claude` process (one of them serves the planner session that sent this). Do not change Claude Code settings (`~/.claude/settings.json`, any `.claude/settings*.json`). Never touch `.claude/worktrees/*` (reading is fine), Forager, or any tag. In `~/Zynergy/Claude-kit`, the only permitted change is the one `git checkout --detach` in step 3.

## Closed decisions
Owner, 2026-09-23: "So make it so I can run the tests here" (from the phone app). Therefore:
- F1: Remote Control server mode supports `--spawn session` (single-session mode: serves exactly one session and rejects additional connections) and pre-creates its session in the current directory by default (`--create-session-in-dir`, on by default), per https://code.claude.com/docs/en/remote-control. Start, from `~/Zynergy/Claude-kit`, detached so it outlives your shell: `claude remote-control --spawn session --name "Kit live exercise"`, stdout and stderr to `/tmp/kitfix_rc_exercise.log`. No `--permission-mode` flag: the session must use the default mode so the approval prompt reaches the owner. Use the `claude` on PATH; report its path and `claude --version`.
- F2: before starting, confirm that this version supports those flags (`claude remote-control --help`). If `--spawn session` is not supported, or the command asks an interactive question, or refuses to start because it is launched from inside another Claude Code session, stop and report the exact message. Do not work around it (no environment-variable changes, no other flags).
- F3: after starting, confirm from the log that the server is up and report the session URL it prints, and the process ID. The owner opens "Kit live exercise" in the app.
- F4, record: a new intent 2026-09-23-07 claims this dispatch's saved copy, which the v0.1 hook saved in the harness worktree `~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01JDbTJ4Gj9C6xygXM5tPdHz/prompts/preserved/` (identify it by content and header time). Copy it byte for byte into the fix worktree as the next free number (expected `2026-09-23-05.md`), sha256 on both sides, original left untracked and unedited. Intent 07 also records the failed first attempt described in "Base and state" as an observation. It stays open; its terminal is written after the exercise, once the server is stopped (not in this dispatch).
- F5, numbering: commit and push the record BEFORE starting the server, then move `~/Zynergy/Claude-kit` to the new tip with `git checkout --detach <tip>`, after checking with `git diff --stat 0e7f5f9 <tip>` that only `RECORD.md` and `prompts/preserved/` differ and nothing under `.claude/`. That way the exercise's saved dispatches in that checkout are numbered after this one, and no number names two different files.

## Prediction
- The record commit passes both checkers.
- After step 3, `~/Zynergy/Claude-kit` is detached at the new tip, clean apart from `.claude/worktrees/`, with `.claude/` identical to `0e7f5f9`.
- The server log shows a session URL; the process is running with working directory `~/Zynergy/Claude-kit` (check `/proc/<pid>/cwd`).

## Finish line and abort conditions
Finish: record committed and pushed, `~/Zynergy/Claude-kit` at the new tip, the server running, then STOP and report "Exercise session ready" with the session name, URL, PID, log path, `claude` path and version. Abort on any Base-and-state mismatch, any F2 condition, any diff outside `RECORD.md` and `prompts/preserved/`, or anything that would require touching an existing `claude` process or settings.

Steps:
1. Record per F4; run all checks; commit; push.
2. F5 diff check.
3. `git -C ~/Zynergy/Claude-kit checkout --detach <tip>`; confirm.
4. F2 check, then start the server per F1; confirm per F3.
5. STOP and report.

## Checks
On the record commit, in the fix worktree: `python3 -m unittest discover -s .claude/hooks/tests -v`, `python3 check_record.py --render-check`, `python3 check_prompts.py --render-check`, `python3 -m unittest discover -s tests -v`, `python3 release_check.py`.

## Out of scope
The live exercise itself, stopping the server, restoring `kit-v0.1`, removing any harness worktree, the terminals for intents 04, 06 and 07, the completion report, the PR, and any v0.2 item.

## Device items
None.