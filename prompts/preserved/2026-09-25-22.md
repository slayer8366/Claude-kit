HEAD: 3847590469634ed45759e429ce7900be07a4d169
Target subagent: coder
Type: build
Preserved: 2026-09-25T10:26:16Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for a batch of four small items. Follow `.claude/agents/coder.md` at your base, including item 10 for the merge.

# Base and state
Premises to verify:
- **main.** origin/main is 22723e0a3993dffd911ffb20267ad79cabb4f669 (PR #21 merge). Nothing is tagged. The record's last entry is 2026-09-25-37; no intent is open. Create `kit-v0.2-batch1` from origin/main in ~/Zynergy/Claude-kit-fixes.
- **Planner worktree W** (~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB, at 3847590). Your hooks run from W, and your Bash cwd resets to W. Its untracked store files:
  - D2 `-02` and D5 `-05`: leave them
  - `-21`: on main
  - this dispatch, expected as `2026-09-25-22.md`. If the hook gave another name, that name governs.
- **Planner log.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl. The planner session was restarted by the owner and resumed with context. Find:
  - the owner's "New session" message
  - the SessionStart additional context the hook injected: "Claude-kit session_check: this session's hooks may be stale … HEAD is 3 commits behind origin/main …"

  Report their line numbers and timestamps, and whether the log records a hook entry or attachment for it.
- **Code** (verify each):
  - session_check.py's advice line gives `git … fetch && git … merge --ff-only origin/<branch>`
  - `update_worktree.py` sits at the repo root and is released
  - README's Layout `tests/` row and its "Tests" section
  - `docs/standing-rulings.md`: B-11's "Enforced by" says not yet enforced, pending T17
  - history_guard.py's merge conditions (T17)

# Scope boundary
The four items:
1. **`.claude/hooks/session_check.py`.**
   - If `<toplevel>/update_worktree.py` exists, the update advice becomes:
     "`python3 <toplevel>/update_worktree.py <toplevel>` (dry run), then the same with `--apply`. It moves aside untracked copies that the branch now tracks, then fast-forwards."
   - Otherwise the advice stays as it is.
   - Add test t8 in `.claude/hooks/tests/test_session_check.py`, failing first.
2. **README.md:** the Layout `tests/` row and the "Tests" section name all four test files in tests/: install/drift, release_check, find_dispatches and update_worktree. Docs only.
3. **`docs/standing-rulings.md`:** a new entry, B-13, superseding B-11.
   - B-11's ruling text word for word, including the owner's line-712 words.
   - Enforced by: history_guard.py's conditions (a)-(e), with lines at your base, plus coder.md item 10.
   - Evidence: the four coder merges so far, #17, #19, #20 and #21, each with its backup folder in ~/forager-backups (`2026-09-25-03`, `-04`, `-05`, `-07`).
   - Add `Superseded-by: B-13` to B-11, and one line to "Superseded rulings".
   - No `Accepted:` line.
   - Write the text with Edit/Write, since it names the merge command.
4. **Record T5's owner check** in the intent, as a field `Owner check (T5):`. Cite the lines found above, and quote the injected context's first two lines. Note that "3 commits behind" matched the three commits of the #21 merge, and that no `.claude/` path was listed.

Record:
- this dispatch's copy and intent 2026-09-25-38
- the tests-only commit (t8), the fix commit (items 1-3), and terminal 2026-09-25-39

No other file changes.

# Closed decisions
The owner, in the planner log. Cite by line and timestamp:
- "do 1 and 2 together sequentially, then 3". It answers the planner's list, whose item 1 reads: "A small batch of fixes: the warning's update advice; README's tests row; B-11's 'not enforced' wording in the rulings file; recording your T5 check".
- The rulings-file rules: supersede, don't edit, as in the file's header.

# Merge
authorised: this PR only (kit-v0.2-batch1 -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup. The merge command may run with cwd W. Merge nothing else.

# Prediction
- **Tests-only commit:** t8 builds a fixture repo whose toplevel has an `update_worktree.py` file and which is behind origin. It asserts that the advice contains `update_worktree.py` and `--apply`. It fails today, because the advice has only the git commands. Hook tests: 125 run, 1 failure.
- **After the fix:**
  - 125 hook tests OK, and t1-t7 unchanged
  - 47 tests/ OK
  - render checks 30/30 and 10/10, both checkers PASS
  - release_check PASS with 22 files
  - `grep -c '^\*\*Accepted:\*\*' docs/standing-rulings.md` is 0
  - the rulings diff has exactly one removed line, if "None yet." is gone, or none; report which
- **Revert check:** session_check.py from base gives the same 1 failure.
- **Live check:** W's own session_check still gives the old advice, because W is at 3847590. Your branch's copy, run with cwd W as a script, gives the new advice (W has no update_worktree.py either, so for this check point `<toplevel>` at the fixes checkout). Show both.

Your mechanism prediction goes in the intent.

# Finish line and abort conditions
Finish line, pushed on kit-v0.2-batch1:
1. the copy and the intent
2. the tests-only commit
3. the fix
4. the terminal

Then the PR with CI green, then the merge under the Merge section. Report the merge commit. No tag.

Abort and report on any of these:
- any Base-and-state mismatch other than the dispatch's name
- a needed change outside scope
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit
- CI not green: don't merge
- history_guard denying the merge after the backup: quote it

Owner messages after this dispatch's Agent call: an abort only if one tells you to do or not do something in this scope, or changes a decision here. Record every other message in the terminal. A planner message follows coder.md item 6.

# Checks
- Failing-first run, revert check, counts before and after.
- Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
- Tests in their own Bash call.
- The merge's backup evidence and history_guard's decision.

# Out of scope
- T10, the T3 backlog, D2, D5, updating W.
- Other rulings-file entries.
- Other PRs, tags, deleting anything.

# Device items
None.