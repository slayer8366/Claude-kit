HEAD: c196c9de8465bed6d5072266462424d05aa8f3d2
Target subagent: coder
Type: build
Preserved: 2026-09-25T20:14:51Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit v0.2 task T8 (`docs/specs/2026-09-24-kit-v0.2-tasks.md:15`; spec item 8 at `docs/specs/2026-09-24-kit-v0.2-spec.md:29`). Follow `.claude/agents/coder.md` in ~/Zynergy/Claude-kit, including items 1, 6, 8, 10 (with its new last step) and 11.

# Base and state
Premises to verify:
- **main.** origin/main is c196c9de8465bed6d5072266462424d05aa8f3d2 (PR #28 merge, batch 2). No tags. The record's last entry is 2026-09-25-74 (the batch-2 terminal), and no intent is open.
  - Create `kit-v0.2-t8` from origin/main in ~/Zynergy/Claude-kit-fixes.
  - Push with `git -C ~/Zynergy/Claude-kit-fixes push -u origin kit-v0.2-t8`.
- **C** is ~/Zynergy/Claude-kit, on main at c196c9d, with only `.claude/worktrees/` untracked, plus this dispatch's copy saved by the hook (expected `-37.md`; the hook's name governs). Your hooks and Bash cwd are C.
- **No sweep is needed.** Every store file on main is claimed; -36 is claimed by -73.
- **Planner log B** is /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit/2d9316b0-e46a-5c35-b7e4-125fac5c9a3e.jsonl. Cite by timestamp and tool_use id; `python3 session_agents.py <B>` lists the Agent calls.
- **Tools on main to build on:**
  - `launch_session.py`: pre-checks, hooks check, `--session-id`, stream-json, `--permission-prompts none`, `--max-budget-usd`, `--model`, time limit, signal handling, log found by session id with the `LAUNCH_SESSION_PROJECTS_ROOT` override. Exit codes: 0 finished, 3 timed out, 4 wrong session, 5 failed to start, 130 interrupted.
  - `session_agents.py`.
  - `find_dispatches.py`.
  - README "Blocked calls in the session log" (:221-229 or thereabouts): a blocked dispatch shows as `toolDenialKind` on the tool_result, not as a `deny` decision.
- **The four exercise cases**, from the T7 pulse's hand-back in B:
  - Owner ruling item 5 (RECORD.md:109-111): "a pulse, a build awaiting approval, a blocked mismatch, and a blocked unknown agent".
  - D7 (RECORD.md:122): the unknown agent is `general-purpose`; the mismatch is a `Type: pulse` prompt sent to coder.
  - The v0.1 evidence is at docs/audits/2026-09-23-kit-v0.1-fixes-completion-report.md:94-105:
    - case 1: `permissionDecision` "allow", then the pulse's hand-back.
    - case 2: "ask", then "Denied by user" with `toolDenialKind` "user-rejected".
    - case 3: "dispatch_guard: Type 'pulse' may only be dispatched to 'pulse', not 'coder'.", with `is_error` and `toolDenialKind` "permission-rule".
    - case 4: "subagent type 'general-purpose' may not be dispatched…", with "permission-rule".
  - dispatch_guard's decision appears as an `attachment` of type `hook_success` with `hookName` "PreToolUse:Agent", `toolUseID`, and `stdout` holding the decision JSON.

# Scope boundary
**Files:**
- new `run_exercise.py` at the repo root
- new `tests/test_run_exercise.py`
- `release.json`: add `run_exercise.py` to `vendored`
- README.md: a new section "Live exercise", the Layout row, and the Tests text
- `docs/specs/2026-09-24-kit-v0.2-tasks.md`: only if T8's done-when needs the case-2 wording (see below)

Nothing else changes.

**run_exercise.py** (Python 3.8+, standard library only; the module docstring states the rule first).

Usage: `python3 run_exercise.py --repo <checkout> --out-dir <dir> [--model claude-opus-5-5] [--max-budget-usd 2.00] [--timeout 600] [--cases 1,2,3,4] [--claude <path>]`.

1. **Worktree.** Create a detached worktree of `origin/<first protected branch>` (as last fetched; the tool does not fetch) at `<out-dir>/worktree`. It does not remove it. It prints the path, and says the coder or owner records its store copies (find_dispatches.py) and then removes it with `git worktree remove`.
2. **Cases.** For each case, write its prompt to `<out-dir>/case-N.prompt` and run `launch_session.py` from the same checkout as run_exercise.py with:
   - `--cwd <worktree>`, `--out <out-dir>/case-N.jsonl`
   - the model, budget and timeout; the defaults are `claude-opus-5-5`, 2.00 and 600
   - the launcher's `--permission-prompts none`
3. **Prompts.** Each prompt tells the session to make exactly one Agent call, in the foreground (`run_in_background` false), sending the text below the marker unchanged to the named subagent type, and then to stop. The dispatched texts:
   - (1) `**Type:** pulse` to `pulse`, with the pulse sections kit.json requires. The question needs no permission prompt: "Read README.md and report its first line" (a Read, never Bash).
   - (2) `**Type:** build` to `coder`, with every section kit.json requires for a build (Merge included). Its body says to do nothing and hand back "exercise case 2".
   - (3) `**Type:** pulse` to `coder`.
   - (4) any `**Type:** pulse` text to `general-purpose`.
4. **Evidence.** For each case, find the session's log (from the launcher's report) and parse it as JSON. Do not match on key order or line numbers. Take the case's Agent tool_use, the dispatch_guard `hook_success` attachment for that `toolUseID`, and the tool_result for it. Expected results:
   - case 1: decision allow; a completed result with an agentId, and a hand-back or completed result text.
   - case 2: decision ask; then a tool_result with `is_error` and a `toolDenialKind`. Report the actual value. No subagent transcript for that call.
   - case 3: a tool_result with `is_error`, `toolDenialKind` "permission-rule", and dispatch_guard's mismatch text.
   - case 4: the same, with the unknown-agent text (dispatch_guard's or role_guard's; report which).
   - Blocked calls are matched on `toolDenialKind`, never on a `deny` decision (T16's rule, moved to T8).
5. **Output.**
   - Write `<out-dir>/evidence.txt`: one line per case with the case, session id, log path, tool_use id, hook decision, `toolDenialKind`, the first 150 characters of the result text, and PASS or FAIL with the reason.
   - Print the same lines.
   - Exit 0 if every run case passes, 1 if any fails, 2 on a usage error. A launcher exit other than 0 is that case's FAIL, quoting the launcher's report.
6. **Scope of action.** It deletes nothing. It writes only under `<out-dir>`, plus the worktree it creates.

**README "Live exercise":** the four cases and what each proves, the evidence rules (toolDenialKind, not deny), case 2's auto-deny meaning, the cost cap, and the worktree clean-up.

**T8 done-when:** if the live run shows case 2's `toolDenialKind` differs from v0.1's "user-rejected", note it in the README, and amend the task row only if its wording no longer holds.

**Record:**
1. this dispatch's copy and the intent (with the correction field below)
2. the tests-only commit
3. the fix
4. the live run
5. a record commit with dispatch-notes for the store copies the live run leaves in the worktree
6. the terminal

Then the PR, the merge, and item 10's update of C.

**Correction to 2026-09-25-74** (the intent carries a `**Correction to 2026-09-25-74:**` field, as RECORD.md:419 does). Terminal -74 said `completed`, but at that time its finish line's merge and C update had not happened. From the batch-2 coder's hand-backs and B (cite by timestamp):
- The first `gh pr merge 28 --merge` was denied by Claude Code's auto-mode classifier, "[Merge Without Review]". Quote it verbatim from the hand-back.
- The owner's "Go ahead and merge when ready" (B 2026-09-25T19:37:26.868Z) was relayed by the planner's SendMessage `toolu_01NbinDAuBA71sHFFzFf9tGH`, and the second attempt was denied the same way.
- The owner's "settings.json updated with the permissions" (B 19:47:31.755Z) was relayed by SendMessage `toolu_01LnnznzhNEmHr7b7XEdr7qU`. The third attempt merged PR #28 at 19:48:01Z: merge commit c196c9de8465bed6d5072266462424d05aa8f3d2, parents cedeb52 and f986630.
- C was updated by update_worktree.py: dry run and `--apply` exit 0, 4 files moved to ~/forager-backups/2026-09-25-18, fast-forward 9e6ff6b..c196c9d; session_check was silent.
- The coder stopped at the correction because check_record has no standalone correction kind (check_record.py:117, :155, :302, :428-449). The planner chose this field.

# Closed decisions
- The owner's AskUserQuestion answers in B, after the planner's T8 summary. Quote the option texts from the tool_use:
  - Where: "Temporary worktree (Recommended)"
  - Case 2: "Auto-deny counts (Recommended)"
  - Budget: "Haiku, $0.50 per case (Recommended)", then overridden by the owner (two messages, verbatim): "Use Opus 5.5 not Haiku".
- The planner set the cap to $2.00 per case, the other budget option's figure, because $0.50 is too low for Opus with a subagent. The model is `claude-opus-5-5`.
- The owner: "Go ahead and first fix the cause … then send the usual move-and-pull and then T8." (B, before batch 2.) The move-and-pull was done by batch 2's post-merge update.
- The planner's choices: the script's name, releasing it, exit codes, the evidence line format, and `--cases`.
- B-11/B-13: coders merge at the planner's discretion.

# Merge
authorised: this PR only (kit-v0.2-t8 -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup. Run it from cwd C without `--repo`, as one Bash call. If it is denied, quote the denial and stop. Merge nothing else. Then do item 10's update of C.

# Prediction
**Tests-only commit.** `tests/test_run_exercise.py` uses a fake `claude` (through `--claude`, passed on to the launcher). The fake writes a fixture log under `LAUNCH_SESSION_PROJECTS_ROOT` for the session id it is given, and prints a matching init. Cases:
- (a) all four fixture logs as expected: exit 0, four PASS lines
- (b) case 3's log with a `deny` decision but no `toolDenialKind`: FAIL
- (c) case 2's log where the call ran (no denial): FAIL
- (d) case 1 with no result: FAIL
- (e) launcher exit 4: that case FAILs, quoting the report
- (f) `--cases 3`: only case 3 runs
- (g) the worktree is created detached at origin/<branch> and not removed
- (h) nothing is written outside `<out-dir>`
- (i) bad arguments: exit 2

Before the fix, all fail because the script is absent. The 137 hook tests stay OK.

**After the fix:** all tests/ pass; 137 hook tests OK; render checks 30/30 and 13/13; both checkers PASS; release_check PASS with 27 files and no denylist hit (grep the new text first).

**Revert check:** revert the whole fix (the script and release.json). The same failures return.

**Live run (one, not retried):** `python3 run_exercise.py --repo ~/Zynergy/Claude-kit-fixes --out-dir <scratch>/exercise`. Expected:
- cases 1, 3 and 4 PASS as in v0.1
- case 2: ask, then a denial with a `toolDenialKind` you report
- under $8 in total

Paste evidence.txt. Record the worktree's store copies:
- case 1's and case 2's dispatches, if dispatch_guard saved them: dispatch-notes, pulse `answered` and build refused/declined, whichever outcome check_record accepts, as in the v0.1 exercise's notes
- any refused copies for cases 3 and 4

Then remove only that worktree.

Put your mechanism prediction in the intent.

# Finish line and abort conditions
**Finish line**, pushed on kit-v0.2-t8:
1. the copy and the intent (with the correction)
2. the tests-only commit
3. the fix
4. the exercise record commit
5. the terminal

Then CI green on the final commit, the backup, the merge with `--merge`, and the merge commit reported; C updated by item 10's last step; the exercise worktree removed after its copies are recorded. No tag.

**Abort and report on:**
- any Base-and-state mismatch other than the dispatch's name
- a needed change outside scope (the hooks, launch_session.py, session_agents.py)
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit
- the live run exceeding $8, or a case hitting its cap (report it; no retry)
- CI not green (no merge)
- the merge denied by anything (quote it)
- the dry run or `--apply` exiting 1
- an owner message after this Agent call that tells you to do or not do something in this scope, or changes a decision here
- a planner message that widens the scope or changes a closed decision without quoting an owner ruling

A live case FAIL is not an abort: record it and report it.

# Checks
- The failing-first run and the revert check.
- Counts before and after.
- Both checkers before every commit that touches RECORD.md; read B in a separate call before each.
- Tests in their own Bash call.
- `pgrep -f` for the fakes afterwards.
- evidence.txt, and the live cost per case.
- find_dispatches output for the exercise worktree.
- The backup evidence and the merge commit's parents.
- The C update outputs.

# Out of scope
- Changing any hook or the launcher.
- Removing any worktree other than the exercise one.
- D2 and D5.
- Other PRs, tags, and deleting anything else.

# Device items
None.