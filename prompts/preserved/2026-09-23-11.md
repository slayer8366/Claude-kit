HEAD: 93904076a95498b3a877438f6d354fc1b1b88419
Target subagent: coder
Type: build
Preserved: 2026-09-23T22:49:56Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Claude-kit v0.1 fixes: end the stray process, start "Kit live exercise" in ~/Zynergy/Claude-kit

## Role
You are the coder. You execute this dispatch, you make no design decisions, and a gap is a stop-and-ask. Record it as `coder.md` and the checkers require (in the fix worktree's RECORD.md), and run `check_record.py` and `check_prompts.py` before every commit that touches `RECORD.md`.

## Base and state
- `~/Zynergy/Claude-kit-fixes`: branch `kit-v0.1-fixes`, HEAD = `origin/kit-v0.1-fixes` = `0e7f5f9`, clean. Open intents: 2026-09-23-04 and 2026-09-23-06.
- `~/Zynergy/Claude-kit`: detached HEAD at `0e7f5f9`, clean apart from `.claude/worktrees/`.
- `origin/main` = `9390407`. No tags.
- PID 100263 is `claude remote-control --help`, started by the previous coder from the harness worktree `bridge-cse_01JDbTJ4Gj9C6xygXM5tPdHz`, hung with no output. The owner sees no new session in the app from it.
- Two earlier dispatches were hook-saved in that harness worktree's `prompts/preserved/` and never recorded: `2026-09-23-05.md` (the stopped launcher dispatch, sha256 `009446d4672d76fbdc3a93848b74715ef13c21be4021beb92115c4b121072c8c`) and, for this one, the next file (expected `2026-09-23-06.md`; identify by content and header time).
Verify all of this first; if anything differs, stop.

## Scope boundary
End PID 100263; record this dispatch; move `~/Zynergy/Claude-kit` to the record tip; start one interactive Claude Code session with Remote Control in `~/Zynergy/Claude-kit`, inside a detached tmux session so it has a terminal and outlives your shell. Nothing else. Do not signal any other `claude` process (PID 71870 `claude rc` and the four `--print` sessions stay untouched). Do not change Claude Code settings. Never touch `.claude/worktrees/*` (reading is fine), Forager, or any tag. In `~/Zynergy/Claude-kit`, the only permitted change is the one `git checkout --detach`.

## Closed decisions
Owner, 2026-09-23: "Try it now." (after the planner proposed `cd ~/Zynergy/Claude-kit && claude --rc "Kit live exercise"` and ending PID 100263). Earlier: "You should think of ways that code can do things for me instead, and safeguard those ways so I can use them confidently." Therefore:
- G1: end PID 100263 with SIGTERM; if it is still alive after 10 seconds, SIGKILL. Before signalling, confirm `/proc/100263/cmdline` is still `claude remote-control --help`; if not, do not signal and stop.
- G2, record: a new intent 2026-09-23-07 claims THIS dispatch's saved copy, copied byte for byte into the fix worktree as the next free number (expected `2026-09-23-05.md` in the fix store), sha256 on both sides, original untracked and unedited. The intent cites the stopped launcher dispatch (harness `2026-09-23-05.md`, sha256 above) by path and hash as not copied, the hang of `claude remote-control --help` as an observation, and the owner's failed first exercise attempt (app sessions spawned in worktrees at `9390407`, prompts answered by the planner, the mismatch rerouted to pulse, that pulse without a report). Deferred to v0.2 in the intent, not built: a session-start check that warns when a session's checkout or hooks differ from the expected ones; a live-exercise runner; a session launcher with a time limit, a check for the expected session, and cleanup on failure; a tool for recording hook-saved dispatches (copy, sha256, numbering, claim); a path for sending a prompt to a named agent unchanged. Intent 07 stays open; its terminal comes after the exercise, once the session is stopped.
- G3: commit and push the record, then `git diff --stat 0e7f5f9 <tip>` must show only `RECORD.md` and `prompts/preserved/`, nothing under `.claude/`; then `git -C ~/Zynergy/Claude-kit checkout --detach <tip>`.
- G4, launch, with safeguards: confirm `tmux` exists (if not, stop). Confirm no tmux session named `kit-live-exercise` exists (if one does, stop). Start: `tmux new-session -d -s kit-live-exercise -c /home/zynergy-labs/Zynergy/Claude-kit 'claude --rc "Kit live exercise"'`. No permission-mode flag. Then, within a 90-second limit, poll `tmux capture-pane -p -t kit-live-exercise` and look for evidence that Remote Control connected (an `/rc active` indicator, a session URL, or a Remote Control message). If the pane shows a trust dialog, an interactive question, an error, or nothing conclusive by the limit: capture the pane text to `/tmp/kitfix_rc_pane.txt`, kill ONLY the tmux session `kit-live-exercise`, confirm its processes are gone, and stop and report. Do not answer any dialog and do not try other flags or environment changes.
- G5: on success, confirm the `claude` process under that tmux session has working directory `/home/zynergy-labs/Zynergy/Claude-kit` (`/proc/<pid>/cwd`), and save the pane text to `/tmp/kitfix_rc_pane.txt`. Leave the session running.

## Prediction
- G1: PID 100263 exits on SIGTERM.
- The record commit passes both checkers; the checkout moves to the new tip with `.claude/` identical to `0e7f5f9`.
- G4: the pane shows Remote Control connected within 90 seconds, and the session appears in the owner's app as "Kit live exercise". (Unverified: whether the trust dialog was accepted for that directory before; if not, G4 stops.)

## Finish line and abort conditions
Finish: PID 100263 gone; record committed and pushed; `~/Zynergy/Claude-kit` at the new tip; "Kit live exercise" running under tmux in that directory; then STOP and report "Exercise session ready" with the tip hash, the tmux session name, the claude PID and cwd, the pane evidence, and how to stop it (`tmux kill-session -t kit-live-exercise`). Abort on any Base-and-state mismatch, any G1/G4 stop condition, any diff outside `RECORD.md` and `prompts/preserved/`, or anything needing another process or settings to be touched.

Steps:
1. G1.
2. G2; run all checks; commit; push.
3. G3.
4. G4, then G5.
5. STOP and report.

## Checks
On the record commit, in the fix worktree: `python3 -m unittest discover -s .claude/hooks/tests -v`, `python3 check_record.py --render-check`, `python3 check_prompts.py --render-check`, `python3 -m unittest discover -s tests -v`, `python3 release_check.py`.

## Out of scope
The live exercise itself, stopping the exercise session, restoring `kit-v0.1`, removing harness worktrees, the terminals for intents 04, 06 and 07, the completion report, the PR, and building any v0.2 item.

## Device items
None.