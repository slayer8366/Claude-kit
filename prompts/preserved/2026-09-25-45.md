HEAD: 68a1ef4c7cbd5e843708526d0e85559dd33e0f9f
Target subagent: coder
Type: build
Preserved: 2026-09-25T23:07:44Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for a small Claude-kit v0.2 fix. find_dispatches.py should run git with automatic housekeeping off, so that git itself cannot write to the repository and break its read-only promise. Follow `.claude/agents/coder.md` in ~/Zynergy/Claude-kit, including items 1, 6, 8, 10 (with its last step) and 11.

# Base and state
**Wait first.** Batch 5's PR #33 (kit-v0.2-batch5) is still open; its coder is finishing the terminal and the merge. Both tasks append to RECORD.md, so start only after #33 is MERGED.
- Wait with one Bash call: `timeout 2700 bash -c 'until [ "$(gh pr view 33 --repo slayer8366/Claude-kit --json state -q .state)" = MERGED ]; do sleep 60; done'`.
- If it times out, or #33 is closed without merging, stop and report.
- Then `git -C /home/zynergy-labs/Zynergy/Claude-kit-fixes fetch origin`, and run C's update if batch 5's coder has not (report which).

Premises to verify after the wait:
- **origin/main** is #33's merge commit. Report it. No tags. The record's last entry is batch 5's terminal (expected 2026-09-25-86); no intent is open.
- **Branch.** Create `kit-v0.2-fd-gc` from origin/main in ~/Zynergy/Claude-kit-fixes, and push with `git -C /home/zynergy-labs/Zynergy/Claude-kit-fixes push -u origin kit-v0.2-fd-gc`.
- **C** is ~/Zynergy/Claude-kit, on main at that merge after item 10's update. Untracked there:
  - `.claude/worktrees/`
  - `prompts/preserved/2026-09-25-44.md`: the Fable review pulse, unrecorded, and not on main
  - this dispatch's copy (expected `-45.md`; the hook's name governs)
- **Sweep.** Copy -44 into the store with a dispatch-note: Type pulse, Outcome `answered`. Cite its hand-back in planner log B: the Fable 5.1 review of PRs #25-#32, run with a model override. Also cite the owner's added rating request, relayed by the planner's SendMessage.
- **Planner log B:** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit/2d9316b0-e46a-5c35-b7e4-125fac5c9a3e.jsonl.
- **The failure** (batch 5's hand-back and terminal): CI push run 36198938998, Python 3.14 job, `tests/test_find_dispatches.py` `test_read_only` (:265, `assertEqual(sorted(before), sorted(after))`). The fixture's `.git` changed while find_dispatches.py ran: a new `main/.git/info/refs` appeared, and 31 paths present before were missing after. It passed 25/25 locally and on the next commits.
- **Planner's hypothesis (unverified):** a git command in find_dispatches.py triggered `gc --auto` (or `maintenance --auto`) in the fixture repo. The repack runs `update-server-info` (which writes `info/refs`) and packs loose objects. Find every git invocation in find_dispatches.py (git grep) and report which could trigger auto gc.

# Scope boundary
1. **Check the hypothesis first.** In a scratch copy of the test's fixture, lower the thresholds (for example `git config gc.auto 1`, and enough loose objects) so that auto gc fires. Run find_dispatches.py and show that the snapshot changes: `info/refs` appears or objects are packed. Then show that with the fix it does not. If you cannot make auto gc fire from find_dispatches.py's own git calls, stop and report what you found. Don't guess a different cause.
2. **The fix.** Every git call in find_dispatches.py runs with `-c gc.auto=0 -c maintenance.auto=false` and `GIT_OPTIONAL_LOCKS=0` in its environment, through one helper so no call is missed. The docstring's read-only sentence names these. No other behaviour change.
3. **The test.** Add a test to tests/test_find_dispatches.py that sets `gc.auto=1` in the fixture repo, creates loose objects, runs the tool, and asserts that the snapshot is unchanged. Before the fix it must fail for the right reason: the snapshot differs, with `info/refs` or pack changes. Keep `test_read_only` as it is.
4. **Scan the other tools.** Check whether update_worktree.py's read-only phase (the dry run), session_agents.py, launch_session.py's pre-check and run_exercise.py call git in ways that could trigger auto gc. Report what you find, with file:line. Do not change them. The planner will decide.

**Record:**
1. the sweep of -44
2. this dispatch's copy and intent
3. the tests-only commit
4. the fix
5. the terminal

Then the PR, the merge, and item 10's update of C.

# Closed decisions
- The owner's AskUserQuestion answer in B: "find_dispatches housekeeping". Quote the option text from the tool_use.
- The owner, verbatim: "Run the fix real quick".
- The planner's choices: the three settings, the one helper, and scanning (not changing) the other tools.
- B-11/B-13: coders merge at the planner's discretion. Note: the Fable review's P1 questions that arrangement, and the owner has not yet ruled on it. Merge only under the Merge section below. If Claude Code's permission check denies the merge, quote it and stop.

# Merge
authorised: this PR only (kit-v0.2-fd-gc -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup. Run it from cwd C without `--repo`, as one Bash call. If it is denied, quote the denial and stop. Merge nothing else. Then do item 10's update of C.

# Prediction
- **Step 1:** with `gc.auto=1` and loose objects, a find_dispatches.py run changes the fixture's `.git` (info/refs or packs).
- **Tests-only commit:** the new test fails on the snapshot difference; everything else passes.
- **After the fix:** all tests pass, the hook tests are unchanged, render checks are 30/30 and 13/13, both checkers PASS, and release_check PASSes with 27 files and no denylist hit.
- **Revert check:** find_dispatches.py from base gives the same failure.

Put your mechanism prediction in the intent.

# Finish line and abort conditions
**Finish line**, pushed on kit-v0.2-fd-gc:
1. the sweep
2. the copy and intent
3. the tests-only commit
4. the fix
5. the terminal

Then CI green, the backup, the merge, and the merge commit reported. Then C updated. No tag.

**Abort and report on:**
- a Base-and-state mismatch other than the dispatch's name or line drift
- #33 not merging within the wait
- step 1 not reproducing
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit
- CI not green (no merge)
- the merge denied (quote it)
- the dry run or `--apply` exiting 1
- an owner message after this Agent call that tells you to do or not do something in this scope
- a planner message that widens the scope without quoting an owner ruling

# Checks
- Step 1's evidence.
- The failing-first run and the revert check.
- Counts before and after.
- Both checkers before every commit touching RECORD.md; read B in a separate call before each.
- Tests in their own Bash call.
- The backup evidence and the merge commit's parents.
- The outputs of C's update.
- The scan of the other tools.

# Out of scope
- Changing the other tools.
- Any Fable-review finding beyond this.
- The T3 backlog, D2 and D5.
- Other PRs, tags, deleting anything.

# Device items
None.