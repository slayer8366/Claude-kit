HEAD: 9e6ff6b2c2b06bbd8fd4d73f4a04b23e415046ad
Target subagent: coder
Type: build
Preserved: 2026-09-25T19:12:42Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for a Claude-kit v0.2 fix batch (batch 2) that comes before T8. It has three fixes. Follow `.claude/agents/coder.md` in ~/Zynergy/Claude-kit, including items 1, 8, 10 and 11.

# Base and state
Premises to verify:
- **main.** origin/main is cedeb526887397945349f3ff030123466af9aeea (PR #27 merge, T7). No tags. The record's last entry is 2026-09-25-72 (T7 terminal); no intent is open.
  - Create `kit-v0.2-batch2` from origin/main in ~/Zynergy/Claude-kit-fixes. It is currently on the merged kit-v0.2-t7 at 9675f8a, clean.
  - Push with `git -C ~/Zynergy/Claude-kit-fixes push -u origin kit-v0.2-batch2`.
- **C** = ~/Zynergy/Claude-kit, on main at 9e6ff6b, behind origin/main. Your hooks and Bash cwd are C.
  - Untracked: `.claude/worktrees/` (leave it alone), and `prompts/preserved/2026-09-25-33.md`, `-34.md`, `-35.md`. All three are tracked on origin/main and recorded: -69, -70, and -71's claim.
  - This dispatch is expected as `-36.md`; the hook's name governs.
  - No sweep is needed, since every store file on main is claimed. This dispatch's copy goes with its intent.
- **Planner log B:** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit/2d9316b0-e46a-5c35-b7e4-125fac5c9a3e.jsonl. Cite by timestamp and tool_use id.
- **update_worktree.py** (origin/main) refuses a protected branch or a detached HEAD. The docstring at :19-20 says "(the main checkout is updated with `git pull` by its owner)"; `survey` is at :118-124. Its dispatch, store 2026-09-25-20.md:46 and :72 (test u6: "the main checkout, on protected `main` → exit 1"), states this as a planner rule, not an owner ruling. Verify both.
- **The project-directory rule.** The README text in the "Resuming after a disconnect" and "Launching a session" sections, and `launch_session.py`'s log-path function, say `/` and `.` become `-`. `~/.claude/projects` shows `_` does too: `bridge-cse_013ve…` → `…bridge-cse-013ve…`. Find every place in the tracked code and README that states or computes the rule (git grep).
- **launch_session.py** runs the session with `start_new_session=True`. A SIGINT/SIGTERM/SIGHUP to the launcher does not stop the session's group (T7 terminal -72 and its hand-back).

# Scope boundary
**Fix 1: the main checkout is updated by the tool, after every merge.**
- `update_worktree.py`: a checkout whose current branch equals the target branch (the first of `protected_branches`) is updated in the same way. Untracked store copies that origin/<branch> tracks byte for byte are moved aside to a backup, and then the checkout is fast-forwarded only (`merge --ff-only`).
- Still refused:
  - a detached HEAD;
  - a current branch that is protected but is not the target;
  - anything that is not a fast-forward.
- The docstring states the new rule first. It also says why this is not a history_guard bypass: a fast-forward to origin's own tip changes nothing on the remote and merges nothing new into the branch.
- `tests/test_update_worktree.py`:
  - u6 changes: the main checkout on `main`, with origin ahead and a matching untracked store copy → exit 0 with `--apply`, the copy moved, HEAD equal to origin/main.
  - Add u6b: detached HEAD → exit 1.
  - Add u6c: on a second protected branch that is not the target → exit 1 (use a test kit.json listing two protected branches).
  - Add u6d: main checkout with a local commit not on origin → exit 1, not a fast-forward.
- `.claude/agents/coder.md` item 10 gains a final step: after the merge, run `python3 ~/Zynergy/Claude-kit-fixes/update_worktree.py ~/Zynergy/Claude-kit` as a dry run, then with `--apply`. Report both outputs. If either exits 1, report it and do nothing further. Name the path generically in the released text, "the main checkout", with this repo's paths only as the example.
- README: the update_worktree section, and the "Merging and undoing a merge" section, say the same.

**Fix 2: one rule for the project directory name.**
- Establish the real rule from the data. For every directory under `~/.claude/projects`, take the first log record that carries `cwd` and compare its sanitised form with the directory name. Report matches and mismatches for these candidate rules:
  - (i) `/ . _` → `-`
  - (ii) every character outside `[A-Za-z0-9]` → `-`
  - (iii) any other rule the data shows
- Implement the rule the data supports in `launch_session.py`. If `session_agents.py` computes it too, implement it there as well.
- Correct the README text in both places.
- If the data fits no single rule, stop and report.

**Fix 3: interrupting the launcher stops the session.**
- `launch_session.py` handles SIGINT, SIGTERM and SIGHUP while a hook check or a session is running. It does the same stop as the time limit (SIGTERM to the group, `--grace`, SIGKILL, confirm the group is gone), prints the report with outcome "interrupted", and exits 130.
- Signals that arrive before launch or after the session has ended give a plain exit 130, without killing anything.
- Document this in the docstring and README.

**Tests (tests-only commit first):**
- u6 changed; u6b, u6c, u6d added.
- In `tests/test_launch_session.py`:
  - (m) the launcher is sent SIGINT while a hanging fake runs → exit 130, outcome "interrupted", and none of the group's processes are left (the fake records its PIDs and its child's);
  - (n) SIGTERM to the launcher, same result;
  - (o) the log-path function against fixture cwds containing `_`, `.`, `/` and a space, expecting the rule that Fix 2 establishes.

  Write (o)'s expectations after Fix 2's data check, and put that check's output in the intent.

**Record:**
1. this dispatch's copy and the intent
2. the tests-only commit
3. the fix (update_worktree.py, launch_session.py, session_agents.py if needed, coder.md, README)
4. the terminal

Then the PR and the merge. Then, as the new item 10's last step and this batch's live check, update C with the merged update_worktree.py (dry run, then `--apply`).

# Closed decisions
- **The owner, in B, verbatim:** "Go ahead and first fix the cause of the repeated move-and-pull, together with the log-path and Ctrl-C fixes, as a small batch, then send the usual move-and-pull and then T8."
- **The planner's choice of fix for "the cause".** Let update_worktree.py fast-forward the main checkout, and make updating it a step in coder.md item 10.
  - It relaxes the planner's own rule from 2026-09-25-20 (store :46, :72), which was not an owner ruling.
  - Rejected alternative: moving the dispatch hook's save location, which would touch dispatch_guard, check_prompts, find_dispatches and T1's shared counter.
- **The batch's own post-merge update of C is "the usual move-and-pull" the owner asked for next.**
- **B-11/B-13:** coders merge at the planner's discretion. The planner authorises this PR below.

# Merge
authorised: this PR only (kit-v0.2-batch2 -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup. Run it from cwd C without `--repo`, as one Bash call. Merge nothing else.

# Prediction
- **Tests-only commit:**
  - u6 fails: the old code exits 1 on protected `main`. u6b and u6c pass (still refused). u6d passes too, if the old code refuses on the protected-branch check first. Report which.
  - (m) and (n) fail: the old launcher exits from the signal without stopping the group, so leftover PIDs are found. Name the actual failure.
  - (o) fails for `_` under the old rule, if the data shows `_` → `-`.
  - The 137 hook tests stay OK.
- **After the fix:** all tests/ OK, 137 hook tests OK, render checks 30/30 and 13/13, both checkers PASS, release_check PASS with 26 files and no denylist hit (grep the new text first).
- **Revert check:** revert the whole fix (the code files, and release.json if it changed). The same tests fail again.
- **Post-merge live check:**
  - Dry run on C: exit 0, moving `-33`, `-34`, `-35` and this dispatch's copy (all tracked on main by then), fast-forward 9e6ff6b..<merge commit>.
  - `--apply`: exit 0, HEAD equals origin/main, only `.claude/worktrees/` untracked.
  - Then `python3 ~/Zynergy/Claude-kit/.claude/hooks/session_check.py`, with a SessionStart payload for C, prints nothing.

Put your mechanism prediction in the intent.

# Finish line and abort conditions
**Finish line**, pushed on kit-v0.2-batch2:
1. the copy and intent
2. the tests-only commit
3. the fix
4. the terminal

Then CI green on the final commit, the backup, the merge with `--merge` and the merge commit reported. Then C updated by the merged update_worktree.py, with both outputs and the session_check result reported in the hand-back. No tag.

**Abort and report on:**
- any Base-and-state mismatch other than the dispatch's name
- Fix 2's data fitting no single rule
- a needed change outside scope (dispatch_guard, check_prompts, find_dispatches, history_guard, role_guard)
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit
- CI not green (no merge)
- history_guard denying the merge after the backup (quote it)
- the post-merge dry run or `--apply` exiting 1 (report it verbatim, no workaround)
- an owner message after this Agent call that tells you to do or not do something in this scope, or changes a decision here
- a planner message that widens the scope or changes a closed decision without quoting an owner ruling

# Checks
- Fix 2's data table.
- The failing-first run and the revert check.
- Counts before and after.
- Both checkers before every commit touching RECORD.md; read B in a separate call before each.
- Tests in their own Bash call.
- `pgrep -f` for the fakes after the test runs.
- The backup evidence and the merge commit's parents.
- The post-merge update outputs.

# Out of scope
- T8.
- Moving the dispatch hook's save location.
- Other hooks.
- D2 and D5.
- The worktrees under `.claude/worktrees/`.
- Other PRs, tags, deleting anything.

# Device items
None.