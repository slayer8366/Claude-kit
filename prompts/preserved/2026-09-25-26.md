HEAD: 1599a53975b8f980035c594d52f7d125de768370
Target subagent: coder
Type: build
Preserved: 2026-09-25T14:59:36Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit v0.2 tasks T14, T15 and T16 in one dispatch (`docs/specs/2026-09-24-kit-v0.2-tasks.md` lines 21-23). Follow `.claude/agents/coder.md` at your base, including items 1, 10 and 11.

# Base and state
Premises to verify:
- **main.** origin/main is 1599a53975b8f980035c594d52f7d125de768370 (PR #24 merge, T10). Nothing is tagged. The record's last entry is 2026-09-25-58; no intent is open. Create `kit-v0.2-t14-16` from origin/main in ~/Zynergy/Claude-kit-fixes.
- **Planner worktree W** (~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB, at 1599a53). Your hooks run from W, and your Bash cwd resets to W. Its untracked store files:
  - D2 `-02` and D5 `-05`: leave them
  - `2026-09-25-25.md`: the planner's end-to-end re-send of pulse 2026-09-24-03 (header `Repeat-of: preserved/2026-09-24-03.md`, Preserved 14:56:25Z). It answered with W's HEAD, 1599a53.
  - this dispatch, expected as `2026-09-25-26.md`. If the hook gave another name, that name governs.
- **Sweep.** Copy `2026-09-25-25.md` into the store under its hook name, with dispatch-note 2026-09-25-59 (Type pulse, Outcome answered). Per coder.md item 11, its Observed cites the original `2026-09-24-03.md` and the T10 end-to-end result. check_prompts must pass its repeat check.
- **Planner log.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl. Cite by timestamp first. The re-send's Agent call and its hand-back are just before the owner's "I'll take your recommendation".
- **Sources:**
  - docs/audits/2026-09-23-kit-v0.1-extraction-completion-report.md:260-261: "The kit has no record of the guards' known bypasses…"
  - workshop/extraction-pulse-2026-09-23.md:179: the merge route bypasses the guard
  - role_guard.py's docstring: "patterns over the command text …"
  - history_guard.py's docstring
  - RECORD.md 2026-09-23-10 (Observed: blocked Agent calls logged with `toolDenialKind` permission-rule)

# Scope boundary
**T14:**
- A new released file, `.claude/hooks/BYPASSES.md`: an intro, then one table with columns ID (B-NN), Guard, Bypass, Status, Test.
  - Status is `open` for every row. The owner rules on accepted versus fixed later.
- A new released test file, `.claude/hooks/tests/test_bypasses.py`:
  - one test per row, asserting that the bypass gets through (the hook returns None, or allow, for that input)
  - one consistency test: every ID cited in a hook docstring exists in the table, and every row's Test names a test that exists
- Each guard's module docstring gains a line "Known bypasses: .claude/hooks/BYPASSES.md <IDs>". The guards are role_guard, dispatch_guard, device_guard, history_guard and session_check.
- `release.json`: add both new files.

**T15:**
- A README "Backups" section: `backup_dir`; the folder layout (`<UTC date>-NN`, MANIFEST.sha256, INDEX.md); merge backups (coder.md item 10); update_worktree's moves.
- The ruling: "created after" for a backup means a file's birth time (statx btime), never mtime. Where a filesystem gives no birth time, the backup's record says so rather than falling back.

**T16:**
- A README section "Blocked calls in the session log": a dispatch that dispatch_guard or role_guard blocks is logged with `toolDenialKind`, for example "permission-rule", on the tool_result, not a `deny` decision. Evidence: 2026-09-23-10.
- One sentence in dispatch_guard's docstring saying the same.

**Task list:**
- Amend T16's done-when to "The toolDenialKind behaviour is documented; evidence checks match on it in T8 (owner ruling at T16)".
- Add "matching blocked calls on toolDenialKind (moved from T16)" to T8's done-when.

**Record:**
- the sweep: the copy and note -59
- this dispatch's copy and intent 2026-09-25-60
- a docs commit (T15, T16, task list, docstring lines)
- the tests-only commit (T14's tests)
- the fix (BYPASSES.md, the docstring citations, release.json)
- terminal 2026-09-25-61

No guard behaviour changes: T14 records bypasses, it doesn't close them.

# Closed decisions
The owner, in the planner log. Cite by timestamp:
- **"I'll take your recommendation"**, choosing T14-T16 as one batch, then T6.
- **The AskUserQuestion answers:**
  - T14 "Table + proving tests (Recommended)"
  - T16 "Document now, match in T8 (Recommended)"

  Quote both options' texts.

The planner's rules for T14's rows:
- A row goes in only if its test shows the bypass getting through today. Investigate these candidates and report each one, in or out, with the reason:
  - (1) a push or merge run through an interpreter or wrapper (`python3 -c` calling git, `sh -c '…'`, `env git push …`) past history_guard or role_guard's patterns
  - (2) `gh api` with a write method that merges a PR, run by the coder role (role_guard blocks gh api writes for the planner and pulse only)
  - (3) a refspec or branch given through a shell variable, such as `B=main; git push origin $B`
  - (4) a quoted heredoc marker hiding following lines from the push check (T12 backlog)
  - (5) device_guard: adb reached another way (a full path to adb, or an alias)
  - (6) dispatch_guard: anything you find
- Candidates that are over-blocking rather than bypasses are out; list them in the hand-back. Examples: `<< EOF` with a space, and `$W` in `git -C $W`.
- **Hook inputs only:** tests call the hooks with payloads. Never run a real push, merge, `gh api` or adb command.

# Merge
authorised: this PR only (kit-v0.2-t14-16 -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup. The merge command may run with cwd W and without `--repo`. Merge nothing else.

# Prediction
- **Tests-only commit:** test_bypasses.py.
  - The per-row tests pass: they assert today's behaviour.
  - The consistency test fails, because BYPASSES.md and the docstring citations don't exist yet.
  - Hook tests: 129 + (rows + 1) run, 1 failure.
  - test_install's vendored run is unaffected, since the file isn't vendored yet.
- **After the fix:**
  - all hook tests OK
  - 47 tests/ OK
  - render checks 30/30 and 13/13, both checkers PASS, including the repeat check on `-25`
  - release_check PASS with 24 files and no denylist hit. Grep the new text for the denylist patterns first.
- **Revert check:** removing BYPASSES.md (the only change) makes only the consistency test fail again.

Your mechanism prediction goes in the intent.

# Finish line and abort conditions
Finish line, pushed on kit-v0.2-t14-16:
1. the sweep
2. the copy and the intent
3. the docs commit
4. the tests-only commit
5. the fix
6. the terminal

Then the PR with CI green, the merge under the Merge section, and the merge commit reported. No tag.

Abort and report on any of these:
- any Base-and-state mismatch other than the dispatch's name
- a needed behaviour change to any guard
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit
- CI not green: don't merge
- history_guard denying the merge after the backup: quote it

Owner messages after this dispatch's Agent call: an abort only if one tells you to do or not do something in this scope, or changes a decision here. Record every other message in the terminal. A planner message follows coder.md item 6.

# Checks
- Failing-first run and revert check.
- Counts before and after.
- Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
- Tests in their own Bash call.
- The merge's backup evidence.
- In the hand-back, the candidate list with each one's decision and test name.

# Out of scope
- Closing any bypass.
- D2 and D5.
- T6-T8.
- Updating W.
- Other PRs, tags, deleting anything.

# Device items
None.