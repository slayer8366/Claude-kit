HEAD: 93904076a95498b3a877438f6d354fc1b1b88419
Target subagent: coder
Type: build
Preserved: 2026-09-23T14:52:43Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Claude-kit v0.1 fixes: item 4b, and prepare ~/Zynergy/Claude-kit for the live exercise

## Role
You are the coder. You execute this dispatch, you make no design decisions, and a gap is a stop-and-ask. Record it as `coder.md` and the checkers require (in the fix worktree's RECORD.md), and run `check_record.py` and `check_prompts.py` before every commit that touches `RECORD.md`.

## Base and state
- `~/Zynergy/Claude-kit-fixes`: branch `kit-v0.1-fixes`, HEAD = `origin/kit-v0.1-fixes` = `3ce90e3`, clean. Open intent: 2026-09-23-04.
- `~/Zynergy/Claude-kit`: branch `kit-v0.1` at `a544ca9` = `origin/kit-v0.1`; the only untracked path is `.claude/worktrees/` (harness worktrees, two of them, each at `9390407`). Its `prompts/preserved/` holds only `2026-09-23-01.md`, tracked.
- `origin/main` = `9390407`. No tags.
- The owner confirms no Claude Code session is open in `~/Zynergy/Claude-kit` itself (harness worktrees under `.claude/worktrees/` are separate checkouts).
Verify all of this first (except the owner's confirmation); if anything differs, stop.

## Scope boundary
Item 4b (below) in the fix worktree; back up; then switch `~/Zynergy/Claude-kit` to a detached checkout of the fix branch's tip, so the owner's live exercise runs there. Nothing else. This overrides 2026-09-23-02's "never edit files in ~/Zynergy/Claude-kit" for exactly one change: the one `git checkout --detach` in step 4. No other edits in that checkout. Never touch `.claude/worktrees/*` (reading is fine), Forager, or any tag.

## Closed decisions
Owner rulings, 2026-09-23: "The whole test goes in Claude-kit. That's the whole point of that repo." On the previous coder's flag 1 (unversioned citations): "Go ahead and fix it then." On backing up the first coder's /tmp evidence: "keep it". Therefore:
- E1: the live exercise (step 4 of 2026-09-23-02, the four cases) runs in a session the owner starts in `~/Zynergy/Claude-kit`, not in `~/Zynergy/Claude-kit-fixes`.
- E2: that checkout is set to a detached HEAD at the fix branch's tip after this dispatch's commits (git will not check out `kit-v0.1-fixes` while the fix worktree holds it). Before the checkout, verify with `git diff --stat 3ce90e3 <tip>` that the tip differs from `3ce90e3` only in `RECORD.md`, `prompts/preserved/`, `workshop/denylist.txt`, `check_record.py`, `check_prompts.py`, `README.md` and `tests/test_release_check.py`, and in nothing under `.claude/` (the hooks and kit.json stay exactly those of `3ce90e3`). Because this dispatch is committed to the fix branch before the checkout, the exercise's saved dispatches are numbered after it, and no number names two different files.
- E3, record: a new intent 2026-09-23-06 claims this dispatch's saved copy. It stays open; its terminal is written after the exercise, once `~/Zynergy/Claude-kit` is back on `kit-v0.1` (not in this dispatch). This dispatch's saved copy is the v0.1 hook's file in the harness worktree `~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01JDbTJ4Gj9C6xygXM5tPdHz/prompts/preserved/` (identify it by content and header time), copied byte for byte into the fix worktree as the next free number (expected `2026-09-23-04.md`), sha256 on both sides, original left untracked and unedited.
- E4, backup before any change, to a new `~/forager-backups/<UTC date>-<next free NN>/`: a copy of `~/Zynergy/Claude-kit` excluding `.claude/worktrees/`, plus the untracked files under `~/Zynergy/Claude-kit/.claude/worktrees/*/prompts/preserved/`, plus `/tmp/kitfix_evidence/` and `/tmp/kitfix_saved/`. A `MANIFEST.sha256` of every file, verified on both sides; one line appended to `~/forager-backups/INDEX.md` in its table format (append-only; a mistake gets a correction line). Delete nothing anywhere.
- E5, item 4b: add a second line to `workshop/denylist.txt`: `(?i)\bClaude-kit addition\b` with a comment in the style of item 4's. Reword `check_record.py:953` and `check_prompts.py:244` from "(Claude-kit addition)" to "(Claude-kit v0.1 addition)", and update the README's quotes of them (around lines 192 and 293) to match. Add a test in `tests/test_release_check.py` in the style of item 4's. One commit, after the record commit.

## Prediction
- Item 4b: with the pattern added and the old wording in place, `release_check.py` fails on exactly `check_record.py:953` and `check_prompts.py:244` (the README is not in the release set). After rewording it passes with 18 denylist patterns.
- The record commit passes both checkers.
- After the checkout, `git -C ~/Zynergy/Claude-kit status` shows HEAD detached at the fix tip, clean apart from `.claude/worktrees/`; `.claude/kit.json` there carries `approval_exempt_types` and `agent_roles`; the harness worktrees are unchanged.

## Finish line and abort conditions
Finish: backup done and verified; intent 06 and item 4b committed and pushed; `~/Zynergy/Claude-kit` detached at the fix tip; then STOP and report "Claude-kit ready for live exercise" with the tip hash. Abort on: any Base-and-state mismatch; any diff from `3ce90e3` outside the E2 list or anything under `.claude/`; a manifest mismatch; item 4b failing on lines other than the two predicted; any need to edit other than as scoped.

Steps:
1. Backup per E4.
2. Record per E3; run all checks; commit; push.
3. Item 4b per E5: failing-first with the release check output pasted; sabotage with a saved copy in `/tmp/kitfix_saved/c06_item4b/`, restore from that copy, confirm with `cmp`; run all checks; commit; push.
4. The E2 diff check, then `git -C ~/Zynergy/Claude-kit checkout --detach <fix tip>`; confirm per the prediction.
5. STOP and report, with old and new test counts.

## Checks
On every commit, in the fix worktree: `python3 -m unittest discover -s .claude/hooks/tests -v`, `python3 check_record.py --render-check`, `python3 check_prompts.py --render-check`, `python3 -m unittest discover -s tests -v`, `python3 release_check.py`. After step 4, run the same five in `~/Zynergy/Claude-kit` (they are reads; confirm afterwards that `git status` there is unchanged).

## Out of scope
The live exercise itself, restoring `kit-v0.1`, the terminals for intents 04 and 06, the completion report, the PR, and any v0.2 item.

## Device items
None.