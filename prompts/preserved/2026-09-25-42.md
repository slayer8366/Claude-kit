HEAD: 6a848aca23a50fdde28d3580b3d7447b1c5002de
Target subagent: coder
Type: build
Preserved: 2026-09-25T21:49:49Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit v0.2 fix batch 4, four small follow-ups. Follow `.claude/agents/coder.md` in ~/Zynergy/Claude-kit, including items 1, 6, 8, 10 (with its last step) and 11.

# Base and state
Premises to verify:
- **origin/main** is 6a848aca23a50fdde28d3580b3d7447b1c5002de (PR #31 merge, bypass rulings). No tags. The record's last entry is 2026-09-25-82; no intent is open.
- Create `kit-v0.2-batch4` from origin/main in ~/Zynergy/Claude-kit-fixes. Push with `git -C /home/zynergy-labs/Zynergy/Claude-kit-fixes push -u origin kit-v0.2-batch4`, using an absolute path.
- **C** is ~/Zynergy/Claude-kit, on main at 6a848ac. Only `.claude/worktrees/` is untracked, plus this dispatch's copy (expected `-42.md`; the hook's name governs). No sweep is needed.
- **Planner log B** is /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit/2d9316b0-e46a-5c35-b7e4-125fac5c9a3e.jsonl.
- **Item 1.** session_agents.py's fallback `denied: <first line>` has no dedicated test since batch 3 (terminal -80's flag).
- **Item 2.** history_guard does not expand `~` in `git -C ~/…`. `current_branch` gets the literal path, git fails, and the push is refused with "could not read the current branch (fatal: cannot change to '~/…')" (terminal -80). Check whether the `git merge` check's `-C` path (MERGE / `dash_c`) has the same gap.
- **Item 3.** This one is unverified; prove it first. The planner believes the heredoc detection treats a `<<<` here-string as a heredoc marker. The `HEREDOC` regex `<<(-?)(['"]?)(\w+)\2` can match the `<<EOF` inside `<<<EOF`. If so, the push check drops the lines after it, up to a line `EOF`. Example: `cat <<<EOF`, newline, `git push origin main`, newline, `EOF`, run on a feature branch. That would hide the push. Terminal -82 flagged that `<<<` is still treated as a marker.
- **Item 4.** A pull-request merge through the REST API with `curl` is not caught: `curl -X PUT -H "Authorization: …" https://api.github.com/repos/<o>/<r>/pulls/<N>/merge`. The B-03 fix covers `gh api` only. No BYPASSES.md row covers it. B-01 to B-08 are ruled: B-03 and B-05 `fixed`, the rest `accepted`.

# Scope boundary
1. **Fallback test.** Add a test to tests/test_session_agents.py: a `denied` result whose text matches none of the four rules shows `denied: <first line>`, with the second line not printed. It asserts today's behaviour, so it passes on arrival. Prove it with a revert-style check: temporarily make the fallback print something else. The test must fail, and then the code is restored. Nothing is committed from the sabotage.
2. **`~` expansion.** history_guard expands a leading `~` or `~user` in a `git -C` directory with `os.path.expanduser`, as the shell would. Do this before reading the branch, for the push check and, if it has the same gap, the `git merge` check. Nothing else is expanded. `$VAR` stays unexpanded; that is B-04, accepted.
   - Tests: `git -C ~/<repo> push origin <feature>` is allowed, and `git -C ~/<repo> push origin main` is denied as a protected-branch push, not a "could not read" error. Use a temporary HOME holding a fixture repo, passing the environment to the hook the way the harness allows. If the harness can't set HOME, report how you solved it.
   - The docstring states the rule.
3. **`<<<`, fix only if real.**
   - First write the test: the example above on a feature branch is denied as a push to main.
   - If it fails today, fix it: a `<<` preceded by `<`, or followed by `<`, is not a heredoc marker. Then show it passes. All T12 and B-05 heredoc tests must keep passing.
   - If it already passes today, there is no bypass. Report that, keep the test as a regression test, and make no code change for item 3.
   - Either way, BYPASSES.md gets no row for it. It belongs with B-05, `fixed`. If you fix it, add one sentence to B-05's row: "also `<<<` here-strings (batch 4)".
4. **B-09, record only.**
   - New row B-09: Guard history_guard, role_guard. Bypass: a PR merge through the REST API with `curl` (or another HTTP client), which neither the `gh pr merge` rule nor B-03's `gh api` check sees. Status `open`.
   - Add a proving test, `test_b09_pr_merge_through_curl`: history_guard and role_guard give no decision for the coder role, using payloads only.
   - Cite B-09 in history_guard's and role_guard's "Known bypasses:" lines.
   - Never run curl, gh or any network call for real.

**Record:**
1. this dispatch's copy and intent
2. the tests-only commit: the tests for items 2, 3 and 4, and the fallback test
3. the fix: history_guard.py, BYPASSES.md, docstring lines
4. the terminal

Then the PR, the merge, and item 10's update of C.

# Closed decisions
- **The owner, in B:** "Do both small follow-ups as a task after this merges" (21:31:18.979Z). "Both" means the fallback test and the `~` fix, as the planner listed them.
- **The owner's AskUserQuestion answer in B:** "Fix <<<, record curl (Recommended)". Quote the option text from the tool_use.
- **The planner's choices:**
  - `expanduser` only, with no `$VAR` expansion
  - B-09's wording
  - item 3's "prove it first, drop the fix if not real"
- **B-11/B-13:** coders merge at the planner's discretion.

# Merge
authorised: this PR only (kit-v0.2-batch4 -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup. Run it from cwd C without `--repo`, as one Bash call. If denied, quote it and stop. Merge nothing else. Then do item 10's update of C.

# Prediction
**Tests-only commit:**
- The `~` tests fail. Today both the feature push and the main push are refused with "could not read the current branch".
- The `<<<` test fails, if the planner's reading is right.
- The fallback test and test_b09 pass.
- The consistency test passes, since no docstring cites B-09 yet.
- tests/: the vendored-hook-tests case fails if the hook tests fail.
- Report the counts, starting from 140 hook tests and 93 tests/.

**After the fix:** all hook tests and tests/ OK; render checks 30/30 and 13/13; both checkers PASS; release_check PASS with 27 files and no denylist hit.

**Revert checks:**
- history_guard.py from 6a848ac gives the same `~` and `<<<` failures.
- The fallback sabotage fails only the fallback test.
- Removing the B-09 row while keeping the citations fails the consistency test.

Put your mechanism prediction in the intent.

# Finish line and abort conditions
**Finish line**, pushed on kit-v0.2-batch4:
1. the copy and intent
2. the tests-only commit
3. the fix
4. the terminal

Then CI green on the final commit, the backup, the merge with `--merge` and the merge commit reported. Then C updated by item 10's last step. No tag.

**Abort and report on:**
- any Base-and-state mismatch other than the dispatch's name
- a T12, B-05 or merge-rule test failing after the fix
- a needed change to a guard other than history_guard's behaviour (docstring lines are fine)
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit
- CI not green (no merge)
- the merge denied by anything (quote it)
- the dry run or `--apply` exiting 1
- an owner message after this Agent call that tells you to do or not do something in this scope
- a planner message that widens the scope without quoting an owner ruling

# Checks
- The failing-first run, the three revert checks, and the fallback sabotage.
- Counts before and after.
- Both checkers before every commit touching RECORD.md; read B in a separate call before each.
- Tests in their own Bash call.
- The backup evidence and the merge commit's parents.
- The outputs of C's update.

# Out of scope
- Fixing B-09 or any accepted row.
- `$VAR` expansion.
- D2 and D5.
- Other PRs, tags, deleting anything.

# Device items
None.