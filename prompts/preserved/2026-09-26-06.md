HEAD: 5ba1581f252a2e10a8825d6c3520a0054291e748
Target subagent: coder
Type: build
Preserved: 2026-09-26T03:05:59Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for the first promotion of Claude-kit's pre-main into main: open the pull request, write the backup, merge it, update the checkouts. This is a merge dispatch under coder.md item 10(b) at 5ba1581: you write no record entry; the next build's sweep records this merge as a `merge` entry with Base main and claims this dispatch's store copy with a `merged` dispatch-note. Follow coder.md items 6, 10 and 12 otherwise. Deferral does not apply.

# Base and state
Premises to verify before acting.
- **origin/pre-main** is 5ba1581f252a2e10a8825d6c3520a0054291e748 (the PR #39 merge, parents 9d07747 and 90d26b5). **origin/main** is 0319ccd7a8818dbabec7faa2f67c31cde349e11f. `git merge-base origin/main origin/pre-main` is 0319ccd, so main is an ancestor of pre-main. No tags. CI on 5ba1581: push run 36213535328, success in both jobs.
- **Both branches** carry GitHub protection: pull request required, enforced for admins, checks `python 3.8` and `python 3.14` required, no required reviews.
- **The record** at 5ba1581 ends with terminal 2026-09-26-32 (closing -31, completed); no intent is open. The range 0319ccd..5ba1581 holds five first-parent merges, PRs #35 to #39, recorded as merge entries -21, -24, -27, -30 and, still owed to the next sweep, #39 itself.
- **The evidence the owner read before approving this dispatch** (planner log F, the message before this dispatch's Agent call): five builds R1, R2, R3, R4, R6, each with failing-first tests, a revert check, CI green on its final commit, a backup, and a terminal `completed` under the new rule; three prediction misses recorded in terminals -23, -26 and -32; no deferral in any build. The diff 0319ccd..5ba1581 touches 26 files: the six hooks and their tests, check_record.py, coder.md, kit.json, settings.json, README.md, docs/specs/2026-09-26-review-fixes-plan.md, six store copies and RECORD.md.
- **C** (~/Zynergy/Claude-kit) is on pre-main at 5ba1581. **W**, your Bash cwd, is /home/zynergy-labs/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01RGmfuBWv4x1Nvnpa3fBQDZ at 5ba1581, with two untracked store copies: `prompts/preserved/2026-09-26-05.md` (a pulse) and this dispatch's copy (expected `2026-09-26-06.md`; the hook's name governs). Leave both where they are; the next sweep claims them.
- **Planner log F** is /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-01RGmfuBWv4x1Nvnpa3fBQDZ/d5592fbe-c371-5027-aaf9-18ab19a5613c.jsonl. Read it in a separate call before the merge; do not print its long lines.
- **The merge rule you run under** is W's history_guard at 5ba1581 (R4 and R6 live): the backup's `branch` must be a protected branch, the bundle must list `<sha> refs/remotes/origin/main`, INDEX.md matches whole tokens, and the remote's refs/heads/main must equal the backup's sha at merge time; every hook subprocess has a 20-second timeout.

# Scope boundary
1. `git fetch origin` in ~/Zynergy/Claude-kit-fixes (or W); confirm the two tips above.
2. Open the pull request from pre-main into main with `gh pr create --base main --head pre-main`, a title "Promote pre-main to main: review fixes R1 to R4 and R6" and a body written to /tmp and passed with `--body-file`, listing the five PRs, their merge commits and record IDs, and citing this dispatch's store copy. Report the PR number.
3. Wait for the pull request's checks to pass (`gh pr checks <N> --watch` is a read). If any check fails, stop.
4. Write the backup per coder.md item 10: a new folder under ~/forager-backups holding `main.bundle` (a bundle of `origin/main`, whose list-heads shows `0319ccd… refs/remotes/origin/main`), `merge.json` `{"pr": N, "branch": "main", "sha": "0319ccd7a8818dbabec7faa2f67c31cde349e11f", "bundle": "main.bundle"}`, `MANIFEST.sha256` over both, and one INDEX.md row naming the folder, `#N` and the sha in the header's five columns.
5. Merge with one bare Bash call `gh pr merge N --merge` from W, no `cd`, no redirection, no `--repo`. If denied, quote the denial and stop; do not retry a substantive denial. A denial on form for your own invocation shape may be corrected once, and reported.
6. Confirm: `gh pr view N --json state,mergeCommit,mergedAt`; `git fetch origin`; `git log -1 --format=%H%x20%P origin/main` shows the merge commit with parents 0319ccd and 5ba1581; origin/pre-main unchanged at 5ba1581.
7. Update C and W with `python3 ~/Zynergy/Claude-kit-fixes/update_worktree.py <path>` (dry run, then `--apply`). Both target origin/pre-main, which has not moved, so both report no fast-forward; report all four outputs. W's two untracked copies are not on pre-main, so they are left alone.
8. Hand back with the values the next sweep needs: PR number, Head pre-main, Base main, Merge-commit, Pre-merge 0319ccd…, Backup folder, Merged-by `coder preserved/<this copy>`, Carries (record IDs -01 to -32 of 2026-09-26 and the merge entries), merged-at.

No commit, no record entry, no tag, nothing deleted.

# Closed decisions
From planner log F, the owner, verbatim; cite line and timestamp:
- "Answer is B. Maybe set up a pre-main in the specific branch, to act as a dummy main so that all work is reversible. Only after passing the evidence gate does it qualify for main. …" (2026-09-25, line 157).
- "Finish R6, I'll tag, then proceed with R5 and R7" (2026-09-26, line 463).
- The approval of this dispatch at the dispatch hook, given after the planner's evidence message, is the evidence gate of option B and of coder.md item 10(b).
Planner's choices: the PR title; a merge commit rather than a squash; the backup folder written after the checks pass.
Standing: none cited.

# Merge
authorised: this pull request only (pre-main -> main), with `--merge`, once its checks are green, after the backup of origin/main in item 4. Merge nothing else.

# Prediction
Planner's outcome prediction:
- `gh pr create` succeeds; the pull_request run on 5ba1581 passes both jobs within a few minutes.
- The backup passes W's rule at 5ba1581: list-heads shows the pair, ls-remote of refs/heads/main returns 0319ccd, and `gh pr merge N --merge` is not denied.
- origin/main becomes a merge commit with parents 0319ccd and 5ba1581; origin/pre-main stays 5ba1581.
- Both dry runs report target origin/pre-main and no fast-forward; both `--apply` runs exit 0 changing nothing; W's two copies are reported left alone.
State your own expectation before step 5.

# Finish line and abort conditions
Finish line: the pull request merged into main as described, the backup on disk and indexed, the four update outputs reported, and the hand-back values in item 8. No tag.

Abort and report on any of:
- either tip not as stated, or main not an ancestor of pre-main
- any check failing on the pull request
- the merge denied for a reason other than your own invocation's form (quote it)
- origin/main moving between the backup and the merge
- a dry run targeting a branch other than pre-main, or any dry run or `--apply` exiting 1
- an owner message in F after this dispatch that tells you to do or not do something in this scope
- a planner message that widens the scope without quoting an owner ruling

# Checks
- The two tips, the merge-base, and the PR's checks.
- The backup's `git bundle verify`, list-heads, `sha256sum -c`, and INDEX row.
- The merge commit's parents from `git log`, and `gh pr view`.
- The four update outputs.
- The hand-back ends with "Decisions I made", "Flags outside scope" and "Deferred".

# Out of scope
- Any file change, record entry or commit.
- Tags (the owner's).
- R5, R7, the Forager install.
- The two untracked copies in W.
- Deleting anything.

# Device items
None.