HEAD: 8b1c88a69086b52698fa63e6f6a05937acf5685f
Target subagent: coder
Type: build
Preserved: 2026-09-25T08:16:08Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for the first merge under T17's rule: merge PR #17 into main, after writing the backup that history_guard checks. Follow `.claude/agents/coder.md` at your base, in particular item 10.

# Base and state
Premises to verify:
- **main.** origin/main is 8b1c88a69086b52698fa63e6f6a05937acf5685f (PR #16 merge, T17). Nothing is tagged.
- **PR #17** (kit-v0.2-wt-move -> main) is open and mergeable, with head 8c2e6b3 and CI green on it. It carries store copy `2026-09-25-17.md`, intent 2026-09-25-28 and terminal 2026-09-25-29. Check it with `gh pr view 17 --json state,headRefOid,mergeable,statusCheckRollup,baseRefName`. The base must be `main`.
- **Hooks.** Your session's hooks are the planner worktree W's, now at 8b1c88a: T17's history_guard, and kit.json with `backup_dir` "~/forager-backups" and a required Merge section. Confirm that W's `.claude/kit.json` has `backup_dir`.
- **W** is ~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB. This dispatch is expected in its untracked store as `2026-09-25-18.md`. If the hook gave another name, that name governs. D2, D5 and `-17` are also untracked there; leave them.
- **Backups.** ~/forager-backups/INDEX.md; the last folder is `2026-09-25-02`.
- **Planner log.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl.
- **history_guard** blocks any Bash command whose text contains the literal merge command, unless it passes T17's checks. Keep that literal out of commit messages, the PR body and heredocs. Write record text to files with Write or Edit.

# Scope boundary
- **The merge:** merge PR #17 only, from ~/Zynergy/Claude-kit-fixes, following coder.md item 10:
  - `git fetch origin`
  - a new backup folder `~/forager-backups/<next name>/` holding:
    - `git bundle create` of `origin/main`
    - `merge.json` with `pr` 17, `branch` "main", `sha` = `git rev-parse origin/main`, and `bundle`
    - `MANIFEST.sha256` over both
  - one INDEX.md line containing the folder name, `#17` and that sha
  - then the merge command for PR 17 with `--merge`, as one command with nothing else in it
- **Record,** on a new branch `kit-v0.2-merge-17` created from `origin/kit-v0.2-wt-move` (8c2e6b3), so that entry IDs continue from -29:
  - this dispatch's store copy
  - intent 2026-09-25-30, committed and pushed before the merge
  - terminal 2026-09-25-31 after it
  - a PR `kit-v0.2-merge-17` -> main, left open for the owner

  **Do not merge that record PR.** This dispatch authorises PR #17 only.
- Nothing else: no code, docs or config change, no other PR merged, no tag, nothing deleted.

# Closed decisions
- **The owner, in the planner log:** "Instruct a coder to merge 17 into main". An interrupted first attempt, "Instruct a codet to me Merge 17 into main", precedes it. Cite both by line and timestamp.
- **T17's rules,** as merged:
  - coder.md item 10
  - history_guard's conditions (a)-(e)
  - B-11's ruling text (owner, line 712)

# Merge
authorised: PR #17 only (kit-v0.2-wt-move -> main), with a merge commit (`--merge`), after the item-10 backup. Not the record PR this dispatch opens.

# Prediction
- **Before merging:**
  - History_guard allows the merge command: no deny; the command runs.
  - `gh pr view 17` then shows state MERGED, and its merge commit's first parent is 8b1c88a.
- **After `git fetch`:**
  - origin/main is that merge commit.
  - `git diff 8b1c88a origin/main --stat` shows only #17's two files: `prompts/preserved/2026-09-25-17.md` and RECORD.md.
- **The backup:**
  - `sha256sum -c MANIFEST.sha256` passes.
  - `git bundle list-heads` shows 8b1c88a.
- **Counts** in the fixes checkout at the record branch, unchanged: 117 hook tests, 39 tests/, render checks 30/30 and 10/10, both checkers PASS, release_check PASS with 19 files.

Your mechanism prediction goes in the intent. It includes which history_guard condition would deny if the backup were missing.

# Finish line and abort conditions
Finish line:
- PR #17 merged under the checked backup
- the record (copy, intent, terminal) pushed on kit-v0.2-merge-17
- its PR open with CI green on its final commit, and not merged
- no tag

Abort and report on any of these, before merging:
- any Base-and-state mismatch other than the dispatch's name
- PR #17 not mergeable, not green, or with a base other than main
- history_guard denying the merge after the backup is written. Quote the deny verbatim, don't work around it, and don't retry with other flags.
- a manifest or bundle check failing
- two failed fixes on one symptom

Owner messages after this dispatch's Agent call: an abort only if one tells you to do or not do something in this scope, or changes a decision here. Record every other message in the terminal. A planner message follows coder.md item 6.

# Checks
- Each step's command and output in the hand-back, including history_guard's decision on the merge command.
- Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
- Tests in their own Bash call.

# Out of scope
- Merging the record PR, or any other PR.
- Fast-forwarding W, which `2026-09-25-17.md` would now block.
- D2, D5, T5, the backlog.
- Tags, deleting anything.

# Device items
None.