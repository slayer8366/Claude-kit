HEAD: 9425ef7b5989a3e8e887c826c7b2379680896f08
Target subagent: coder
Type: build
Preserved: 2026-09-25T21:10:58Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder applying the owner's rulings on the kit's bypass table (`.claude/hooks/BYPASSES.md`, B-01 to B-08). The work has two parts: set every row's Status, and fix rows B-03 and B-05 in history_guard. Follow `.claude/agents/coder.md` in ~/Zynergy/Claude-kit, including items 1, 6, 8, 10 (with its last step) and 11.

# Base and state
Premises to verify:
- **main.** origin/main is 9425ef7b5989a3e8e887c826c7b2379680896f08 (PR #30 merge, batch 3). No tags. The record's last entry is 2026-09-25-80; no intent is open.
  - Create `kit-v0.2-bypass-rulings` from origin/main in ~/Zynergy/Claude-kit-fixes.
  - Push with the absolute path: `git -C /home/zynergy-labs/Zynergy/Claude-kit-fixes push -u origin kit-v0.2-bypass-rulings`. history_guard does not expand `~`.
- **C.** ~/Zynergy/Claude-kit, on main at 9425ef7. Only `.claude/worktrees/` is untracked, plus this dispatch's copy (expected `-41.md`; the hook's name governs). No sweep is needed.
- **Planner log B.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit/2d9316b0-e46a-5c35-b7e4-125fac5c9a3e.jsonl. Cite by timestamp and tool_use id.
- **BYPASSES.md.**
  - Rows B-01 to B-08 are all `open`.
  - The header (:15-16) says "`open` means known and not yet ruled on. The owner rules on each row: accepted, or fixed."
  - Proving tests are in `.claude/hooks/tests/test_bypasses.py`, along with the consistency test `test_table_docstrings_and_tests_agree`.
  - Guard docstrings carry "Known bypasses:" lines. role_guard's cites B-03 and B-08; check history_guard, device_guard, dispatch_guard and session_check.
- **history_guard.**
  - `PR_MERGE` is a whole-text regex; the merge rule is T17's (a)-(e).
  - `strip_heredocs` and `HEREDOC` drop lines after any `<<WORD` on a line, even one inside quotes or a comment. `command_lines` already tracks quotes and comments.
  - The T12 heredoc tests are in test_history_guard.py.

# Scope boundary
**1. Statuses.** In BYPASSES.md:
- B-01, B-02, B-04, B-06, B-07 and B-08 become `accepted`.
- B-03 and B-05 become `fixed`.
- The header's Status paragraph says what each value means:
  - `accepted`: known, ruled acceptable; the guard stays as is and the test keeps proving the bypass.
  - `fixed`: closed; the row stays as history and its test now asserts the block.

**2. Fix B-03.**
- history_guard denies, for every role, a `gh api` call to a pull request's merge endpoint (`repos/<owner>/<repo>/pulls/<N>/merge`, with or without a leading `/`) when it would write. That means any of:
  - `-X`/`--method` other than GET;
  - a field flag (`-f`, `-F`, `--field`, `--raw-field`);
  - `--input`.
- A plain GET, which only checks whether the PR is merged, is let through.
- The reason names `gh pr merge` under T17's rule as the one allowed route.
- Match on the whole text, as `PR_MERGE` does. B-02's interpreter forms stay accepted.
- The docstring states the rule.

**3. Fix B-05.**
- A `<<WORD` marker opens a heredoc only when it is outside single quotes, double quotes and `#` comments on its line. Use the same quote and comment states as `command_lines`.
- Every existing T12 heredoc test must keep passing: real heredocs, `<<-`, quoted `'WORD'`/`"WORD"` markers, and an unclosed heredoc.

**4. Tests.**
- test_b03 and test_b05 change to assert the block: each input gets a deny naming the reason.
- Rename them to `test_b03_…_is_blocked` and `test_b05_…_is_blocked`, and update the table's Test column.
- Add a GET-passes test for B-03.
- Add a regression test for B-05: a real heredoc whose body contains `git push origin main` is still dropped, so it gives no decision, as T12 established.

**5. Docstrings.**
- Each guard's "Known bypasses:" line cites only `open` and `accepted` rows.
- history_guard's and role_guard's lines drop B-03; history_guard's drops B-05.
- The consistency test must still pass. If it requires a fixed row to be cited somewhere, extend it minimally to allow `fixed` rows to be uncited, and say so.

**6. README.** One sentence in the bypass or Tests section: rows are ruled `accepted` or `fixed`, and where the table is.

**Record:**
1. This dispatch's copy and the intent.
2. The tests-only commit: changed test_b03 and test_b05, the new tests, and any consistency-test change.
3. The fix: history_guard.py, BYPASSES.md, the docstring lines, README.
4. The terminal.

Then the PR, the merge, and item 10's update of C.

# Closed decisions
The owner's AskUserQuestion answers in B (quote the option texts from the tool_use):
- B-01/B-02 "Accept (Recommended)"
- B-03 "Fix (Recommended)"
- B-04/B-05 "Fix B-05, accept B-04 (Recommended)"
- B-06/07/08 "Accept all three (Recommended)"

The owner, before those: "Do the two T8 findings as small tasks, then the bypass-table rulings" (B), and "Record it, don't restrict (Recommended)" for B-08.

Planner's choices:
- B-03's fix denies write calls to the merge endpoint outright rather than routing them through T17's rule. The option text allowed either ("denied unless it passes the same merge rule, or denied outright").
- GET stays allowed.
- The `accepted`/`fixed` wording.
- Uncited `fixed` rows.

B-11/B-13: coders merge at the planner's discretion.

# Merge
authorised: this PR only (kit-v0.2-bypass-rulings -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup. Run it from cwd C without `--repo`, as one Bash call. If it is denied, quote the denial and stop. Merge nothing else. Then item 10's update of C.

# Prediction
**Tests-only commit.**
- test_b03_…_is_blocked and test_b05_…_is_blocked fail: today the hook gives no decision.
- The B-03 GET test and the B-05 regression test pass.
- All other hook tests pass.
- Report the counts, starting from 138.
- tests/ stays at 93 OK.
- The consistency test fails only if the table's Test column is renamed in this commit. Keep the table unchanged here, so it passes. If renaming the test functions breaks the column lookup, report that.

**After the fix.**
- All hook tests OK, and 93 tests/ OK.
- Render checks 30/30 and 13/13.
- Both checkers PASS.
- release_check PASS, 27 files, no denylist hit.

**Revert check.**
- history_guard.py from 9425ef7 gives the same two failures.
- Show separately that reverting only the B-05 change fails only test_b05.

Put your mechanism prediction in the intent.

# Finish line and abort conditions
Finish line, pushed on kit-v0.2-bypass-rulings:
1. The copy and the intent.
2. The tests-only commit.
3. The fix.
4. The terminal.

Then CI green on the final commit, the backup, the merge with `--merge`, and the merge commit reported. After that, C updated by item 10's last step. No tag.

Abort and report on:
- Any Base-and-state mismatch other than the dispatch's name.
- A T12 heredoc test failing after the B-05 fix.
- A needed change to any guard other than history_guard's behaviour. Docstring lines are allowed.
- A failure for any reason other than the predicted one.
- Two failed fixes on one symptom.
- A denylist hit.
- CI not green (no merge).
- The merge denied by anything (quote it).
- The dry run or `--apply` exiting 1.
- An owner message after this Agent call that tells you to do or not do something in this scope.
- A planner message that widens the scope without quoting an owner ruling.

# Checks
- The failing-first run and both revert checks.
- Counts before and after.
- Both checkers before every commit that touches RECORD.md; read B in a separate call before each.
- Tests in their own Bash call.
- The backup evidence and the merge commit's parents.
- The update of C's outputs.

# Out of scope
- Fixing any `accepted` row.
- The `~` expansion in history_guard's push check.
- session_agents.py.
- D2 and D5.
- Other PRs, tags, deleting anything.

# Device items
None.