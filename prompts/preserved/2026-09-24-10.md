HEAD: 93904076a95498b3a877438f6d354fc1b1b88419
Target subagent: coder
Type: build
Preserved: 2026-09-24T00:36:26Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Close the "Kit live exercise" session (PID 101054)

## Role
You are the coder. You execute this dispatch, you make no design decisions, and a gap is a stop-and-ask.

## Base and state
- PID 101054 is `claude --rc Kit live exercise`, cwd `/home/zynergy-labs/Zynergy/Claude-kit`, the owner's exercise session, now finished with.
- Another coder is running the final dispatch for the Claude-kit v0.1 fixes in `~/Zynergy/Claude-kit-fixes` right now. Its step H5 waits for PID 101054 to exit before returning `~/Zynergy/Claude-kit` to `kit-v0.1`.
Verify the PID's cmdline and cwd first; if they differ, do not signal anything, and stop.

## Scope boundary
End PID 101054 and nothing else. Do not touch any file, any git checkout, RECORD.md, or any other process (not the other coder, not PID 100263, not any other `claude` process). Do not commit or push: the other coder owns the fix branch right now, and a concurrent commit would collide with it.

## Closed decisions
- Owner, 2026-09-23: "And close the session please."
- J1: send SIGTERM to 101054. If it is still alive after 15 seconds, send SIGKILL. Confirm it is gone and report its exit.
- J2, record: this dispatch is saved by the v0.1 hook in the harness worktree `~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01JDbTJ4Gj9C6xygXM5tPdHz/prompts/preserved/` (expected `2026-09-23-08.md`). Do not copy or record it now. Report its path and sha256 so the planner can have it cited in the record afterwards.

## Prediction
PID 101054 exits on SIGTERM within 15 seconds. Its session log under `~/.claude/projects/` stays in place.

## Finish line and abort conditions
Finish: PID 101054 gone; report the PID's cmdline and cwd as verified, the signals sent, and J2's path and sha256. Abort if the cmdline or cwd differ, or if anything beyond J1 would be needed.

Steps:
1. Verify.
2. J1.
3. J2 (read only).
4. Report.

## Checks
`ls /proc/101054` fails after J1. Confirm the session log still exists: find the file under `~/.claude/projects/` that contains "Live exercise 1: pulse hand-back". Read it only.

## Out of scope
Everything else, including the restore of `~/Zynergy/Claude-kit`, which the other coder handles.

## Device items
None.