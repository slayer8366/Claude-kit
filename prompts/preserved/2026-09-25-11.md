HEAD: d59280fceb136d04d2165d3851bdd2466a04ace6
Target subagent: pulse
Type: pulse
Preserved: 2026-09-25T03:21:28Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** pulse

# Role
Read-only pulse for the Claude-kit planner. It gathers the facts needed to put the design questions of v0.2 tasks T13 and T5 to the owner:
- **T13:** "An upgrade removes files the new version dropped; tested".
- **T5:** "A session-start hook warns when checkout or hook identity differs from the declared one; tested with a stale-hook fixture". Its open questions: where the expected identity is declared (kit.json or elsewhere), and whether it warns or blocks.

Answer with file:line or command output per claim. Change nothing.

# Base and state
- Read at origin/main 82725c468ee27f608cf76674044f17b9ffa513fa (T4 merged). Run `git rev-parse origin/main` first, in the planner worktree ~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB. That worktree's own HEAD is d59280f, so read files at origin/main with `git show origin/main:<path>` or `git grep ... origin/main`, not from its working tree.
- A docs coder is working in ~/Zynergy/Claude-kit-fixes right now. Don't read its working tree as the base.

# Rules
- Read only. No write, fetch, checkout, commit or push. Don't touch the counter.
- Mark anything inferred as inferred.
- If a question can't be answered, say why and go on.

# Questions
**T13**
1. install.py at origin/main: how does it handle an existing install? Cover:
   - Does it read the old `.claude/kit.lock`?
   - What does it do with files in the old lock that are absent from the new release set?
   - What does it do with a vendored file the adopter has edited (hash differs from the old lock)?

   Quote the relevant functions with line numbers. Is there any upgrade path at all, or only first install? What happens today if install.py runs twice, or over an older tag?
2. check_kit.py: what does it report for a file present in the adopter but absent from the lock, and for one in the lock but absent from disk?
3. tests/test_install.py: list the test names. Does any test cover a second install or an upgrade?
4. Which files have ever been dropped from release.json's `vendored` list? Use `git log -p origin/main -- release.json` and list additions and removals by commit.

**T5**

5. Claude Code hooks documentation, read-only, via the docs page https://code.claude.com/docs/en/hooks.md if the pulse can fetch it; otherwise say it cannot. Does a SessionStart hook event exist? What input does it receive (cwd, source, transcript_path, …)? Can its output block the session or only add context or a warning? Quote the relevant lines.
6. .claude/settings.json at origin/main: which hook events are registered, and with what commands? Quote it.
7. What identity could be declared today?
   - .claude/kit.lock's fields (from install.py's writer)
   - .claude/kit.json's keys (from guardlib's config validation, with the file:line of the unknown-keys rule)
   - README's config table
   - Would adding a key to kit.json break any existing adopter config under the "unknown keys are invalid" rule, given that templates/kit.json ships to adopters?
8. The stale-hook case from the record, facts only: which record entries describe sessions running old hooks from harness worktrees? Candidates are 2026-09-23-07's Observations, 2026-09-24-05 and the T3 intents; give lines. What distinguished a stale worktree: its HEAD versus origin/main, its hook file hashes, or both?