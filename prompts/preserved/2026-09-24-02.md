HEAD: 93904076a95498b3a877438f6d354fc1b1b88419
Target subagent: coder
Type: build
Preserved: 2026-09-24T14:32:27Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Claude-kit v0.2 T0: housekeeping; move ~/Zynergy/Claude-kit to main

## Role
You are the coder. You execute this dispatch, you make no design decisions, and a gap is a stop-and-ask. Record it as `coder.md` and the checkers require, and run `check_record.py` and `check_prompts.py` before every commit that touches `RECORD.md`.

## Base and state
- `slayer8366/Claude-kit`: PR #3 merged at 2026-09-24T14:31:04Z; `origin/main` = `45f053b8a2719824dbdc2cfa02157e0e4e98bb79`. No tags. `docs/specs/2026-09-24-kit-v0.2-tasks.md` defines T0.
- `~/Zynergy/Claude-kit-fixes`: on `kit-v0.2-plan` at `77870d5`, clean. Record work happens here on a new branch `kit-v0.2-t0` from `origin/main`.
- On `origin/main`, RECORD.md's last entry is `2026-09-24-02` (terminal); no intent is open. Store's last file is `prompts/preserved/2026-09-24-01.md`.
- `~/Zynergy/Claude-kit`: on `kit-v0.1` at `a544ca9`. Untracked: `.claude/worktrees/`, `prompts/preserved/2026-09-23-05.md` (sha256 `e255080b0cbaae10183e26f5500d7f0e115086c2cf66707b610c76920fb12bbd`) and `prompts/preserved/2026-09-23-06.md` (sha256 `75ef241e651f7fe3b00daa01c48ea97dab90c51d1454269263b864515d38344f`). `origin/main` tracks files at the same two paths, expected byte-identical.
- Processes with cwd `~/Zynergy/Claude-kit`, last seen: 110686 (`claude rc`, the bridge hosting the planner) and its child 110712. Report whatever is there now.
- `~/forager-backups/INDEX.md` last line is backup `2026-09-24-01`.
- This dispatch is saved by the planner's v0.1 hook in `~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01VssxEZCn7qBnENwZjcVycP/prompts/preserved/` (expected `2026-09-24-11.md`; identify by content and header time).
Verify all of this first; if anything differs, stop.

## Scope boundary
In `~/Zynergy/Claude-kit`: move the two untracked files to a backup and switch the checkout to `main`. Nothing else there: `.claude/worktrees/*` is not touched (reading is fine). In the fix worktree: the record entries and store copy only. No code, hook, test, `kit.json` or checker change. No v0.2 task other than T0. No merge, no tag. Delete nothing (a move into the backup is allowed for those two files only). Send no signal to any process.

## Closed decisions
Owner, 2026-09-24 (planner log `/home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-01VssxEZCn7qBnENwZjcVycP/ac03f257-0aff-541c-9135-20bb81e09317.jsonl`; quote with line numbers): "Merged" (PR #3), and to the planner's question on the two files, "Move to backup (Recommended)": confirm by sha256 they match main's tracked copies, move them into a new `~/forager-backups` folder with a manifest and INDEX line, then switch to main.

Naming (the hook's rule, UTC date plus next sequence in the fix store): this dispatch's store copy is `prompts/preserved/2026-09-24-02.md`; intent `2026-09-24-03`, terminal `2026-09-24-04`. If taken, use the next free and say so. The name collides with harness `2026-09-24-02.md` in 01JDb and 01Vss (different content); note it as the known R6(a) issue (T1).

Therefore:
- K1, compare: `git -C ~/Zynergy/Claude-kit fetch origin`; compare each untracked file's sha256 with `git show origin/main:prompts/preserved/<name> | sha256sum`. If either differs, stop.
- K2, backup: to a new `~/forager-backups/<UTC date>-<next free NN>/` (expected `2026-09-24-02`), MOVE the two files, keeping their relative path `prompts/preserved/`. `MANIFEST.sha256`, verified against the moved files and against the K1 hashes. One line appended to `INDEX.md` (append-only): what, why (they blocked the switch to main; identical copies are tracked on main), the dispatch/record entry, and restore (not needed; git holds them).
- K3, switch: list `claude` processes with cwd `~/Zynergy/Claude-kit`; report them; they do not block (the owner's ruling covers the switch). `git -C ~/Zynergy/Claude-kit switch main` (create the local `main` tracking `origin/main` if it does not exist, e.g. `git switch -c main --track origin/main`), then fast-forward to `origin/main`. Confirm HEAD `45f053b` on `main`, `git status` shows only `.claude/worktrees/` untracked, and the two files are present, tracked, with the K1 hashes. Run `python3 check_prompts.py` and `python3 check_record.py` in that checkout; both should pass.
- K4, record (fix worktree, branch `kit-v0.2-t0` from `origin/main`): copy this dispatch byte for byte (whole hook-saved file, header included) into the store as named above; intent `2026-09-24-03` claims it, citing T0 in the tasks file. The terminal records K1-K3 with evidence, and lists as NOT done by this dispatch, pending the owner: T0's third point, confirming a new harness worktree starts from main (see Device items).

## Prediction
- K1: both hashes match.
- K3: switch succeeds; both checkers pass in `~/Zynergy/Claude-kit` on main.
- Fix worktree: test counts unchanged (hook 83, install and release 18); both checkers pass.

## Finish line and abort conditions
Finish: K1-K4 done, PR `kit-v0.2-t0` → `main` open, CI green on its final commit, then STOP and report with the PR URL. Abort on: any Base-and-state mismatch; a K1 hash mismatch; a manifest mismatch; the switch refusing for any reason other than the two files (report the git message); any change needed to code or tests; two failed fixes on one CI symptom. Any owner message in the planner log after "Move to backup (Recommended)" that bears on this dispatch: stop and report.

Steps:
1. Verify.
2. In the fix worktree: branch; copy this dispatch into the store; append the intent, uncommitted.
3. K1, K2, K3.
4. Terminal; run all checks; commit; push.
5. PR; CI green.
6. STOP and report.

## Checks
Every commit passes, in `~/Zynergy/Claude-kit-fixes`:
- `python3 -m unittest discover -s .claude/hooks/tests -v`
- `python3 check_record.py --render-check`
- `python3 check_prompts.py --render-check`
- `python3 -m unittest discover -s tests -v`
- `python3 release_check.py`
If history_guard blocks a command over unparseable text (known, T12), write the text to a file under `/tmp/kitv02_*` and retry; record it as a deviation.

## Out of scope
T1-T16, merging, tags, harness worktrees, Forager, any process.

## Device items
Owner check, after this dispatch, not a gate on it: start a new session from the Claude app on this machine, then tell the planner. The planner will send a read-only pulse to confirm the new harness worktree's HEAD is `origin/main` (or later), which completes T0's third point.