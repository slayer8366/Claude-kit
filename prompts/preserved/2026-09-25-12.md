HEAD: d59280fceb136d04d2165d3851bdd2466a04ace6
Target subagent: coder
Type: build
Preserved: 2026-09-25T03:33:18Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit v0.2 task T13 (`docs/specs/2026-09-24-kit-v0.2-tasks.md` line 20): an upgrade removes files the new version dropped, tested. Follow `.claude/agents/coder.md` at your base. That version includes items 1-9 and the standing-rulings paragraph; this dispatch cites no standing ruling.

# Base and state
Premises to verify:
- **main.** origin/main is 3a5a13d268bdbc0ae5c8111ff9e38abf7c222848 (PR #13 merge). Nothing is tagged. The record's last entry is 2026-09-25-16; no intent is open. Create `kit-v0.2-t13` from origin/main in ~/Zynergy/Claude-kit-fixes.
- **Planner worktree W** (~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB, at d59280f). Its untracked store files:
  - D2 `2026-09-25-02.md` and D5 `-05.md`: cited, not copied; leave them.
  - `2026-09-25-11.md`: the planner's pulse "Pulse: T13 and T5 design facts". **Your sweep** copies it and adds a dispatch-note (Type pulse, Outcome answered).
  - This dispatch, expected as `2026-09-25-12.md`. If the hook gave another name, that name governs.

  `find_dispatches.py` should list no `stop` items.
- **Planner log.** /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-013ve7bxjxrGv4tLa8p1kdHB/791cc457-81b7-586e-b2bb-20985d9699d6.jsonl.
- **Code facts** (from the planner's pulse; verify each):
  - install.py never reads an existing `.claude/kit.lock`. `LOCK` is used only to write it (:94-96). There is no delete anywhere. Vendored files are overwritten unconditionally (:81-84). settings.json stops the install if it exists and differs from the new release's bytes (:73-78). Templates are written only if absent (:88-89). Adopter-owned paths are rejected (:32, :61-63).
  - check_kit.py reports only files in the lock (:31-34).
  - tests/test_install.py has 11 tests in `InstallAndDrift`, one tag (`TAG`, :19, :46), and no upgrade test.
  - release.json has never dropped a vendored file.

# Scope boundary
Files:
- **`install.py`:** the upgrade rules below, and its docstring.
- **`tests/test_install.py`:** new upgrade tests, and fixture helpers for a second tag.
- **README.md:** the "Install and drift" section, to describe upgrades.

Record:
- the sweep: the pulse copy and its note, expected ID 2026-09-25-17
- this dispatch's copy and intent -18
- the tests-only commit, the fix, and terminal -19

No change to check_kit.py (see Closed decisions), hooks, checkers, find_dispatches.py, release.json, kit.json, templates, coder.md, CI, or `docs/standing-rulings.md`.

# Closed decisions
Owner rulings in the planner log:
- **The AskUserQuestion result** beginning "Your questions have been answered: \"T5: what counts as the \"expected\"": T13 answered "Unchanged-only rule (Recommended)", and SR-02 answered "Unedited may update". Quote both options' texts from the tool_use input.
- **"Merged."** (PR #13).
- **The AskUserQuestion result** answering "T13: the check_kit part …": "Drop it (Recommended)". The check_kit clause of the T13 option is withdrawn. Record that the planner withdrew it, with the owner's agreement, and why.

The SR-02 answer supersedes ruling 4(6) of 2026-09-23-01 (RECORD.md:56-57). The intent carries a field `Supersedes-ruling: 2026-09-23-01 ruling 4(6)`, quoting the old wording and the new rule.

Cite each by line and timestamp.

The planner's rules for install.py:
1. **First install** (no `.claude/kit.lock` in the target): behaviour unchanged, including the settings.json rule.
2. **Upgrade** (a lock exists; read its `files` map as the old hashes). Before writing anything, classify every path:
   - **(a) Kept:** in the old lock and in the new release.
     - On disk with sha256 equal to the old lock's: unchanged, so overwrite.
     - Missing from disk: write it.
     - Differs from the old lock: edited, so stop.
     - `.claude/settings.json` follows this rule on an upgrade; the first-install settings rule does not apply.
   - **(b) Dropped:** in the old lock, not in the new release.
     - Equal to the old lock: remove the file. Leave directories in place.
     - Missing: nothing to do.
     - Differs: edited, so stop.
   - **(c) New:** in the new release, not in the old lock.
     - Absent on disk: write it.
     - Present and byte-identical to the new release: fine.
     - Present and different: stop (a file of the adopter's at a path the kit now ships).
   - **Stopping:** any stop happens before anything is written or removed. It lists every stopping path with its reason ("edited since the installed tag", or "exists and is not the kit's"), exits 1, and leaves the target byte-for-byte as it was.
   - **Otherwise:** apply the writes and removals, write templates as now, and write the new lock last. Print each removed path.
3. **An unreadable or malformed existing lock:** stop before writing, naming the lock. Don't treat it as a first install.
4. **The docstring and README** state these rules, including that an upgrade can remove files.

# Prediction
- **Tests-only commit.** New tests use a second kit tag built from the first with changes: a dropped vendored file, a changed hook file, a changed settings.json, and a new vendored file, as each test needs.
  - u1: an unchanged dropped file is removed, and the lock has the new tag and no entry for it
  - u2: an edited kept file stops the upgrade; the target is unchanged, the path is named, and it exits 1
  - u3: an edited dropped file stops the same way
  - u4: an unedited settings.json is replaced by the new release's
  - u5: an edited settings.json stops
  - u6: a new-path file already present and different stops
  - u7: an upgrade with only a changed hook file succeeds, and check_kit then passes
  - u8: a malformed lock stops

  Before the fix:
  - u1 fails: the file remains.
  - u2, u3 and u6 fail: exit 0, file overwritten or left.
  - u4 fails: exit 1, "exists and differs".
  - u8 fails: the malformed lock is ignored and the install succeeds.
  - u5 and u7 pass.

  tests/: 29 + 8 = 37 run, 6 failures. Hook tests stay at 106.
- **After the fix:**
  - 37 tests/ OK and 106 hook tests OK
  - render checks 30/30 and 10/10, both checkers PASS
  - release_check PASS with 19 files: install.py is not released, so the count is unchanged
- **Revert check:** install.py from origin/main against the committed tests gives the same 6 failures.

Your mechanism prediction goes in the intent.

# Finish line and abort conditions
Finish line, pushed on kit-v0.2-t13:
1. the sweep
2. the copy and the intent
3. the tests-only commit
4. the fix
5. the terminal

Then PR kit-v0.2-t13 -> main open, with CI green on its final commit. No merge, no tag. In particular, never tag this repository; the tests tag only their temporary copies.

Abort and report on any of these:
- any Base-and-state mismatch other than the dispatch's name
- a needed change outside scope, including check_kit.py
- a test failing first for a reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit

Owner messages after this dispatch's Agent call: an abort only if one tells you to do or not do something in T13's scope, or changes a decision above. Record every other message in the terminal. If unsure, stop. A planner message follows coder.md item 6.

# Checks
1. Failing-first run and revert check, as predicted. Each "unchanged target" assertion compares every file under the adopter, bytes and presence, before and after.
2. All counts before and after. Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
3. Run tests in their own Bash call, never with a commit or push.

# Out of scope
- check_kit.py and `docs/standing-rulings.md`. SR-02's text there now differs from this ruling: flag it, don't edit it.
- T5, T10, the T3 backlog, updating W.
- Merge, tag, deleting anything outside the tests' temporary directories.

# Device items
None.