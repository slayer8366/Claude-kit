HEAD: 48c391dd2dd1f8c71b48c1cb3690bfe56510e6b0
Target subagent: coder
Type: build
Preserved: 2026-09-26T01:56:39Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder for Claude-kit review fix R4: the merge rule's backup check ties the bundle to the branch, matches INDEX.md by whole tokens, and reads the remote for freshness; the README says what the check proves and makes the undo procedure safe; two BYPASSES rows are brought up to date. Follow `.claude/agents/coder.md` at the base (48c391d), including items 1, 2, 5, 6, 10 and 12. Deferral is not allowed in this build.

# Base and state
Premises to verify. Line numbers are at 48c391d; drift is drift.
- **origin/pre-main** is 48c391dd2dd1f8c71b48c1cb3690bfe56510e6b0 (the PR #37 merge). origin/main is 0319ccd. No tags. Both branches carry GitHub protection.
- **Branch:** create `kit-review-r4` from origin/pre-main in ~/Zynergy/Claude-kit-fixes (on kit-review-r3 at 27226a2, clean). Push with `git -C /home/zynergy-labs/Zynergy/Claude-kit-fixes push -u origin kit-review-r4`.
- **C** (~/Zynergy/Claude-kit) is on pre-main at 48c391d, clean but for `.claude/worktrees/`. **W**, your Bash cwd, is /home/zynergy-labs/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_01RGmfuBWv4x1Nvnpa3fBQDZ at 48c391d; its only untracked file is this dispatch's copy (expected `prompts/preserved/2026-09-26-03.md`; the hook's name governs). Copy it byte for byte, keep the name, record path, size and sha256 in the intent. W's hooks are now R3's, so your Bash calls are judged at command position; no write-then-run detour should be needed.
- **The record** at 48c391d ends with terminal 2026-09-26-26 (closing -25, completed). No intent is open. The sweep owes one `merge` entry: PR 37, Head kit-review-r3, Base pre-main, Merge-commit 48c391dd2dd1f8c71b48c1cb3690bfe56510e6b0, Pre-merge 0ff36345d0812c956ab1a423089d0b26ecf65468, Backup 2026-09-26-04, Merged-by `coder preserved/2026-09-26-02.md`, Carries 2026-09-26-24 to -26, merged 2026-09-26T01:51:46Z. Derive the values yourself. INDEX.md rows 2026-09-26-03 and -05 are update_worktree moves, not merge backups.
- **Planner log F** is /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit--claude-worktrees-bridge-cse-01RGmfuBWv4x1Nvnpa3fBQDZ/d5592fbe-c371-5027-aaf9-18ab19a5613c.jsonl. Read it in a separate call before each record commit; do not print its long lines.
- **history_guard.py** at 48c391d (875 lines): docstring (d) :18-27, (e) :28-30; `SHA40` and `MANIFEST_LINE` near :167-168; `merge_backup_problem` :613-704: `branch` accepted as any non-empty string :643-644; `git bundle list-heads` :677-683 checks only that `sha` appears as a first field :682; INDEX.md check :685-692 with `folder.name in line` and `sha in line` as substrings :689; freshness by local `rev-parse origin/<branch>` :694-703, no timeout, no remote read. `gh_problem` :767-781 denies `alias` with `set` after it (R3); `GRAPHQL_MERGE` :197.
- **Tests** at 48c391d: `.claude/hooks/tests/test_history_guard.py` `class MergeRule` :381-494: `setUp` :386-398 makes a bare origin, a clone `work` on main pushed and fetched, and an empty backups dir; `write_backup(pr, index)` :403-423 bundles `origin/main` as `main.bundle`, writes merge.json with branch `main`, MANIFEST.sha256, and an INDEX line `- {folder}: #{pr}, main at {sha}` (or the "none", "no-pr", "no-sha" variants); m1 to m9 :435-494; m6 :460-465 pushes a later commit and fetches before asserting "(e) freshness". `.claude/hooks/tests/test_bypasses.py`: `test_b02_interpreter_splits_words`, `test_b14_...` (GraphQL and `gh alias set`).
- **BYPASSES.md** at 48c391d: rows B-01 to B-15. B-02's text still says "The force-push, PR-merge and merge checks match the whole text with whitespace between the words", which R3 made false; B-14 names `gh alias set` only, while `gh alias import <file>` also defines aliases.
- **README.md** at 48c391d: "## Branches" :119; "## Merging and undoing a merge" :149-196, with "history_guard lets the merge through only ..." at :156 and the undo bullets at :186-189 (revert; "reset the branch to merge.json's `sha` and force-push it").
- Counts at 48c391d: hook tests "Ran 170 tests" OK; tests/ "Ran 94 tests" OK (its install test mirrors hook failures); render checks 39/39 and 13/13; release_check 27 files, no match.

# Scope boundary
Files: `.claude/hooks/history_guard.py`, `.claude/hooks/tests/test_history_guard.py`, `.claude/hooks/tests/test_bypasses.py`, `.claude/hooks/BYPASSES.md`, `README.md`, `RECORD.md`, the store copy. Conditions (a), (b) and (c), the walk, role_guard and guardlib stay as they are.

1. **Condition (d), tightened.**
   - `branch` must be one of the config's protected_branches; otherwise denied naming the branch and the list.
   - `git bundle list-heads` must list the pair `<sha> refs/remotes/origin/<branch>` on one line; `sha` on another ref, or the ref at another sha, is denied naming what the bundle lists.
   - INDEX.md: the line must contain the folder's name, `#<N>` and `sha` each as a whole token: bounded by characters that are not letters, digits, `-` or `_` for the folder name, not digits for N (as today), and not hex digits for the sha.
2. **Condition (e), remote freshness.** After the local check as today, the hook runs `git ls-remote --quiet origin refs/heads/<branch>` in the payload's cwd with a 20-second timeout. The remote's sha must equal merge.json's `sha`; a mismatch is denied naming both ("the branch moved on the remote after the backup"). A failure, empty output or timeout is denied: "(e) freshness: the remote could not be read (<reason>); a merge is not run blind." The local check stays first so its message still names a missing fetch.
3. **`gh alias import`** is denied like `gh alias set`, for every role, with the same message shape.
4. **Docstring.** (d) and (e) rewritten to state the rules above, including what the check does not prove: the PR's base branch is not read, and the bundle's content is verified only by its listed head and the manifest.
5. **README.md.** In "Merging and undoing a merge": replace the "lets the merge through only ..." sentence with what history_guard proves (a self-consistent backup whose bundle head is the protected branch at the recorded SHA, and that SHA is the branch's tip on the remote at merge time) and what it does not (the PR's base; the bundle beyond its head). Rewrite the undo procedure: revert is the default; reset and force-push only when `git rev-list <sha>..origin/<branch>` lists exactly the merge commit and the merged PR's own commits, since anything else in that range would be lost; the owner runs both by hand. Add one sentence to "Branches" or this section: after a promotion into main, updating C fast-forwards pre-main, which does not move, so the update is a no-op.
6. **BYPASSES.md.** B-02's text rewritten to what is true now (the interpreter passes git's or gh's words as separate strings, so no shell segment has git or gh as its command word; the walk does not read interpreter code), still `accepted`, test unchanged. B-14's text and test extended to `gh alias import`.
7. **Tests, first**, in `MergeRule`: m10, origin moves after the backup with no local fetch, denied "(e) freshness" and "remote"; m11, a bundle of another ref at the same sha (make branch `other` at main's commit, push it, fetch, bundle `origin/other` as the backup's bundle with merge.json still naming `main`), denied "(d) backup" naming the ref; m12, merge.json `branch` "feature" with a matching bundle and INDEX line, denied "(d) backup" and "protected"; m13, INDEX line naming folder `2026-01-01-pr1` only as part of `2026-01-01-pr12` (backup folder named `2026-01-01-pr1`, line `- 2026-01-01-pr12: #1, main at <sha>`), denied "(d) backup" and "INDEX.md"; m14, the remote unreadable (remote URL pointed at a non-existent path after the backup, or the origin directory removed), denied "(e) freshness" and "could not be read"; m1 and m2 still pass with the fixture's real bare origin. In test_bypasses, b14 gains `gh alias import aliases.yml`.

**Record**, in order: the sweep (the merge entry for #37); this copy and the intent; the tests-only commit; the fix (history_guard.py, BYPASSES.md); the docs commit (README); the terminal.

# Closed decisions
- The owner's D1 and "I'll take your recommendations" (F lines 157 and 165), recorded in intent 2026-09-26-02 and docs/specs/2026-09-26-review-fixes-plan.md at 48c391d, whose D4 reads: history_guard checks freshness with one `ls-remote`, timed out and failing closed.
- Planner's choices: `branch` limited to protected_branches rather than reading the PR's base; the pair match on list-heads; token boundaries for the INDEX match; the 20-second timeout; the local check kept before the remote one; `alias import`; the test list; the README wording rules.
- Standing: none cited. The merge rests on this dispatch's Merge section.

# Merge
authorised: this PR only (kit-review-r4 -> pre-main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup of origin/pre-main. One Bash call from a checkout of this clone (W is one), no `--repo`. Note that your own merge runs under W's hooks at 48c391d, not under this PR's rule. If denied, quote the denial and stop. Then update C with the merged update_worktree.py (dry run, then `--apply`), then W the same way; both should target origin/pre-main; if either targets another branch, stop before `--apply`. Report all four outputs.

# Prediction
Planner's outcome prediction (verbatim in the intent's planner field):
- Tests-only commit: the hook suite fails on 6 cases: m10 (today the stale local ref still equals the backup's sha, so the merge is allowed), m11 (sha present in list-heads, allowed), m12 (any branch string accepted, allowed), m13 (substring match, allowed), m14 (no remote read, allowed), and b14's `alias import` (let through). m1 to m9 and every other test pass. tests/ mirrors the failures through its install test. Name the exact count and any case failing for another reason.
- After the fix: hook tests 170 plus the new tests, all OK; tests/ 94 OK; render checks 39/39 and 13/13; both checkers PASS; release_check PASS, 27 files, no denylist hit; CI green on the final commit. The merge of this PR itself passes W's rule at 48c391d as before.
- Revert check: history_guard.py from 48c391d under the new tests gives the same 6 failures.
Put your mechanism prediction in the intent.

# Finish line and abort conditions
Finish line, pushed on kit-review-r4: (1) the sweep; (2) this copy and the intent; (3) the tests-only commit; (4) the fix; (5) the README commit; (6) the terminal, Outcome `completed`, closing at the PR open, CI green on its final commit and the backup written. Then the merge and the two updates, reported in the hand-back. No tag.

Abort and report on any of:
- a Base-and-state mismatch other than the copy's name or line drift
- any existing test failing after the fix
- a needed change to conditions (a) to (c), the walk, role_guard or guardlib
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- a denylist hit
- CI not green on the final commit (no merge)
- the merge denied by anything (quote it)
- a dry run targeting a branch other than pre-main, or any dry run or `--apply` exiting 1
- an owner message in F after this dispatch that tells you to do or not do something in this scope
- a planner message that widens the scope without quoting an owner ruling

# Checks
- The failing-first run and the revert check, with the exact FAILED lines.
- Counts before and after, each suite in its own Bash call.
- Both checkers before every commit touching RECORD.md; F read in a separate call before each.
- The merge entry's values against `git log --format=%H%x20%P` and `gh pr view 37`.
- The backup evidence and the merge commit's parents; the four update outputs.
- The hand-back ends with "Decisions I made", "Flags outside scope" and "Deferred".

# Out of scope
- R6: timeouts on the other subprocess calls.
- R5, R7, update_worktree.py, run_exercise.py, session_agents.py, launch_session.py.
- Reading the PR's base branch in the hook; `gh pr view` in the hook.
- docs/standing-rulings.md, templates, settings files, coder.md.
- The T3 backlog, other PRs, tags, deleting anything.

# Device items
None.