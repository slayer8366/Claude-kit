HEAD: cda554b0a77bca36469193987ee5925188ebc2c5
Target subagent: coder
Type: build
Preserved: 2026-09-25T16:31:54Z by .claude/hooks/dispatch_guard.py
--- verbatim prompt follows ---
**Type:** build

# Role
You are the coder finishing Claude-kit v0.2 T14-T16 after the original coder halted, and first updating the main checkout this session's hooks run from. Your agent definition was loaded from an old checkout (cda554b). Before anything else, read `.claude/agents/coder.md` from ~/Zynergy/Claude-kit-fixes (branch kit-v0.2-t14-16) and follow that version, including items 1, 6, 10 and 11. The T14-16 dispatch is `prompts/preserved/2026-09-25-26.md` on that branch. Read it in full; its scope, rules and Merge section carry over unchanged except where this dispatch says otherwise.

# Base and state
Premises to verify (read-only) before you write anything:
- **Main checkout C** = ~/Zynergy/Claude-kit, branch main at cda554b (PR #7 merge, T12). Its only untracked path is `.claude/worktrees/`, plus whatever the dispatch hook saved for this dispatch under `prompts/preserved/`. This session's hooks run from C, so today history_guard blocks every PR merge and kit.json has no `backup_dir` and no `Merge` section.
- **origin/main** is 1599a53975b8f980035c594d52f7d125de768370 (PR #24 merge, T10). No tags.
- **kit-v0.2-t14-16**, in ~/Zynergy/Claude-kit-fixes with a clean tree, at 4497d61c8d3445cb5ec3f4f93169312666b588bd. It is equal to origin/kit-v0.2-t14-16 and holds the sweep (d710a86), the store copy with intent 2026-09-25-60 (aa7737a), docs (119f97a), tests-only (a94f354) and the fix (4497d61). There is no terminal for -60.
- **PR #25** (kit-v0.2-t14-16 -> main) is OPEN, head 4497d61. All four CI checks passed (runs 36153197791 and 36153318247).
- **The original coder** ran from the other planner session (W, ~/Zynergy/Claude-kit/.claude/worktrees/bridge-cse_013ve7bxjxrGv4tLa8p1kdHB). The owner reports it halted while waiting for CI. Before every push, `git fetch origin` and confirm origin/kit-v0.2-t14-16 is still at the head you last saw. If someone else has pushed, stop and report.
- **This dispatch's store copy:** the dispatch hook saved it in C under `prompts/preserved/`. The name the hook gave governs. If a file with that name already exists on origin/main or on kit-v0.2-t14-16 with different bytes, stop and report.
- **Planner log (this session):** under /home/zynergy-labs/.claude/projects/-home-zynergy-labs-Zynergy-Claude-kit/ (session 2d9316b0-e46a-5c35-b7e4-125fac5c9a3e; confirm the file). The other planner's log is the one named in 2026-09-25-26. Cite by timestamp first.

# Scope boundary
In order:
1. **Record first.** On kit-v0.2-t14-16, commit this dispatch's store copy and an intent entry for this dispatch. Use the next free ID; check_record decides whether it may open while -60 is open. If check_record refuses, stop and report. Push.
2. **Update C.** Run `python3 ~/Zynergy/Claude-kit-fixes/update_worktree.py /home/zynergy-labs/Zynergy/Claude-kit` as a dry run. It must exit 0 with a plan that fast-forwards cda554b to origin/main and moves nothing outside `prompts/preserved/`. Then run the same command with `--apply`. Report both outputs verbatim. Do not touch `.claude/worktrees/` or any worktree in it.
3. **T14-16 terminal.** Write the terminal closing 2026-09-25-60, from evidence you gather yourself. The original coder's hand-back is not available to you; say so in the terminal.
   - Evidence: the commits, the counts at 4497d61, a failing-first run at a94f354 (the consistency test fails), a revert check (4497d61 without BYPASSES.md makes only the consistency test fail), and CI on #25.
   - The bypass candidates (1)-(6) as BYPASSES.md and test_bypasses.py record them.
   - Record the halt and the handover as a deviation.
   Push, and wait for CI to go green on that commit.
4. **This dispatch's terminal**, on the same branch, before the merge. Report the merge itself in the hand-back, as terminal 2026-09-25-37 did. Push, and wait for CI to go green.
5. **Merge.** Write the backup per coder.md item 10 in the next free ~/forager-backups folder. Check it with `sha256sum -c`, `git bundle list-heads` and `git bundle verify`, and add one INDEX.md row naming the folder, #25 and the SHA. Then run `gh pr merge 25 --merge` as one Bash call, with nothing else in it.

No guard, test, doc or config change. The files you may write are RECORD.md, this dispatch's store copy, the backup folder and INDEX.md. update_worktree.py changes C and writes its own backup folder.

# Closed decisions
- **Owner, this session:** "Agent is waiting for CI runs but is halted, so it can't check. So go ahead and pick up from the CI check."
- **Owner's AskUserQuestion answer, this session:** "Update checkout, then merge (Recommended)". The option text: "First a coder runs update_worktree.py (dry run, then --apply) on ~/Zynergy/Claude-kit, which fast-forwards main to origin/main and brings in T17's merge check. Then a second coder writes terminal -61, makes the item-10 backup and merges #25 with --merge once CI is green. The hooks are re-read on every call, so no restart is needed." The planner folded both coders into this one dispatch. The ID -61 is not fixed; the next free ID governs.
- **The Merge section of 2026-09-25-26:** "authorised: this PR only (kit-v0.2-t14-16 -> main), with `--merge`, once CI is green on its final commit, after coder.md item 10's backup."

# Merge
authorised: PR #25 only, with `--merge`, once CI is green on its final commit, after coder.md item 10's backup. The merge may run with cwd C and without `--repo`. Merge nothing else.

# Prediction
- **Dry run:** exit 0, "fast-forward: cda554b..1599a53 (origin/main)", and nothing to move unless the hook's store copy of this dispatch is tracked on main.
- **After `--apply`:** C's HEAD equals origin/main, and C's `.claude/hooks/history_guard.py` and `.claude/kit.json` equal origin/main's. `backup_dir` becomes "~/forager-backups".
- **Counts at 4497d61:** hook tests OK, 129 plus the T14 tests. 47 tests/ OK. Both checkers PASS. release_check PASS with 24 files.
- **Merge:** history_guard lets `gh pr merge 25 --merge` through, and #25 is MERGED with first parent 1599a53, unless main moved.

Put your mechanism prediction in the intent.

# Finish line and abort conditions
**Finish line:** C updated; on kit-v0.2-t14-16, pushed in order: the store copy with the intent, the T14-16 terminal, this dispatch's terminal; CI green on the last one; the backup verified; #25 merged with `--merge`; the merge commit reported. No tag.

Abort and report on any of these:
- any Base-and-state mismatch other than the dispatch's name
- the dry run or `--apply` exiting 1 (report the output, no workaround)
- anyone else pushing to kit-v0.2-t14-16
- check_record refusing the entry order
- a failure for any reason other than the predicted one
- two failed fixes on one symptom
- CI not green (don't merge)
- history_guard denying the merge after the backup (quote it)
- an owner message after this Agent call that tells you to do or not do something in this scope, or changes a decision here
- a planner message that widens the scope or changes a closed decision without quoting an owner ruling

# Checks
- Counts, a failing-first run and a revert check, as in step 3.
- Both checkers before every commit touching RECORD.md. Read the planner log in a separate call before each such commit.
- Tests in their own Bash call.
- The backup evidence.
- `git -C /home/zynergy-labs/Zynergy/Claude-kit status --porcelain` after the update.

# Out of scope
- T6-T8.
- D2 and D5.
- Updating W or any worktree under `.claude/worktrees/`.
- Other PRs, tags, closing any bypass, deleting anything.

# Device items
None.